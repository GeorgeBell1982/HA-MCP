# Phase 3A Guarded Apply Core Contracts

Phase 3A is an unregistered, adapter-neutral guarded-application core. It has no MCP tool, registry entry, CLI command, live Home Assistant adapter, grant producer, deployment change, version change, or runtime write enablement. All live-effect ports are injected test doubles or future adapters; the contract default is `writesEnabled: false`.

## Transaction Journal

Transactions use schema version 2 and the exact states:

`intent_prepared`; `apply_committed`; `post_validation_succeeded`; `reload_intent`; `reload_succeeded`; `verification_succeeded`; `rollback_intent`; `rollback_committed`; `rollback_validation_succeeded`; `rollback_reload_intent`; `rollback_reload_succeeded`; `rollback_verification_succeeded`; `manual_recovery_required`.

Terminal outcomes are:

- `verification_succeeded`: verified
- `rollback_verification_succeeded`: rolled_back
- `manual_recovery_required`: blocked

Each durable record binds `transactionId`, `proposalId`, `proposalStorageSha256`, canonical relative `path`, `expectedSha256`, `candidateSha256`, `diffSha256`, `checkpointId`, `checkpointSha256`, `impact`, immutable `reloadTarget`, mutable monotonic `rollbackReloadRequired`, `version`, `priorState`, timestamps, and structured failure. The journal port is compare-and-swap by `transactionId` and `version`; every accepted transition increments `version` once and is durable before the next effect. Exact transaction reads support bounded CAS reconciliation; immutable identity is rechecked before retrying manual recovery, while `rollbackReloadRequired` may only change from false to true on `rollback_intent`. Journal adapters must enforce the shared legal transition adjacency including durable candidate reload `post_validation_succeeded -> reload_intent -> reload_succeeded` for targeted reloads, direct `post_validation_succeeded -> reload_succeeded` for no-reload changes, and rollback reload `rollback_validation_succeeded -> rollback_reload_intent -> rollback_reload_succeeded -> rollback_verification_succeeded` when required. Terminal states have no legal outgoing transition. Version 1 journal envelopes are rejected non-destructively; checkpoint envelopes remain version 1. All exported exact state arrays, transition adjacency arrays, and recovery tables are recursively frozen at runtime so consumers cannot weaken journal legality.

## Approval Grants

Approval grants are injected only. Phase 3A provides no persistence, CLI, or producer for grants. A single-use apply grant must bind `grantId`, `proposalId`, `proposalStorageSha256`, `candidateSha256`, `diffSha256`, `operation=apply`, `risk`, exact `impact`, exact `reloadTarget`, `issuedAt`, and `expiresAt`.

The approval port requires the exact pending proposal identity and fails closed before `issuedAt`, at expiry (`now >= expiresAt`), on replay, wrong binding, proposal storage drift, candidate or diff drift, discarded/expired/nonpending proposals, and cancellation before consumption.

## Apply Ordering

The coordinator order is:

1. Load proposal and run policy before the path lock.
2. Acquire a bounded lock keyed by validated canonical relative path.
3. Reload proposal, recheck exact identity, and rerun policy.
4. Read source and verify the expected digest.
5. Validate candidate locally.
6. Consume the single-use approval grant.
7. Create checkpoint.
8. Durably create `intent_prepared`.
9. Atomically apply candidate.
10. Durably transition `apply_committed`.
11. Run post-apply validation.
12. Durably enter `reload_intent` and dispatch the exact stored reload target only when `reloadTarget` is non-null; no-reload changes transition directly to `reload_succeeded`.
13. Verify and terminally transition `verification_succeeded`.

Policy denies disabled writes, restart-required impact, missing apply or domain reload capability, nonpending/expired/discarded proposal state, invalid impact, and identity drift. Denials happen before effects.

`AtomicApplyPort.replace` reaches the commit point only at same-directory temp write, file sync and close, atomic rename, and parent directory sync. It reports `before_commit`, `committed`, or `commit_unknown`; `commit_unknown` is treated as possibly committed and reconciled by digest. If the live digest is candidate, the coordinator durably transitions to `apply_committed` with commit-unknown evidence and continues normal post-commit handling. If the live digest is expected/checkpoint, it routes through `rollback_intent -> rollback_committed -> rollback_validation_succeeded -> rollback_verification_succeeded` without restoring bytes. Other or missing digests become manual recovery.

## Rollback And Cancellation

Precommit cancellation has no live effect. After the apply commit point, caller cancellation is ignored and the coordinator uses an internal context to finish verification or rollback. Every post-commit failure carries the latest durable record forward, writes `rollback_intent` before restoring the checkpoint, and sets `rollbackReloadRequired` only when candidate reload dispatch may have started. It then writes `rollback_committed`, runs rollback validation, writes `rollback_validation_succeeded`, performs one exact rollback reload through `rollback_reload_intent -> rollback_reload_succeeded` when required, runs rollback verification, and terminally writes `rollback_verification_succeeded`. Immediately after every checkpoint load, the coordinator recomputes SHA-256 and compares it with `checkpointSha256` before any restore, rollback validation, or rollback verification; mismatch writes `manual_recovery_required` from the latest legal state with no restore attempt. Rollback ambiguity or failure writes `manual_recovery_required`. Rollback restore treats returned and thrown `before_commit`, `committed`, and `commit_unknown` classifications explicitly: `committed` completes rollback validation and verification; `before_commit` moves to manual recovery from `rollback_intent`; `commit_unknown` reads the live digest and only claims `rollback_committed` when the digest is expected/checkpoint, otherwise it requires manual recovery. There is no implicit restart.

## Locks

Resource locks validate canonical relative paths, serialize the same path, allow distinct paths, bound waiters, check signal and deadline before enqueue, on wake, and before return, remove aborted/deadline waiters, and release on all exits.

## Startup Recovery

Startup recovery never reapplies the candidate. It returns one `Phase3RecoveryResult` per inspected record with `transactionId`, terminal state, observed digest classification, observed digest value, disposition (`verified`, `rolled_back`, or `manual_attention_required`), and the durable record. It reads the current live digest and follows the durable table:

- `intent_prepared`: expected/checkpoint routes through no-live-effect rollback completion; candidate restores checkpoint; other or missing writes manual recovery.
- `apply_committed`, `post_validation_succeeded`, and `rollback_intent`: candidate restores checkpoint; checkpoint routes through rollback validation and verification without restore; other or missing writes manual recovery.
- `reload_intent` and `reload_succeeded`: candidate restores checkpoint and requires rollback reload; checkpoint routes through rollback validation plus rollback reload when required; other or missing writes manual recovery.
- `rollback_committed`: checkpoint completes rollback validation and rollback reload or verification; candidate, other, or missing writes manual recovery.
- `rollback_validation_succeeded`: checkpoint completes rollback reload when required or rollback verification only; candidate, other, or missing writes manual recovery without moving backward to `rollback_committed`.
- `rollback_reload_intent`: startup recovery requires manual recovery because dispatch completion is uncertain.
- `rollback_reload_succeeded`: checkpoint completes rollback verification only; candidate, other, or missing writes manual recovery.
- `verification_succeeded`: candidate is verified with no effect; expected/checkpoint, other, or missing digests require external manual attention without changing the terminal transaction.
- `rollback_verification_succeeded`: checkpoint is rolled back with no effect; drift requires external manual attention without changing the terminal transaction.
- `manual_recovery_required`: no effect.

`commit_unknown` is not a journal state; it is structured failure evidence and uses the same digest-driven conservative recovery.

## Phase 3B Protected Proposal Input

Phase 3B adds one unregistered read-only adapter from the existing protected Phase 2 proposal store to Phase3ProposalPort. Exact lookup validates a canonical lowercase UUID, reads only the corresponding protected file, applies the existing no-follow, private-file, single-link, stable-identity, strict-schema, canonical-JSON, and storage-digest checks, and never scans or quarantines. Raw file bytes are zeroed after parsing.

The adapter preserves proposal state, maps current Phase 2 `none` and `restart_required` reloadImpact values to a null reload target, binds the Phase 2 storage digest as proposalStorageSha256, and requires public/protected proposal IDs, idempotency keys, candidate digests, and diff digests to agree. Current Phase 2 `domain_reload` proposals without an explicit stored target are rejected; the adapter never infers a target from the path. Candidate and exact-diff base64 are canonical, strict UTF-8, and digest-verified before a fresh candidate buffer is returned; temporary protected buffers are zeroed. Stable errors reveal no protected content.

Phase 3B does not add a journal, checkpoint, source, write, validation, reload, verification, approval producer, CLI, MCP tool, registry entry, configuration flag, package change, mapping change, or runtime import. Live writes remain disabled.

## Phase 3C Durable Transaction Journal

Phase 3C adds an unregistered append-only implementation of Phase3JournalPort. Each immutable version is a private regular file named by canonical transaction UUID and zero-padded version and contains one strict canonical integrity-enveloped record. Histories start at intent_prepared version 0, remain contiguous, preserve immutable identity and createdAt, and follow the shared legal transition graph.

A version is prepared as record.json in a unique same-parent private pending directory. The record is fully written with short-write and EINTR handling, file-synced, closed, the pending directory is synced, and its no-follow descriptor identity and bytes are rechecked before an atomic hard link creates the final version file. The hard link is the no-overwrite commit point. A competing final artifact is never replaced; a valid committed file yields a CAS conflict and an unsafe artifact fails closed. Any failure after the link yields commit-unknown and is reconciled by exact disk refresh. Parent-directory sync confirms the final link, then owned pending cleanup and a second parent sync complete housekeeping.

Initialization and refresh are non-destructive. Committed evidence is never overwritten, removed, renamed, or quarantined. Recognized safe pending directories are bounded, retained, and ignored as uncommitted crash evidence. A post-link crash may leave the pending record and final file as exactly two links to one inode; recovery accepts that final only while exactly one recognized pending record has the same device and inode. Unrelated hard links, unknown or unsafe artifacts, malformed envelopes, history gaps or forks, identity drift, illegal adjacency, and limit violations fail closed and latch the adapter unhealthy. Every public operation refreshes bounded disk state so independent instances observe committed versions. The default native durability implementation is Linux-only; Windows exercises the platform-neutral logic through an injected durability port and fails closed if native durability is requested.

Phase 3C remains an inert source island. It adds no checkpoint, source, atomic-write, validation, reload, verification, approval producer, CLI, MCP tool, registry entry, configuration flag, package change, mapping change, runtime import, or write activation.

## Phase 3D Durable Immutable Checkpoints

Phase 3D adds an unregistered immutable implementation of `Phase3CheckpointPort`. Each checkpoint is one private regular final artifact named by a canonical lowercase checkpoint UUID. The stored canonical JSON envelope binds schema version, a lowercase nonce, checkpoint UUID, canonical Phase 3 relative path, `expectedSha256`, `sourceSha256`, `contentSha256`, canonical standard base64 content, and `storageSha256`. The checkpoint UUID is derived from the nonce plus immutable envelope identity, and `storageSha256` covers the complete canonical core, so ordinary tamper, re-signed tamper under the original filename, filename/envelope UUID mismatch, noncanonical JSON, noncanonical base64, path drift, digest drift, and oversize records fail closed.

`create` validates canonical path, lowercase SHA-256, decoded content at or below `PHASE2_MAX_TEXT_BYTES` with empty content allowed, and `sha256(content) == expectedSha256` before creating any pending entry. It preflights checkpoint count, pending count, record byte size, and projected aggregate record bytes before append. A record is prepared in a unique same-parent private pending directory, fully written with short-write and EINTR handling, file-synced, closed, pending-directory synced, and then re-opened no-follow to verify descriptor identity and exact bytes. The atomic hard-link to the final lowercase UUID filename is the create-only commit point; no final artifact is ever overwritten. Parent sync, owned pending cleanup, and a second parent sync complete housekeeping.

`load(checkpointId)` requires an exact lowercase UUID and validates only the checkpoint artifact's internal envelope binding: bounded no-follow descriptor stat/read/stat/lstat, strict schema, canonical JSON, canonical base64, storage digest, content digest, source/expected/content digest equality, and filename/envelope UUID equality. It returns fresh caller-owned bytes and zeros temporary read buffers. It does not claim restore-time path enforcement; coordinator rollback remains bound by the journal's durable `checkpointId` plus `checkpointSha256`, and the coordinator recomputes checkpoint byte SHA-256 before restore.

Initialization and every operation refresh bounded disk state. Unknown names, unsafe traversal/root/file topology, symlinks, nonregular files, unrelated hard links, malformed/tampered/noncanonical/oversize artifacts, checkpoint count, pending count, aggregate byte, and scan-entry violations fail closed and latch the store unhealthy. Recognized empty, active, or orphan pending directories are retained and ignored as uncommitted crash evidence. A post-link `nlink=2` final is accepted only when exactly one recognized pending record shares the same device and inode. Precommit cancellation and deadlines are checked before staging, during writes, after sync/close, and after pending sync before link. After link, caller cancellation is ignored so durability confirmation and cleanup can finish. Handled precommit failures remove only owned pending evidence and sync the parent; simulated precommit crashes retain pending evidence. Any failure after link returns `checkpoint_commit_unknown` and retains evidence.

The default native durability path explicitly requires Linux. Windows tests exercise the platform-neutral logic through injected logical durability, and native Windows initialization fails closed. Phase 3D remains an inert source island: it adds no source, atomic-write, validation, reload, verification, approval producer, CLI, MCP tool, registry entry, configuration flag, package change, mapping change, runtime import, or write activation.

## Phase 3E Protected Source Adapter

Phase 3E adds an unregistered read-only implementation of `Phase3SourcePort`. `ProtectedPhase3SourceAdapter` injects a `RepositoryCatalogProvider` and a narrow protected-source boundary interface satisfied by `ProtectedIdentityRegistry`: `assertFresh`, `isProtected`, and `readContent`. It does not register a tool, add runtime wiring, add configuration, or enable writes.

`read(path, context)` validates `canonicalPhase3Path` before any freshness, catalog, or read effect. It derives exactly one Phase 2 operation context for the call, with fresh random UUID `requestId` and `operationId` plus the caller's exact `signal` and `deadlineAt`, and passes that same object through protected freshness, catalog, content read, and final freshness checks. The sequence is: protected freshness; repository catalog; exact catalog entry lookup; absent entry returns `resource_not_found`; preknown protected path or inode returns `protected_resource`; protected content read; root identity, file identity, exact byte length, and `PHASE2_MAX_TEXT_BYTES` validation; second protected freshness; exact lowercase SHA-256; fresh caller-owned bytes. Empty bytes are allowed, and source bytes are never decoded as UTF-8 or parsed as YAML.

`readDigest(path)` validates the same canonical path and then creates one internal Phase 2 context with fresh random UUIDs, a fresh non-aborted signal, and `deadlineAt = Date.now() + 60_000`. It follows the same catalog, protection, read, validation, digest, zeroization, and final freshness sequence. It returns `null` only when the exact catalog entry is absent.

Boundary-owned byte buffers are zeroed in `finally` on success, catalog/read race failures, freshness failures, validation failures, cancellation, deadlines, and unknown exceptions. Caller-owned bytes are created only after all checks pass, so caller zeroization cannot affect later reads. Existing `RepositoryBoundaryError` codes are preserved with stable source-safe messages, except an unexpected `resource_not_found` or `protected_resource` from `readContent` after a catalog/precheck acceptance is classified as generic `service_unhealthy`. Unknown exceptions are classified as generic `service_unhealthy`. Errors do not disclose raw content or the requested target path.

Phase 3E remains an inert source island: it adds no atomic-apply adapter, validation adapter, reload adapter, verification adapter, approval producer, CLI command, MCP tool, registry entry, configuration flag, package change, mapping change, runtime import, write activation, or live Home Assistant mutation. Atomic apply is deferred to Phase 3F; validation, reload, verification, approval production, activation, and live write enablement remain later-adapter work.

## Phase 3F Inert Native Atomic Apply

Phase 3F adds an unregistered `NativePhase3AtomicApply` implementation of `Phase3AtomicApplyPort` plus an inert Linux helper source `openat2-replace.c`. It does not change `phase3Contract`, does not register tools, does not add CLI/config/package/Dockerfile/runtime wiring, and does not enable live writes. Package inclusion, libcrypto provisioning, aarch64 native proof, RW `/homeassistant` mapping, activation, and live Home Assistant mutation are explicitly deferred and unverified.

The TypeScript adapter validates canonical Phase 3 relative paths, lowercase SHA-256 digests, content size at or below `PHASE2_MAX_TEXT_BYTES`, `sha256(content) == contentSha256`, active cancellation/deadline context, Linux platform, and absolute normalized root/helper paths before a helper can run. It owns a candidate byte copy for stdin, preserves caller bytes, and zeros the owned copy on success, failure, cancellation, timeout, and protocol errors. It bounds concurrent helper execution and waiter count before spawning, uses a fixed no-shell helper invocation with `cwd=/`, empty environment, and literal arguments `{root, path, expectedSha256, contentSha256, byteLength}`, sends only the owned candidate copy on stdin, bounds stdout to the exact protocol frame, and bounds/discards stderr.

The helper protocol is exactly one line: `phase3-atomic-apply-v1 status=<before_commit|committed|commit_unknown>[ error=<closed_enum>]`. Missing, malformed, trailing, truncated, or unknown-error output fails closed. Spawn failure, pre-spawn cancellation, and pre-spawn deadline are `before_commit`. After helper start, unexpected exit, signal, stdin/EPIPE, timeout, forced kill, or malformed output are `commit_unknown` unless an exact `before_commit` frame proves no commit. The adapter performs no retry. In-flight caller cancellation terminates the helper process group with a finite grace before kill; helper cancellation before exchange reports `before_commit`, while cancellation after exchange is expected to be ignored by the helper so verification and cleanup can complete.

The C helper is Linux-only and source-only in this slice. It declares the OpenSSL 3 `SHA256` symbol and assumes an explicit external `libcrypto.so.3` link for native tests; no custom cryptography is introduced. It accepts strict fixed arguments and exact candidate stdin, rejects truncation/overflow, opens the root as `O_PATH|O_DIRECTORY|O_NOFOLLOW`, opens parent and target with `openat2` `RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV`, requires a stable regular single-link target without special mode bits or xattrs, checks bounded target size and expected digest, and preserves uid/gid/permission mode.

The helper stages only with `O_TMPFILE`; fallback named creation is prohibited. It fully writes and fsyncs the candidate, links the anonymous file to a high-entropy private same-parent pending name immediately before commit, reopens it no-follow, verifies staged identity/digest, and revalidates root/parent/path/target identity, metadata, and expected digest immediately before the exchange. The sole live commit point is `renameat2(RENAME_EXCHANGE)`. This is atomic replacement, not compare-and-swap. The helper never exchanges back and never performs a second replacement. After exchange, the displaced original remains under the pending name until post-commit verification confirms the live target is the staged candidate and the displaced file is the immediate-prechecked target with the expected digest and metadata; only then it unlinks displaced evidence and performs a second parent fsync before reporting `committed`.

Any uncertainty at or after exchange reports `commit_unknown` and preserves available evidence; displaced evidence is not unlinked on unknown. Handled cancellation before exchange unlinks only the owned pending name when it exists, fsyncs the parent, and reports `before_commit`. `SIGKILL` may leave a recognized pending artifact; the helper performs no autonomous pending cleanup, and future matching artifacts fail closed for manual cleanup. Test-only failure hooks are compile-time gated and cover pre-exchange, exchange, and post-exchange classification without adding runtime wiring.

## Phase 3G Strict YAML Validation Adapter

Phase 3G adds an unregistered `StrictYamlPhase3Validation` implementation of `Phase3ValidationPort`. It is an inert source-only validation adapter: it is not imported by runtime composition, not registered as a tool, not configured, not packaged differently, not mapped into the add-on, and does not enable writes or live Home Assistant mutation.

The adapter accepts only the closed runtime phases `candidate_pre_apply`, `candidate_post_apply`, and `checkpoint_post_rollback`. Invalid runtime phases fail before boundary invocation with `invalid_phase`. Pre-cancelled or expired contexts fail before boundary invocation with the corresponding strict YAML gate code. Each accepted call copies caller bytes into an owned buffer, derives exactly one frozen Phase 2 operation context with fresh random UUID `requestId` and `operationId` plus the caller's exact `signal` and `deadlineAt`, invokes a narrow injected boundary that defaults to `validateStrictYaml`, and zeros the owned input in `finally`. Caller bytes are never modified.

Known `YamlGateError` failures are converted to `Phase3ValidationError` with the original gate-derived code, validation phase, sanitized finite positive line and column, and a stable bounded allowlisted message. `Phase3ValidationErrorCode` is derived from `YamlGateErrorCode` plus `invalid_phase`; the gate code list is not duplicated. Runtime bad byte values become `unsupported_encoding`. Unknown exceptions become `internal_failure` at line 1 column 1 and do not disclose raw content, paths, canaries, causes, or third-party messages.

Phase 3G remains an inert source island. HA API validation, deployment-aware validation, reload adapters, verification adapters, approval production, activation, registration, package/container changes, mapping changes, and live Home Assistant mutation remain deferred and unverified.

## Phase 3H Narrow Domain Reload Adapter

Phase 3H adds the unregistered `NarrowPhase3ReloadAdapter` implementation of `Phase3ReloadPort` and keeps it target-exact. It does not infer domains from repository paths. The caller supplies the durable `{ path, target }` reload request; an injected catalog re-resolves the canonical Phase 3 path and exact operation context and must return the same frozen closed target, or `unavailable`, `ambiguous`, or `unhealthy`. Unavailable, ambiguous, unhealthy, malformed, mismatch, and not-dispatched outcomes are known no-dispatch before rollback reload is required; post-service throws and malformed dispatch results are outcome-unknown.

The injected service boundary accepts only the closed target union and returns an exact frozen outcome of `completed`, `not_dispatched`, or `outcome_unknown`. The adapter imports no generic Home Assistant REST or WebSocket request client, performs no retry or fallback, and exposes no broad reload or restart operation. It validates the path and active context before catalog resolution and rechecks cancellation and deadline immediately before dispatch. Once dispatch starts, the boundary owns the exact context and outcome classification; thrown or malformed dispatch results are conservatively `outcome_unknown`.

`Phase3ReloadError` extends `Phase3CoordinatorError` so durable coordinator failure evidence preserves its closed code. Its public evidence is limited to closed stage, resolution, dispatch, and optional allowlisted target classifications with a fixed message. Requested paths, upstream messages, causes, tokens, and response content are not retained or disclosed.

Phase 3H remains an inert source island. It does not change `phase3Contract`, runtime composition, configuration, registry, package/container content, add-on mappings, write enablement, or live Home Assistant access.

## Phase 3I Durable Reload Recovery Foundation

Phase 3I extends the inert transaction and recovery foundation with an exact durable reload target, candidate `reload_intent -> reload_succeeded` ordering, monotonic `rollbackReloadRequired`, and rollback `rollback_reload_intent -> rollback_reload_succeeded` ordering. The coordinator verifies the candidate only from `reload_succeeded`; it verifies a checkpoint only from `rollback_validation_succeeded` when no rollback reload is required or from `rollback_reload_succeeded` after the exact rollback reload.

Startup recovery never repeats an ambiguous candidate reload. Candidate `reload_intent` and `reload_succeeded` route through rollback with reload required when the target is non-null. Incomplete `rollback_reload_intent` requires manual recovery; `rollback_reload_succeeded` may continue only with the checkpoint digest live. The journal schema, legal adjacency, record-level reload context, recovery table, and root/add-on mirrors are exact and recursively frozen where exported.

Phase 3I remains inert. It adds no adapter activation, runtime composition, configuration, registry, package/container change, mapping change, write enablement, live reload wiring, or live Home Assistant access.

## Phase 3J Narrow Post-Effect Verification Adapter

Phase 3J adds an unregistered `NarrowPhase3VerificationAdapter` implementation of `Phase3VerificationPort`. It accepts candidate verification only from exact `reload_succeeded` records with `rollbackReloadRequired = false` and the durable no-target or target predecessor binding. It accepts checkpoint verification only from exact `rollback_validation_succeeded` records with no rollback reload required or exact `rollback_reload_succeeded` records with a non-null target and rollback reload required. Records are first parsed through the existing strict transaction assertion; Phase 3J changes no coordinator, journal, schema, transition, or observation class.

The adapter injects one exact post-effect source digest port and one narrow trusted verification probe port. For the outcome-selected candidate or checkpoint digest, the order is exactly source digest, probe, source digest with the same operation-context object, two consistency reads, and no retry. Both source reads must return the exact expected lowercase SHA-256. The frozen probe request binds `transactionId`, `path`, `outcome`, `expectedSha256`, `impact`, `reloadTarget`, and `rollbackReloadRequired`.

Probe evidence must be an exact frozen object containing only own immutable enumerable data properties `{ status, transactionId, outcome, expectedSha256 }`. The adapter snapshots descriptors once without property access and rejects accessors, symbols, excess keys, unfrozen values, hostile proxies, malformed evidence, and stale bindings. Closed statuses are `verified`, `rejected`, `unavailable`, and `unhealthy`. Cancellation and deadline are checked before effects and after every fulfilled or rejected boundary call before classifying its return or failure. Stable `Phase3VerificationError` evidence contains only closed code and stage; it retains no cause, requested path, foreign object, upstream message/content, or unexpected observed digest.

Freezing and exact binding establish shape, immutability, and staleness checks; they do not establish authenticity. The injected verification probe is trusted semantically to report the loaded Home Assistant state represented by its bound evidence. Activation remains blocked until a production Home Assistant adapter proves loaded-state observation and reload causality for each supported target. Phase 3J remains an inert exact root/add-on source island with no generic HA REST/WebSocket client, runtime import, configuration, registry, package/container change, mapping change, write enablement, live access, mutation, commit, push, deploy, or release.

## Phase 3K Contextual Protected Post-Effect Source Digest Bridge

Phase 3K makes the inert `ProtectedPhase3SourceAdapter` structurally satisfy both `Phase3SourcePort` and `Phase3PostEffectSourceDigestPort`. The legacy `readDigest(path)` form retains its fresh internal non-aborted signal and 60 second deadline. The contextual `readDigest(path, context)` form validates the canonical path before effects or supplied-context access, then derives exactly one frozen `Phase2OperationContext` with fresh distinct UUIDs and the exact captured caller signal and finite deadline.

Runtime context values are treated as unknown for shape and robustness checks. Null and non-objects, signal values that are not intrinsic-brand-compatible, signal values with any own `aborted` data or accessor descriptor, and non-finite or non-number deadlines fail as sanitized `service_unhealthy`. Signal and deadline properties are snapshotted at most once. Signal validation uses `Reflect.getOwnPropertyDescriptor` plus the intrinsic `AbortSignal.prototype` `aborted` getter without an ordinary untrusted property read; descriptor/proxy traps are caught, and no failure triggers retry or internal-context fallback. Rejecting an own `aborted` descriptor prevents downstream freshness checks from dispatching to a shadow getter.

These JavaScript checks do not authenticate signal provenance. A complete forgery assembled from `AbortSignal.prototype` and copied internal-looking symbols, without an own `aborted` shadow, may be indistinguishable from a coordinator-created signal. `Phase3OperationContext` is therefore an internal trusted coordinator contract: future activation and runtime composition must supply it through a trusted context factory or equivalent provenance boundary. Phase 3K establishes only an intrinsic-brand-compatible, unshadowed signal shape before effects.

Both digest forms use the existing protected source path unchanged: first freshness, exact catalog lookup, protected path/inode precheck, accepted secure read, root/file identity and size validation, lowercase SHA-256, final freshness, and boundary-byte zeroization. Catalog absence alone returns `null`; accepted-read missing/protected races and unknown failures remain fail-closed. Digest calls return no source bytes, perform no retry or cache, and pass the same derived context object to both freshness checks, catalog, and content read.

This bridge proves protected disk/source consistency only within the injected repository boundaries and trusted operation-context contract. It does not prove operation-context or signal authenticity, Home Assistant loaded-state authenticity, or reload causality. Phase 3K remains exact mirrored source/tests/docs only and is absent from runtime composition.

Phase 3K explicitly supersedes Phase 3J's stale deferral of `production probe/source adapters` only to the extent that the source adapter is now structurally satisfied. Trusted runtime context provenance, runtime composition and package reachability, native-live filesystem proof, the production verification probe, Home Assistant loaded-state observation, reload-causality proof, service transport, approval production, registration, activation, and every live mutation remain `BLOCKED` or `UNVERIFIED`. No operational or production-readiness claim is made.

## Phase 3L inert authenticated durable single-use approval source contract

Phase 3L adds `DurablePhase3ApprovalGrants`, an inert source implementation of `Phase3ApprovalPort` with `initialize()`, `issueApplyGrant()`, `consumeApplyGrant()`, and `close()`. The mirrored modules are included by the repository's existing TypeScript build and add-on context copy/package flow, without a package manifest, container, or mapping change. They are not imported by runtime composition, configured, registered, activated, or exposed through a CLI or MCP tool. `phase3Contract.grantProducer` remains `"absent"`: package presence is not runtime composition, and no runtime producer exists or is reachable. This slice performs no Home Assistant access or mutation.

The constructor accepts an already-existing absolute normalized root and an exactly 32-byte key, copies that key, and never creates the root or parents. Native durability is Linux-only. Windows is permitted only with injected logical filesystem/durability boundaries. Native Linux separate-process and power-loss durability is `UNVERIFIED` and activation-blocking.

The fixed root topology is `header.json`, `slot-000` through `slot-255`, `.header-stage-0` through `.header-stage-3`, and `.grant-stage-00` through `.grant-stage-31`, with at most 293 entries. A slot is a private nonempty directory containing mandatory `grant.json`, optional `used.json`, and `.used-stage-0` through `.used-stage-3`, with at most six entries. Frozen exported limits are 256 slots, four header stages, 32 grant stages, four receipt stages, 1024 header bytes, 4096 grant bytes, 1024 receipt bytes, and a fixed 120000 ms TTL. Exact maxima remain healthy. An operation needing an unavailable fixed stage or final slot returns non-latching `approval_capacity_exhausted`.

Expected partial precommit stages may be empty or bounded partial artifacts of the exact fixed type, ownership, mode, link count, and name. They are counted and ignored semantically, not authenticated as finals, and do not make the store unhealthy merely because bytes are partial. Stages are not automatically deleted. A process may remove only its own precommit stage after stable inode/content proof. Header/receipt cleanup is best effort after durability. Stale receipt stages can exhaust all four claims despite logical receipt reservation; retention and cleanup design is activation-blocking. ENOSPC and I/O can still block.

Every present final artifact is strict canonical JSON authenticated by HMAC-SHA256 lowercase 64-character hex. Input is the exact ASCII domain including trailing NUL immediately followed by canonical core JSON UTF-8. Domains are `HA_PHASE3_APPROVAL_KEY_ID_V1\0`, `HA_PHASE3_APPROVAL_HEADER_V1\0`, `HA_PHASE3_APPROVAL_GRANT_V1\0`, and `HA_PHASE3_APPROVAL_RECEIPT_V1\0`. The key-identity core is exactly `{schemaVersion:1,purpose:"phase3_approval_key_identity"}`. The header core is exactly `{schemaVersion:1,storeId,keyId}` and its envelope adds only `headerHmac`. The grant core is exactly `{schemaVersion:1,grant}` where `grant` is the strict `Phase3ApprovalGrant`, and its envelope adds only `grantHmac`. The receipt core is exactly `{schemaVersion:1,grantId,grantHmac,consumedAt}` and its envelope adds only `receiptHmac`. Fatal UTF-8, strict schemas, canonical byte equality, distinct domains, timing-safe comparison, and filename/envelope/receipt binding make modification, insertion, substitution, wrong key, noncanonical bytes, duplicate IDs, and receipt mismatch fail closed.

Header initialization exclusively claims a fixed stage, writes/syncs/stably rereads it, hard-links `header.json`, syncs the root, and authenticates the winner. Concurrent same-key initialization accepts only the durable authenticated winner; a different key fails. Header-stage exhaustion is non-latching capacity failure.

Issuance accepts only a strict pending unexpired proposal and active context at start and immediately precommit. Proposal schema/property access is a foreign boundary: proxy, getter, accessor, or caller-thrown failures become the fixed contract-owned `proposal_not_pending` error without retaining the foreign value or latching the store before a possible commit. Successful parsing produces a frozen schema copy used for every later issue or consume access, so caller mutation or later throwing access cannot cross asynchronous boundaries. Caller and trusted-clock values are finite safe integers; signals use the intrinsic getter without a JavaScript provenance claim. The grant binds exact proposal/storage/candidate/diff/operation/risk/impact/reload-target values. Expiry is `min(issuedAt + 120000, proposal.expiresAt)` in strict ISO milliseconds; callers select neither binding nor expiry. A complete authenticated stage is synced/reread, then authenticated durable state is refreshed before rename. An already-committed grant ID causes stable cleanup of only the process-owned stage and UUID regeneration without creating another slot. Reconciliation distinguishes retained-source contention, own inode/content commit, UUID collision, and indeterminate/conflicting outcomes, with four total UUID attempts. Once rename may cross, cancellation is ignored through reconciliation/durability and postcommit cancellation returns success.

Consumption checks cancellation before refresh, then lookup, and returns authenticated replay before proposal/time validation. Shared ordering remains cancellation; caller-owned lookup/replay; pending proposal; proposal expiry; grant not-yet-valid; grant expiry; exact binding; final cancellation. A fresh trusted clock and the same signal are revalidated immediately pre-link. `consumedAt` is at or after issuance and strictly before grant/proposal expiry; rollback is not-yet-valid and expiry boundaries are closed. Receipt hard-link reconciliation requires pending/final inode plus exact authenticated content. A loser syncs/authenticates the winner before replay. Absent final with retained pending is precommit failure; exact owned final is durable; conflict/indeterminacy is latched `approval_commit_unknown`. Once link may cross, cancellation is ignored.

Header link, grant rename, and receipt link track `not_committed`, `possibly_committed`, and `durably_committed`; possible begins immediately before the syscall and containing-directory sync establishes durability. Issue and consume revalidate the trusted clock and captured signal after the asynchronous precommit hook, with no await between that validation, entering `possibly_committed`, and invoking rename/link. After possible commit, every failed observation or reconciliation is latched `approval_commit_unknown` unless stable evidence proves the operation did not commit or containing-directory sync plus authenticated exact evidence proves durability. Contention is accepted only after directory sync and authenticated winner validation. Committed-but-threw results reconcile from inode/content. Bounded whole-scan and slot-scan retries tolerate concurrent expected stage cleanup, stage creation, hard-link progression, and rename progression only when a subsequent complete scan is stable; persistent or malformed state remains fail-closed. Filesystem and durability calls cross a private sanitizing wrapper that retains only internal allowlisted errno classification, while hook failures are separately sanitized; boundary-thrown `Phase3ApprovalError` objects cannot select public codes or disclose messages, paths, or causes. Before-commit boundary exceptions are converted to sanitized approval errors and latched when they establish store unhealthiness. After parent sync, hook, cleanup, or housekeeping failure is never commit-unknown and the durable call remains successful; housekeeping sync failure may latch future operations.

`initialize()` installs every successful scan through one ownership handoff that first zeros all previously retained authenticated grant buffers; this applies to repeated and overlapping initialization. Unsuccessful scans zero their temporary buffers, and only the final installed scan remains retained. `close()` gates new operations, waits all in-flight reconciliation, shares one promise, then zeros the final retained buffers, owned JavaScript key, and temporary record/MAC buffers and clears state. Stable-read buffers are transferred only on successful ownership handoff; close failures and every unsuccessful slot-scan exit zero them first. It does not claim caller-key, JavaScript heap, OpenSSL, filesystem, or cache zeroization.

Threat boundary: HMAC authenticates present artifacts. The private root is assumed honest, non-rollback, and protected from unauthorized deletion/restoration. Deleting `used.json` or restoring a pre-consumption root can re-enable a grant; deletion and rollback detection are unsupported and activation-blocking. Root rollback/deletion protection, native Linux process/power-loss validation, production key provisioning/rotation/zeroization wiring, retention/stale-stage remediation, operator CLI authorization/audit, runtime composition, registration, activation, and all live mutation remain blocked. Existing build/copy/package presence does not remove any activation blocker.

## Phase 3M inert native durable approval evidence contract

Phase 3M adds a repository-owned Linux evidence harness for the inert Phase 3L `DurablePhase3ApprovalGrants` source. The worker imports only the built `dist/phase3/durableApproval.js` module and constructs it with the default filesystem and durability adapters. Its closed IPC protocol is `ready`, then strictly increasing non-overlapping `initialize`/`issue`/`consume`/`close` requests, sanitized relative lifecycle-hook evidence, and one exact result. The harness does not reproduce the approval HMAC domains, envelope schemas, or authentication algorithm; fresh worker initialization, consumption, and replay provide authentication evidence through the implementation under test.

The evidence registry is frozen at exactly 56 ordered IDs: three environment prerequisites; three initialization races/restarts; one cross-process issue/consume/replay flow; two issue/consume races; three artifact-tamper rows; three same-instance exhaustion/removal/retry rows; eight named root/header/scan topology rows; nine precommit/postcommit/parent-synced process-kill rows; twenty-two scoped syscall-fault rows; and direct `link`/`linkat` shim probes. Output is bounded JSONL with one exact manifest first, every registered row exactly once in order, and one internally consistent summary last. Unknown, missing, duplicate, reordered, oversized, or malformed records fail parsing. A `FAILED`, `BLOCKED`, or `UNVERIFIED` row makes the harness exit nonzero.

Every worker is detached into a process group with finite stdout, stderr, IPC-message, IPC-byte, and completion bounds. Error, close, IPC, and stdio handling is registered before the controller inspects the child PID; a spawn with no PID therefore enters the same tracked, bounded ready/closed lifecycle instead of escaping as an unhandled event. Timeout, overflow, malformed IPC, requested crash, or controller failure kills the group, awaits child close, and polls until the group no longer exists. A client remains tracked until group absence is proven; close, kill, lifecycle, and absence-proof failures are retained and surfaced, and row cleanup failure becomes explicit bounded evidence without truncating the ordered JSONL contract. The detached process-group prerequisite probe routes every created child, including error-before-close and missing-PID outcomes, through one bounded cleanup section that always observes close. A valid PID whose absence is unproven is terminated; absence is proven only by `ESRCH`; every probe, lifecycle, termination, close, and absence failure is aggregated. Preflight and non-passing rows retain only capped harness-owned diagnostics, with raw errors, absolute paths, environment values, and unbounded content excluded. Completion requires native Linux, UID 0, a working C compiler, a non-tmpfs native evidence root, and detached process-group support. The C fault shim remains shared with the existing persistence harness; Phase 3M adds scoped `link` and `linkat` interception plus an `open-family` selector and compiles a bounded embedded direct-link probe with warnings as errors. Existing exact syscall selectors and `validate:linux:persistence` remain compatible.

Crash evidence is process-death evidence, not power-cut evidence. A precommit kill must leave the final absent and a fresh process must retry successfully. A postcommit or parent-synced kill must leave a final that a fresh process authenticates; committed receipt cases must report replay. Scoped short positioned writes must be retried to successful fresh authentication. A containing-directory fsync fault after the final syscall must report `approval_commit_unknown`; other named pre-final syscall failures fail closed. Same-key initialization converges. A different-key race has exactly one success and one `approval_commit_unknown`, after which a fresh losing key is unhealthy and the winning key is healthy. An established wrong key is unhealthy while a fresh correct key remains healthy. Colliding issue UUIDs produce two unique durable grants, and consume contention produces one success and one replay.

The topology claim is intentionally limited to the named private-root, root-owner/mode, header symlink/hardlink/nonregular, unknown-root-entry, root-scan-overflow, and slot-scan-overflow rows. Grant/receipt native topology variants not named by the registry and actual power-cut behavior remain `UNVERIFIED` and activation-blocking. The test fake accepts prebuilt evidence only when both `NODE_ENV=test` and the explicit `--allow-test-fake` option are present; it never substitutes for native completion evidence.

Phase 3M remains inert. It adds no runtime import, composition, approval producer, key provisioning, operator command, CLI/MCP exposure, configuration, registry, container content, add-on mapping, Home Assistant access, mutation, deployment, or release. `phase3Contract.grantProducer` remains `"absent"`. External UID 0 Linux runs of `pnpm verify`, `pnpm validate:linux:approval`, and unchanged `pnpm validate:linux:persistence` are required before native evidence can be classified as passed; absent those runs, native completion is `BLOCKED`.

## Phase 3N inert exclusive-custody stale-stage remediation contract

Phase 3N adds only the source method `DurablePhase3ApprovalGrants.remediateStaleStages()` and its injected `acquireExclusiveRootCustody(root)` option. It does not add the method to `Phase3ApprovalPort`, invoke remediation automatically, provide a default or production custody provider, or change runtime composition, CLI/MCP exposure, configuration, package/container/mount content, add-on mappings, Home Assistant access, or mutation. The root and add-on modules remain exact byte mirrors and `phase3Contract.grantProducer` remains `"absent"`.

Remediation reserves the instance synchronously before any asynchronous acquisition. It is available only while the instance is open, healthy, never successfully initialized, and has no operation in flight or prior remediation reservation. A fixed-capacity initialization failure remains healthy and permits remediation; any unhealthy initialization failure blocks it. While reserved, initialize, issue, consume, and another remediation call reject without latching. Existing overlapping initialize behavior remains unchanged when no remediation is reserved. The reservation counts as in flight from before acquisition until cleanup and release settle, so `close()` gates new work and waits through both acquisition and release.

Acquisition is the first filesystem-affecting action. A missing, rejected, or malformed acquisition performs no scan or deletion, is sanitized to the existing `approval_store_unhealthy` code, and latches the instance. The trusted lease is exactly a frozen ordinary object with one own enumerable, immutable data property named `release`, no symbols or excess properties, and a non-proxy function value; both lease and release proxies are rejected with `node:util` `types.isProxy`. The release function is copied once after complete lease validation, called exactly once in `finally` with the lease as receiver, awaited, and required to fulfill with `undefined`. A malformed lease is not released by the consumer: the provider retains responsibility for every resource associated with an untrusted result. There is no callback-started, adoption, or cleanup-registration concept at this boundary.

After valid acquisition, remediation performs one strict no-retry scan of the exact fixed topology and never installs scanned header, grant, or slot state into the instance. It captures root, slot, final, stage-directory, and stage-child identities and exact expected entries. Existing finals are stably read, canonically parsed, and authenticated with the injected key. Headerless remediation is accepted for cleanup only when the root contains recognized header/grant stages and no `header.json` or slot; it never returns remediation success. Strict detection of an empty headerless private root first attempts to sync the root, then rejects and latches whether that reconciliation sync succeeds or fails. After a successful sync-and-reject, a fresh instance may initialize the empty root normally. Unknown names, out-of-range names, unsafe node types/modes/owners/links, complete but invalid or wrongly keyed stage envelopes, tampered finals, duplicate final grant IDs, unsafe alias topology, excess children, or any scan instability abort before deletion. Empty and incomplete stage writes remain recognized stale precommit artifacts. Every scan-owned buffer is zeroed on success and failure, and remediation never creates, rewrites, or deletes `header.json`, a slot, `grant.json`, or `used.json`.

Candidate order is total and fixed: numeric header stages, numeric grant stages, then numeric slots and numeric receipt stages. Before each candidate, the immediate parent must retain the captured device, inode, node type, mode, UID, and GID and exactly the expected remaining entries. Expected parent metadata and topology are refreshed after each owned mutation. File stages are full-identity/content revalidated, removed with `force:false` and no recursive option, proven absent, and followed immediately by containing-directory sync. `ENOENT` is accepted only as explicit post-operation reconciliation evidence; it is never silently accepted before an attempted effect.

A grant stage must be an exact empty directory or contain exactly one `grant.json`. Its child, when present, is non-forcibly removed, proven absent, and followed by stage-directory sync. A no-follow read-only handle pins the scanned stage directory across child cleanup, continuity proof, narrow `rmdir`, absence proof, and root sync so immediate inode reuse cannot disguise substitution. Recursive cleanup is never used. Header, grant, and receipt counts advance only after absence and successful immediate-parent sync.

When a removal throws under exclusive custody, observed absence means the effect occurred and must still be synced before counting or continuing. Exact unchanged identity/content/topology is a before-effect failure. Substitution, extra topology, or an observation that cannot be classified is unhealthy. A sync failure leaves durability unknown. Before an authenticated established store can return success, including zero-candidate success, remediation reconciliation-syncs the root and then every existing slot parent in numeric order. It then performs the final strict authenticated scan and identity proof. This fresh reconciliation closes uncertainty left by a prior instance whose stage deletion is already absent; any reconciliation sync failure rejects and latches. Any scan, effect, reconciliation, final-scan, or release failure after trusted acquisition latches the instance and may leave partial durable progress; retry requires a fresh instance and is idempotent against the observed filesystem state. Process death or power loss before the relevant containing-directory sync remains unverified and cannot be reclassified by this JavaScript contract.

A two-link header or receipt stage is accepted only when the scan proves it is the single alias of the authenticated corresponding final. After unlink and parent sync, the final is reread and reauthenticated and must retain device, inode, node type, mode, UID, GID, size, and exact bytes while changing from link count two to one; only resulting timestamp changes are tolerated. Removing an unlinked stage must leave finals fully unchanged. Completion for an authenticated established store requires root/slot reconciliation syncs followed by a second strict full scan, no remaining stages, exact final bytes and identities subject only to the proved alias transition, and immutable root/slot identity. Success returns only a frozen exact `{headerStages,grantStages,receiptStages}` count object.

Phase 3N supersedes Phase 3L's stale-stage-remediation blocker only at the inert source/state-machine level. Native production remediation and custody evidence, a production custody provider, process-death behavior before sync, actual power-cut behavior, production key provisioning/rotation/zeroization, operator authorization/audit, root deletion and rollback protection, runtime composition, registration, activation, and every live mutation remain `BLOCKED` or `UNVERIFIED`. No production-readiness or power-loss claim is made.

## Phase 3O inert conditional Linux advisory-custody evidence contract

Phase 3O adds an inert, explicitly constructed Linux adapter implementing the existing `Phase3ApprovalCustodyProvider` and a native evidence foundation. It is never imported or composed except by tests and the native harness. It adds no package script, helper packaging, default provider, runtime/configuration/index/CLI/MCP/audit integration, container or mount change, Home Assistant access, or mutation. It is not production custody, does not supersede any Phase 3N production-custody blocker, and does not make remediation, approval production, registration, or activation reachable.

The claim is advisory exclusion only among cooperative participants that open the same continuously pathname-bound inode in the same mount namespace on a positively allowlisted local filesystem while the direct helper remains alive. The helper allowlist is tmpfs `0x01021994`, ext2/3/4 `0xEF53`, XFS `0x58465342`, Btrfs `0x9123683E`, and OverlayFS `0x794c7630`. Every family must be reported separately as `native_tested`, `permitted`, or `UNVERIFIED`; an allowlist entry is not native evidence. Current `initialize`, `issue`, and `consume` calls do not participate. Nonparticipants and a different mount namespace are outside the claim.

The C helper is Linux-only and remains one direct process: it performs no fork, exec, thread creation, or dynamic loading. It accepts exactly an absolute normalized root and canonical expected parent PID. It installs `PR_SET_PDEATHSIG` before lock acquisition, rechecks the expected parent PID, opens the root with `O_RDONLY|O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC`, then performs `fstat` and `fstatfs`. Filesystem rejection precedes owner/mode rejection, so `/proc` must return the exact unsupported-filesystem result. The opened inode must be a directory owned by the helper's effective UID with no group/other permission bits. The helper takes `flock(LOCK_EX)` and makes no stronger claim than the tested filesystem/mount behavior.

Stdout is ASCII and bounded to 512 bytes total; stderr is bounded to 1024 bytes and any stderr byte fails. Startup permits exactly one LF-terminated frame and no trailing bytes. No output is permitted after ready. Canonical unsigned integers are exactly `0|[1-9][0-9]{0,19}`, and `ctime_nsec` is at most `999999999`.

```text
phase3-approval-custody-v1	ready	dev=UINT	ino=UINT	mode=UINT	uid=UINT	gid=UINT	nlink=UINT	ctime_sec=UINT	ctime_nsec=UINT	fs_type=UINT
phase3-approval-custody-v1	failure	code=CODE
```

`CODE` is exactly `invalid_arguments`, `parent_invalid`, `parent_changed`, `root_open_failed`, `root_unsafe`, `filesystem_unsupported`, `lock_failed`, `ready_write_failed`, or `internal_error`. Startup emits a matching failure frame and exit except that exit 69 may follow an incomplete ready/failure write.

| Exit | Meaning                                   |
| ---: | ----------------------------------------- |
|    0 | exact control, clean unlock, clean close  |
|   64 | invalid arguments                         |
|   65 | parent invalid or changed                 |
|   66 | root open or safety failure               |
|   67 | unsupported filesystem                    |
|   68 | lock failure                              |
|   69 | ready/failure write failure               |
|   70 | zero-byte EOF abandonment                 |
|   71 | malformed, duplicate, or trailing control |
|   72 | unlock failure                            |
|   73 | close failure                             |
|   74 | internal failure                          |

Release control is exactly byte `0x52` followed by EOF. The adapter records control as sent only when `stdin.end(0x52)` is successfully invoked while the child is still running. Only that proved control send, no post-ready stdout or stderr, exit 0, and observed `close` allows `release()` to fulfill `undefined`; an exit-0-before-release/close race therefore fails closed as holder loss.

The TypeScript provider uses the actual `process.platform` whenever the real runner is selected; platform substitution is accepted only with an injected fake runner. Root/helper paths and finite positive operation/grace options are validated before spawn. The acquisition monotonic deadline starts at provider invocation, before preflight. Real metadata/filesystem probes use `node:fs/promises`; injected probes have the same asynchronous contract. Preflight is raced against that deadline, rejects with the fixed `timeout` classification without cleanup grace, and a late preflight result can never spawn. The helper is `lstat`-checked before spawn and after ready: no symlink, regular file, current owner, owner-executable, not group/other writable, and exactly one link. This is a race-detection check, not a race-safe authenticity pin. The root is no-follow checked before spawn and after ready for directory type, current owner, private mode, and exact device, inode, full mode/type, UID, GID, link count, size, ctime, and filesystem type. Those asynchronous post-ready probes share the original acquisition deadline, must exactly match the helper frame, and cannot resolve a lease after timeout or termination.

The adapter returns the existing exact frozen one-property lease. At most eight live records exist per provider and 32 across the module, counted before spawn and retained until child `close`. The direct child is neither detached nor treated as a process group. Error, exit, close, stdout, stderr, and stdin listeners are installed before reading the PID. Lifecycle is exactly `running`, `exited_waiting_close`, or `closed`; only `child.kill` is used and only while running. Observing an exit event or non-null child `exitCode`/`signalCode` disables all later signals. No raw `process.kill` or cached-PID signaling is used.

Acquisition and release each have a caller settlement ceiling of `operationTimeoutMs + terminationGraceMs` from their start; pre-spawn acquisition timeout settles at `operationTimeoutMs` because no child cleanup is required. Every operation-timeout, protocol, stderr, child, stdin, and missing-PID termination path first enters one `terminationStarted` latch. Termination entry is idempotent: at most one TERM attempt and one grace timer occur, followed by at most one KILL attempt, all only while running. Deferred ready validation requires that termination never started and that no timeout, protocol, stdin, spawn, or signal failure exists before it can validate or resolve a lease. Once termination has started, `release()` neither clears its grace timer nor sends control. Grace expiry with an acquired but nonclosed helper durably records `cleanup_unproved`, destroys and unreferences child handles exactly once, and rejects an already-requested release; a later first release rejects immediately from that retained error without adding a signal or timer. A close observed before release instead retains the causal close classification, and concurrent or repeated release calls always share one promise. A bounded rejection does not delete the record, which remains charged until `close`; only close releases capacity. After final settlement, all non-lifecycle event/data/error/stdin handling and termination entry points are inert; only exit/close state and close-time capacity release remain live. Failed acquisition follows the same bounded lifecycle.

Public TypeScript failures contain a closed code and fixed sanitized message only: `startup_failed` ("Approval custody helper failed during startup"), `protocol` ("Approval custody helper protocol failed"), `signal` ("Approval custody helper terminated by signal"), `timeout` ("Approval custody helper timed out"), `stdin` ("Approval custody helper control failed"), `holder_lost` ("Approval custody holder was lost"), and `cleanup_unproved` ("Approval custody helper cleanup was not proved"). Only `startup_failed` may additionally retain one closed helper `CODE`; no foreign message, path, cause, stderr, or canary is retained.

The frozen native registry is exactly N001–N025 in order: Linux, architecture, libc, compiler, UID 0, recorded supported filesystem, UID 0 acquisition, nonroot acquisition through UID drop, same-root contention, different-root independence, clean handoff, SIGKILL/reacquire, holder-parent death, waiter-parent death, pathname replacement while waiting, symlink/nondirectory/unsafe-mode/wrong-owner rejection, exact `/proc` unsupported frame/67, zero EOF, malformed/duplicate/trailing control, and Phase 3N remediation API mechanics. JSONL contains the manifest first, each row exactly once in order, and one consistent summary last. Unknown, missing, duplicate, reordered, or nonpassing mandatory rows make the harness exit nonzero. It records kernel, architecture, libc, UID, filesystem magic/mount, compiler, hardening flags, and helper hash. N003 collects every affirmative family from a nonempty Node glibc report, successful nonempty identifying `ldd` output, and inspected identifying loaded-library paths. It accepts only exactly one unique family and then reports evidence from one source; missing, nonzero, empty, ambiguous, mixed-source, or same-source musl/glibc contradiction fails instead of guessing.

The exact Phase 3O scope is the mirrored TypeScript adapter, mirrored C helper, focused U001–U033 tests, isolation update, native harness, this contract, and the requirements ledger: nine files total. Explicit blockers remain holder death in the middle of remediation; root rename, replacement, rollback, deletion, and transient replace/restore; mount-namespace identity; nonparticipants; helper authenticity and race-safe launch; descriptor-relative remediation operations; production packaging; every runtime caller; and production custody. Phase 3O native evidence does not turn advisory `flock` into mandatory exclusion and does not establish a stable pathname, rollback resistance, process/power-loss remediation safety, or production readiness.

## Phase 3P inert create-once approval-key contract

Phase 3P adds only the exact mirrored `approvalKey.ts` source island, its focused test, isolation updates, and these contract records. It exports inert `provisionPhase3ApprovalKey(options): Promise<void>` and existing-only `loadPhase3ApprovalKey(options): Promise<Phase3ApprovalKeyLease>`. Provision never returns the key. Load never creates a directory or key. Neither API performs load-or-create, replacement, unlink, retry except the bounded `EINTR` handling stated below, rotation, deletion, repair, recovery mutation, store creation, composition, registration, activation, CLI/MCP exposure, audit integration, package/container/configuration/mapping change, or Home Assistant access. The optional defaults are key-state paths `/data/phase3` and `/data/phase3/approval.key`; no approval-store root is exported.

Inputs must be absolute normalized paths, and the key must be a direct child of the configured state directory. The state directory must already exist as a real no-follow directory owned by the real UID with exact mode `0700`. The logical process boundary must report Linux, callable `getuid()` and `geteuid()`, and equal finite nonnegative real/effective UIDs before any parent or key filesystem action. The filesystem boundary must provide actual `O_DIRECTORY` and `O_NOFOLLOW` values. The native defaults use Node's Linux `crypto.randomBytes`, `lstat`, absolute-path `open`, positioned `read`/`write`, descriptor `stat`, descriptor `sync`, and `close`; injected process/filesystem/random boundaries permit deterministic platform-neutral tests but do not make native Windows supported.

Each operation first no-follow `lstat`s the state directory, opens it `O_RDONLY|O_DIRECTORY|O_NOFOLLOW`, compares descriptor `stat` to the path snapshot, and retains that descriptor. At each named checkpoint, descriptor and path are revalidated against the pinned device/inode and exact owner/mode/type. Provision performs checkpoints before randomness, immediately before exclusive create, after final key validation, and after the retained-directory `fsync` immediately before close. Load performs checkpoints before initial key access and after final key validation immediately before close. These checks claim static or persistent pathname substitution detection only under an honest private parent. They are not descriptor-relative key operations.

Provision accepts only one genuine exact 32-byte, not-all-zero `Buffer` scratch and always zeros that same scratch. A malformed non-Buffer ArrayBuffer view is rejected as invalid only after a prototype-independent best-effort byte wipe of its intrinsic backing region; malformed DataView, typed-array, detached, or otherwise unreadable view state never changes the fixed public error. It opens the final absolute key path directly with `O_CREAT|O_EXCL|O_WRONLY|O_NOFOLLOW` and mode `0600`; there is no temporary name or rename. `EEXIST`, including an existing symlink, is `approval_key_exists` without inspection or replacement. The full positioned write is exactly 32 bytes. Short positive writes continue; `EINTR` continues within the shared fixed 64-attempt ceiling; zero progress, overrun, or exhaustion fails. It then performs key `fsync`, descriptor metadata validation, key close, final path `lstat` identity/metadata validation, parent revalidation, retained-directory `fsync`, final parent revalidation, and directory close. The key must be a regular real-UID file with exact mode `0600`, link count one, size 32, and nonzero content. Only completion of this sequence returns success. Success reports completed syscall/validation/close sequencing, not proof against actual power loss.

The exclusive-open fulfillment is the create commit boundary. Before fulfillment, unsafe metadata is `approval_key_unsafe`, boundary failures are `approval_key_io_failure`, invalid input/platform/UID/flag/random contracts are `approval_key_invalid`, and `EEXIST` is `approval_key_exists`. From fulfillment through all primary and cleanup work, any failure is `approval_key_commit_unknown`; the artifact is never removed, replaced, repaired, or inspected by a provisioning retry. A retry therefore returns `approval_key_exists` if the artifact remains. No close is retried.

Load no-follow `lstat`s the key before opening it. Only `ENOENT` from this initial key `lstat` is `approval_key_missing`; later disappearance or no-follow drift is `approval_key_unsafe`. It opens the absolute path read-only with `O_NOFOLLOW`, compares descriptor metadata and identity to the path snapshot, performs a bounded positioned exact 32-byte read plus one-byte EOF proof, re-stats the descriptor, final-`lstat`s the path, revalidates the parent, and requires key then directory close success before creating a lease. Static wrong size, unexpected EOF, trailing data, or all-zero content is `approval_key_corrupt`; type/owner/mode/link or identity/metadata drift is `approval_key_unsafe`; other boundary and malformed I/O results are `approval_key_io_failure`. Failure zeros the candidate and returns no lease.

The load lease is exactly `Object.freeze({key, release})`. `key` is one stable mutable 32-byte `Buffer`. `release(): void` is synchronous, returns `undefined`, is safely repeatable, and zeros indices 0 through 31 of that same buffer after caller mutation without consulting its shadowable `byteLength`, `length`, `fill`, or prototype methods. No successful load transfers a second buffer. Cleanup follows deterministic first-primary precedence: an earlier invalid/missing/exists/unsafe/corrupt/I/O result remains authoritative over later cleanup failures; a cleanup-only load failure is I/O; every post-exclusive-open provision failure is commit-unknown. Public failures are one exported `Phase3ApprovalKeyError` with the closed codes and fixed messages recorded in the requirements ledger, no `cause`, and no injected path, key, random, canary, or upstream text.

Two provisioners can produce exactly one success and one exists result. A racing loader can return a valid readability-only lease after all 32 stable bytes are visible but before provision has completed key/directory synchronization and validation. The lease carries no provisioning or durability state. Callers must serialize provision/load, and approval-store initialization may consume a lease only when the caller independently knows provision returned success or after a future reconciliation protocol. A deterministic test pauses after the full write before key sync, permits such a load, and then makes provision fail commit-unknown; that lease does not authorize store initialization.

After commit-unknown, a later load can establish only currently readable valid bytes and key-ID compatibility. It cannot upgrade the unknown result to success or authorize approval-store initialization, registration, composition, or activation. Restart/persistence reconciliation, a cross-process commit marker or lock, interrupted corrupt-key removal, and every automatic recovery action are future work.

The test-only durable-store bridge uses a distinct pre-existing sibling root such as `/data/phase3-approvals`, never the key-state root and never a nested child. It is exercised only after known successful provision; same-key store initialization converges and a wrong key is rejected. The store is closed before the lease is released in `finally`. `DurablePhase3ApprovalGrants` copies constructor key bytes independently, and zeroization remains limited to its existing `close()` contract plus the lease's own release contract.

Explicit residual blockers are descriptor-relative `openat`/`openat2` key access, transient same-UID or root swap-and-restore, mount namespaces, process death, real power loss, restart/persistence reconciliation, corrupt interrupted-key removal, a cross-process commit marker or lock, rotation/deletion, simultaneous key-and-store loss, rollback, backup, operator authorization/audit/status/recovery, production key custody, runtime composition, registration, activation, release, publication, and every live mutation. Native tests are syscall evidence only. Process-death, close/fsync ambiguity across death, and actual power loss remain `UNVERIFIED` and activation-blocking.

## Phase 3Q inert existing-key synchronization contract

Phase 3Q adds only `synchronizeExistingPhase3ApprovalKey(options): Promise<Phase3ApprovalKeySyncLease>` to the exact mirrored Phase 3P source island, plus focused tests, isolation updates, and these contract records. It adds no runtime consumer, composition, authority, audit event, lock, marker, journal, status file, store operation, configuration, package/container/mapping change, CLI/MCP surface, Home Assistant access, or mutation. The API is Linux-only and existing-only. It never creates, writes, replaces, unlinks, repairs, rotates, migrates, or removes logical key content. Positioned reads may update access time, and file or directory `fsync` may change filesystem persistence state; the contract therefore makes no "filesystem has no effects" claim, only that this operation issues no logical content-write call.

The only supported value is fresh typed sync-sequence evidence before reopening an already initialized store whose existing `header.json` is successfully authenticated with the returned key. An empty root, a missing header, or an incomplete header is absent/uninitialized. Synchronization alone never authorizes store initialization. Absent-store initialization requires separate explicit non-rollback authority for the identity. The test-only bridge first creates and closes an authenticated header under independently known successful provisioning authority, releases that earlier load lease, synchronizes the existing key, constructs and initializes the reopening store before release, closes every store before release, and proves wrong-key rejection. No production bridge or authority is added.

Lifecycle serialization is a caller precondition: provisioning, rotation, store initialization, and every key or store writer must be excluded for the full call and subsequent store construction/authentication interval. Multiple synchronizers are supported. The API has no lock, marker, persisted status, enforced expiry, or state machine. Mutation after the final read or after return remains possible outside the precondition. A provision race is adversarial and out of contract; no observation from such a race authorizes initialization.

The successful Linux sequence is exactly:

1. Validate configuration and real/effective UID, no-follow `lstat` the existing private parent, open it `O_RDONLY|O_DIRECTORY|O_NOFOLLOW`, bind descriptor/path identity, and retain the descriptor.
2. Perform the initial key `lstat`; only `ENOENT` at this exact observation is `approval_key_missing`.
3. Open the exact key path `O_RDONLY|O_NOFOLLOW` with no `O_RDWR`, create, or write flag; bind descriptor and path metadata/identity.
4. Perform the first bounded positioned exact 32-byte read plus one-byte EOF proof into the candidate; reject all-zero content.
5. Revalidate key descriptor/path and parent descriptor/path against their pinned metadata.
6. `fsync` the read-only key descriptor. Native support is claimed only for Linux.
7. Revalidate key descriptor/path and parent descriptor/path again.
8. `fsync` the retained parent descriptor.
9. Perform a second bounded positioned exact 32-byte read plus EOF proof into a separate verification buffer, reject all-zero content, compare exactly 32 indexed bytes, then finally revalidate key descriptor/path and parent descriptor/path.
10. Close the key descriptor and then the parent descriptor successfully, wipe the verification buffer, and only then return the candidate lease.

No close or failed synchronization is retried internally. Only a fresh explicit invocation retries the operation. The shared exact-read ceiling remains 64 attempts per read operation, including its EOF proof; only `EINTR` is retried. The candidate and verification buffers are wiped after all cleanup on every failure. The verification buffer is always wiped after cleanup on success or failure, including comparison, synchronization, close failure, and injected prototype manipulation. Fixed indexed comparison and wiping do not consult shadowable `length`, `byteLength`, `fill`, or prototype methods.

The lease is exactly `Object.freeze({key, syncStatus: "file_and_parent_sync_completed", release})`. `key` is the one mutable 32-byte candidate buffer. The literal means only that this invocation completed two matching reads, both synchronization calls, all specified validation checkpoints, both closes, and verification-buffer wiping. It is forgeable application data, nonpersistent, caller-scoped, and neither authorization nor proof of historical reconciliation, provisioning success, key continuity, storage survival, or non-rollback state. `release(): void` is synchronous and repeatable, wipes exactly indices 0 through 31 of the same key despite caller/prototype manipulation, and irrevocably ends valid caller use of that key. The unchanged literal remains readable after release and does not become a trusted state machine.

### Phase 3Q error and evidence matrix

| Boundary or observation                            | Condition                                                   | Public result                                              | Evidence/cleanup rule                                |
| -------------------------------------------------- | ----------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------- |
| Options, platform, UID, paths, or required flags   | Invalid Phase 3P contract                                   | `approval_key_invalid`                                     | No key access and no lease.                          |
| Initial parent `lstat`                             | Boundary failure, including absent parent                   | `approval_key_io_failure`                                  | No lease.                                            |
| Initial parent metadata                            | Wrong type/UID/mode                                         | `approval_key_unsafe`                                      | No lease.                                            |
| Parent open/binding or any later parent checkpoint | `ENOENT`, `ELOOP`, metadata/identity drift, or substitution | `approval_key_unsafe`                                      | First primary survives cleanup.                      |
| Parent open/binding or checkpoint                  | Other boundary or malformed result                          | `approval_key_io_failure`                                  | No lease.                                            |
| Initial key `lstat`                                | `ENOENT` only                                               | `approval_key_missing`                                     | Sole missing classification.                         |
| Initial key metadata                               | Wrong type/UID/mode/link                                    | `approval_key_unsafe`                                      | No lease.                                            |
| Initial key metadata                               | Static size other than 32                                   | `approval_key_corrupt`                                     | No lease.                                            |
| Key open/binding or later key checkpoint           | `ENOENT`, `ELOOP`, metadata/identity drift, or substitution | `approval_key_unsafe`                                      | No lease.                                            |
| Key open/binding or checkpoint                     | Other boundary or malformed result                          | `approval_key_io_failure`                                  | No lease.                                            |
| Either exact data read                             | Unexpected EOF                                              | `approval_key_corrupt`                                     | Both owned buffers wiped after cleanup.              |
| Either EOF proof                                   | Trailing byte                                               | `approval_key_corrupt`                                     | Both owned buffers wiped after cleanup.              |
| Either complete read                               | All zero                                                    | `approval_key_corrupt`                                     | Both owned buffers wiped after cleanup.              |
| Either read boundary                               | Non-`EINTR`, malformed/overrun count, or retry exhaustion   | `approval_key_io_failure`                                  | Both owned buffers wiped after cleanup.              |
| Two complete nonzero reads                         | Any of the fixed 32 bytes differs                           | `approval_key_unsafe`                                      | No lease; both owned buffers wiped.                  |
| Read-only key descriptor `fsync`                   | Unsupported or any failure/malformed result                 | `approval_key_io_failure`                                  | No lease and no status upgrade.                      |
| Retained parent descriptor `fsync`                 | Unsupported or any failure/malformed result                 | `approval_key_io_failure`                                  | No lease and no status upgrade.                      |
| Key or parent close                                | Any failure/malformed result                                | `approval_key_io_failure` unless an earlier primary exists | Key close precedes parent close; no retry.           |
| Cleanup after an earlier primary                   | Revalidation or either close also fails                     | First primary unchanged                                    | Candidate/verification still wiped after cleanup.    |
| Entire exact sequence                              | All boundaries fulfill and both reads match                 | Frozen sync lease                                          | Verification wiped; candidate remains until release. |
| Later explicit invocation                          | Stable existing state                                       | Independently evaluated fresh result                       | Prior success/failure literal supplies no authority. |

The literal is evidence of invocation sequencing only. Native tests establish that the default Linux implementation can `fsync` an `O_RDONLY` key descriptor, keeps key content unchanged, supports repeated/concurrent calls, and authenticates an already-header-bound distinct-root store while rejecting a wrong key. Injected boundary tests, exact event assertions, and source inspection establish exact flags, call order, zero logical write calls, failure classification, drift checks, cleanup precedence, and fixed wiping. Native tests do not claim exact syscall flag tracing or prove the absence of every kernel write. Separate-process death and actual power-loss/storage-survival behavior remain `UNVERIFIED` and are not Phase 3Q completion gates.

Explicit residual blockers are external non-rollback authority and epoch definition; absent-store authorization; descriptor-relative key access; lifecycle locking; transient pathname swaps, namespaces, and hostile same-UID mutation; persistent trusted status; process death and real power loss; corrupt-key removal; rotation and migration; simultaneous loss, rollback, and backup; operator workflow, custody, audit, composition, registration, activation, release, publication, and live mutation.
