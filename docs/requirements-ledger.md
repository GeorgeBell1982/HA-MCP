# Requirements ledger

Status: Phase 1 validation contract, updated 2026-07-15

## Objective

Build a standalone, production-quality TypeScript MCP server that complements Home Assistant's official MCP server by providing bounded, auditable configuration inspection and a staged, validated, reversible mutation workflow without exposing generic shell, arbitrary file writes, secrets, or unrestricted service calls.

## Risk

`HIGH`. The project crosses authentication, authorization, filesystem, Git, process execution, smart-home availability, secret handling, public MCP contracts, and cross-platform deployment boundaries. Production mutation is explicitly outside the initial development phase.

## Acceptance criteria

| ID     | Observable requirement                                                                                                                                                                                                                                                                                                                                                                           | Planned implementation evidence                                                                                                                               | Planned validation evidence                                                                                                                                                                                                                                                                                                                     |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-001 | Codex connects using distinct `ha_*` engineering tools through local stdio and the HA add-on's authenticated Streamable HTTP endpoint; an optional local stdio bridge preserves stdio-only clients.                                                                                                                                                                                              | Shared tool registry with stdio, HTTP, and bridge transport adapters plus Codex examples.                                                                     | MCP client integration tests list/invoke identical tools over both transports and through the bridge.                                                                                                                                                                                                                                           |
| AC-002 | REST and WebSocket clients provide authenticated, typed, timed-out read access without leaking credentials through URLs, redirects, proxies, or cross-origin forwarding.                                                                                                                                                                                                                         | Separate clients with validated responses, strict endpoint policy, bounded retries for idempotent reads, and WebSocket reconnect policy.                      | Mock HTTP/WebSocket integration tests for success, 401, timeout, disconnect/reconnect, rejected URL userinfo/scheme, redirects, and cross-origin credential non-forwarding.                                                                                                                                                                     |
| AC-003 | Read-only tools inspect system information, entities, automations, scripts, helpers, dashboards, scenes, blueprints, config, errors, Git, and pending diffs with filtering/pagination.                                                                                                                                                                                                           | Read services and bounded result contracts.                                                                                                                   | Unit and integration tests for empty/single/multiple and truncation paths.                                                                                                                                                                                                                                                                      |
| AC-004 | Configuration access is confined to configured roots and safe text formats.                                                                                                                                                                                                                                                                                                                      | Canonical-path policy rejects traversal, out-of-root paths, symlink escapes, disallowed symlink writes, oversized/binary/unsupported files.                   | Temporary-filesystem adversarial tests on Ubuntu and Windows.                                                                                                                                                                                                                                                                                   |
| AC-005 | Home Assistant YAML tags and split includes are handled without resolving `!secret`; structured edits preserve comments/order and avoid unrelated reformatting where feasible.                                                                                                                                                                                                                   | Round-trip YAML document model and include graph scanner.                                                                                                     | Fixtures for all requested include tags, duplicate keys, comments, anchors, and secrets.                                                                                                                                                                                                                                                        |
| AC-006 | Every write begins as an immutable, expiring proposal containing all requested metadata, content hashes, exact diff, validation/risk/reload information, and redacted output.                                                                                                                                                                                                                    | Proposal repository and state machine.                                                                                                                        | Hash, expiry, stale proposal, tampering, redaction, and serialization tests.                                                                                                                                                                                                                                                                    |
| AC-007 | Applying a proposal rechecks hashes and approval, creates a recoverable checkpoint, validates, applies atomically, revalidates, reloads the narrowest domain, verifies, and automatically restores on failure.                                                                                                                                                                                   | Transaction coordinator with injected filesystem, validator, reload, verification, backup, and Git ports.                                                     | Full success and failure workflow tests, including crash/failure injection at every state transition.                                                                                                                                                                                                                                           |
| AC-008 | Writes, restarts, and deletes default disabled; high-risk/destructive actions require explicit approval metadata; deletion is unavailable by default.                                                                                                                                                                                                                                            | Central policy engine evaluated before side effects.                                                                                                          | Authorization matrix and configuration-default tests.                                                                                                                                                                                                                                                                                           |
| AC-009 | Service calls are explicit allowlisted operations; no generic service-call or shell/write tool exists.                                                                                                                                                                                                                                                                                           | Capability registry and deployment command allowlist.                                                                                                         | MCP tool inventory snapshot and policy rejection tests.                                                                                                                                                                                                                                                                                         |
| AC-010 | Validation has local, deployment-aware Home Assistant, and post-apply levels with machine and human summaries.                                                                                                                                                                                                                                                                                   | Validator composition and deployment adapters.                                                                                                                | Parser, check-config result, timeout, unavailable-adapter, and post-apply tests.                                                                                                                                                                                                                                                                |
| AC-011 | Reloads prefer affected domains; restart is separate, disabled by default, and never used merely to obtain validation.                                                                                                                                                                                                                                                                           | Change-impact mapper and reload/restart policy.                                                                                                               | Mapping tests and restart-denial tests.                                                                                                                                                                                                                                                                                                         |
| AC-012 | Git operations are scoped to proposal files, detect unrelated changes/conflicts, avoid history rewriting and remote pushes, and commit only through separately authorized `ha_commit_change`; apply checkpoints are non-commit backups/journals.                                                                                                                                                 | Git adapter using argument arrays/scoped pathspecs plus separate backup store.                                                                                | Temporary-repository tests for dirty trees, conflicts, scoped commits, rollback, no apply-time commits, and no remote operations.                                                                                                                                                                                                               |
| AC-013 | Every tool call is durably represented by a redacted JSONL audit record; if the audit sink is unavailable, the call fails before protected data or side effects are returned.                                                                                                                                                                                                                    | Fail-closed audit middleware, startup health check, serialized append sink, and safe rotation contract.                                                       | Schema, redaction, partial-write recovery, concurrency, injection, permissions, disk/write failure, correlation, and rotation tests.                                                                                                                                                                                                            |
| AC-014 | Structured automation/script/helper/dashboard/scene tools generate proposals and cannot bypass workflow.                                                                                                                                                                                                                                                                                         | Domain builders call the same proposal service.                                                                                                               | Contract tests proving no direct write path.                                                                                                                                                                                                                                                                                                    |
| AC-015 | Deployment-specific validation, reload, restart, and config access are isolated for OS, Supervised, Container, and Core; first supported target is selected only after environment discovery.                                                                                                                                                                                                    | Deployment adapter interfaces and capability negotiation.                                                                                                     | Adapter contract tests plus supported-target integration/E2E tests.                                                                                                                                                                                                                                                                             |
| AC-016 | CLI provides `doctor`, `check-auth`, `check-config-path`, `check-git`, `validate`, `list-capabilities`, and `show-policy` without disclosing secrets.                                                                                                                                                                                                                                            | CLI composition over the same application services.                                                                                                           | CLI exit-code, output, redaction, and unavailable-capability tests.                                                                                                                                                                                                                                                                             |
| AC-017 | Configured secret-source files are metadata-only and excluded from reads/search/index/diff/Git-content/diagnostic paths; known exact secret values and credential patterns are redacted from all outputs, while raw HA logs are never returned by default.                                                                                                                                       | Secret-source registry, canonical identity checks, exact-value redactor held only in memory, heuristic credential redactor, and safe error summarizer.        | Direct/alias/symlink read and search denials plus canary tests across diffs, logs, responses, audit, diagnostics, and Git output.                                                                                                                                                                                                               |
| AC-022 | High-risk approval cannot be satisfied by MCP-client assertions; it uses a separate operator-controlled out-of-band channel and binds one-time authorization to proposal ID and diff digest.                                                                                                                                                                                                     | Phase 3 approval port, initially local CLI confirmation producing a short-lived single-use server-side grant.                                                 | Forgery, replay, expiry, restart, wrong digest/proposal, and cancellation tests.                                                                                                                                                                                                                                                                |
| AC-023 | The project installs as a custom Home Assistant add-on on HA OS/aarch64 without `/config` in Phase 1, host access, privileged mode, Docker socket, general Supervisor API, or public exposure; all mutation options default false.                                                                                                                                                               | Add-on repository metadata, container, minimal permissions, schema/options, health/start scripts, and ingress diagnostics/setup.                              | Metadata/schema validation, aarch64 container build, startup/health tests, manifest capability inspection, and absence-of-mount tests.                                                                                                                                                                                                          |
| AC-024 | The disabled-by-default HTTPS transport uses generated/pinned TLS plus distinct paired client credentials and enforces secure ingress bootstrap, hashed secrets, per-client rotation/revocation, Host/Origin/proxy, body/output, session ownership, timeout, rate/concurrency, topology, and exposure policies; neither credential leaves its trust boundary or crosses plaintext LAN transport. | Separate ingress/HTTPS listeners, certificate/client credential stores under `/data`, HTTP security middleware, and bridge OS credential/pinning integration. | Startup-default, TLS generation/storage/fingerprint/rotation, entropy/hash/constant-time, unauthorized/query credential, non-loopback plaintext refusal, Host/Origin/DNS-rebinding/forwarded-header, oversized, replay/cross-client/session expiry, rotation, concurrency/overload, logging-canary, topology, MITM, and identity-pinning tests. |
| AC-025 | In add-on mode the HA client uses only the Supervisor-injected runtime token against the fixed Home Assistant Core API proxy and never accepts arbitrary origins/paths or exposes/persists the token.                                                                                                                                                                                            | `homeassistant_api: true` manifest plus fixed-origin/path HA client credential provider.                                                                      | Manifest denial assertions, fixed-origin/path tests, 401/403 redaction, and token canaries across options, diagnostics, audit, logs, MCP and bridge traffic.                                                                                                                                                                                    |
| AC-018 | Documentation covers architecture, security, deployment types, setup, tools, least privilege, backup, mutation kill switch, audit rotation, and all requested recovery cases.                                                                                                                                                                                                                    | Required documentation set.                                                                                                                                   | Documentation link/command/example review.                                                                                                                                                                                                                                                                                                      |
| AC-019 | Tests are layered: unit, mocked API integration, temporary filesystem/Git, complete workflow, rollback, and practical Docker E2E without production access.                                                                                                                                                                                                                                      | Test suites and fixtures.                                                                                                                                     | Phase gates and clean-room validation.                                                                                                                                                                                                                                                                                                          |
| AC-020 | Each phase runs formatting, linting, type checking, relevant tests, and security tests; critical safety failure stops progress.                                                                                                                                                                                                                                                                  | Package scripts and CI phase gates.                                                                                                                           | Exact command evidence and clean-room runs.                                                                                                                                                                                                                                                                                                     |
| AC-021 | No live Home Assistant change occurs without explicit approval of the exact change; no commit/push/deploy occurs unless separately requested.                                                                                                                                                                                                                                                    | Operational policy and human gates.                                                                                                                           | Review of audit trail and prohibited-action checks.                                                                                                                                                                                                                                                                                             |

## Phase 2 Slice B acceptance ledger

| ID    | Observable requirement                                                                                                                                                                        | Implementation evidence                                                                              | Validation evidence                                                                                                                    |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| B-001 | Linux file content uses authoritative beneath-root openat2 confinement; Windows, missing helper, unsupported kernel, or unsafe packaging fails unavailable with no Node race-safety fallback. | Native helper source and NativeOpenat2Reader protocol boundary.                                      | Protocol/unit tests now; Linux active-race and packaged-image checks remain UNVERIFIED until Slice G.                                  |
| B-002 | Only strict relative paths and existing bounded regular strict-UTF-8 non-NUL files beneath /homeassistant are eligible.                                                                       | Frozen path schema, helper path validation, resolution flags, descriptor metadata and output bounds. | Invalid-path/protocol/encoding tests; native filesystem adversarial suite in Slice G.                                                  |
| B-003 | secrets.yaml and configured sources are metadata-only by device/inode; hard-link aliases are denied and registration failures latch unhealthy.                                                | ProtectedIdentityRegistry with bounded staged identity registration.                                 | Direct/alias/readiness/duplicate-path/duplicate-identity/failure-latch tests.                                                          |
| B-004 | Exact known values plus heuristic shapes are removed from every nested output class; content is unavailable until a safe value provider succeeds.                                             | SecretValueProvider readiness gate and ExactSecretRedactor.                                          | Canary tests across response/error/snippet/diff/diagnostic/Git-shaped data plus bounds/cycles/cancellation.                            |
| B-005 | Helper work and redaction honor deadlines/cancellation, bounded concurrency/queues/output, child termination, byte cleanup, and stale-result isolation.                                       | BoundedPermitPool, child lifecycle, post-read operation checks, staged registry commit.              | Queue cancellation/deadline/handoff, byte-zeroing, malformed/oversized output, root identity tests; live child termination in Slice G. |

Behavioural matrix: Linux helper present/absent/unsupported; Windows unavailable; valid/invalid/symlink/hard-link/cross-mount paths; empty/small/oversized/binary/NUL files; protected registry unready/ready/failed; exact provider success/failure; immediate/queued/cancelled/deadline helper work; unchanged/changed root and file identity; development boundary versus packaged add-on. macOS is unavailable like Windows for this repository capability. No tool registration, mount, version, release, deployment, or live Home Assistant access is part of Slice B.

## Scope

In scope: the standalone engineering MCP server, read-only HA API integration, bounded config repository access, proposal lifecycle, validation/reload/verification/rollback, scoped Git, audit, CLI, adapters, tests, and documentation.

Out of scope for initial delivery: replacing the official Home Assistant MCP server, arbitrary shell/file/service access, remote Git push, cloud deployment, production configuration mutation, delete tools enabled by default, token management UI, and supporting undocumented Home Assistant internals without a versioned adapter and fallback.

Public contracts: MCP tool names/input/output schemas; environment variable names; proposal/audit schemas; CLI commands/exit codes; deployment adapter capability model.

## Phase 2 Slice C acceptance ledger

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                              | Implementation evidence                                                                                     | Validation evidence                                                                                                                             |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| C-001 | YAML validation uses exact direct dependency `yaml@2.9.0`, the strict YAML 1.2 core parser profile, and remains unregistered/capability unavailable.                                                                                                                                                                                                                                | Root/add-on package and lock mirrors; `strictYamlGate.ts`; no tool-registry or add-on manifest change.      | Add-on mirror check, dependency lock inspection, full repository validation.                                                                    |
| C-002 | A synchronous immutable byte snapshot no larger than 512 KiB is hashed, fatal-UTF-8 decoded, and incrementally parsed without mutating or reserializing caller bytes; BOM, NUL, bare CR, mixed endings, directives, and multiple documents fail closed.                                                                                                                             | Snapshot/final wipe, preflight, 4096-code-unit surrogate-safe Parser + Composer loop.                       | Empty/explicit-empty, LF/CRLF, digest/nonmutation, invalid byte/directive/document, exact byte/multibyte/surrogate fixtures.                    |
| C-003 | Only scalar Home Assistant include/input/secret tags are accepted; raw references are bounded to 100, 512 bytes each, and 32 KiB aggregate, while secret names are represented only by digest and byte length.                                                                                                                                                                      | Explicit scalar custom-tag profile and bounded reference metadata.                                          | Every allowed tag, unknown/known-denied/collection tag, N/N+1, aggregate N/N+1, and secret canary tests.                                        |
| C-004 | Keys are untagged, unanchored primitive scalars with typed canonical identity; duplicate lexical equivalents, non-finite values, complex keys, and `<<` merge syntax are rejected.                                                                                                                                                                                                  | Structural map/pair/key validation independent of JS object conversion.                                     | Block/flow, string-vs-number, duplicate numeric/boolean/-0, tagged/anchored/collection/nonfinite/merge fixtures.                                |
| C-005 | Syntax depth is at most 64; structural nodes and separately alias-expanded nodes are each at most 100,000; aliases are acyclic, resolved, unique-anchor, at most 100 total and at most 100 per target.                                                                                                                                                                              | Structural and expansion traversals with resolved-node identity stacks and fanout accounting.               | Depth/node/reference inclusive N and N+1 fixtures, duplicate/unresolved/cyclic anchors, and expansion bomb.                                     |
| C-006 | Cancellation and a projected monotonic deadline are checked before parsing, around every incremental chunk, after each event-loop yield, at validation phase boundaries, and at least every 256 structural/expanded/reference work units; diagnostics expose only stable allowlisted code plus numeric line/column, and unexpected library failures become safe `internal_failure`. | Internal operation clock, `setImmediate` yields, bounded validation polling, and sanitized `YamlGateError`. | Pre/mid-parse and post-parse structural/expanded cancellation/deadline tests plus unexpected-error canary and bounded diagnostic surface tests. |
| C-007 | Successful output is deeply frozen bounded metadata no larger than 32 KiB and contains no AST, content, parser diagnostics, or raw secret name.                                                                                                                                                                                                                                     | Frozen digest/count/reference metadata and serialized-size gate.                                            | Metadata freeze/size and secret non-disclosure assertions.                                                                                      |

Behavioural matrix: zero-byte, whitespace/comment-only, explicit-empty, and populated documents; LF/CRLF/none versus bare/mixed CR; ASCII/multibyte/surrogate boundaries; valid/invalid UTF-8 and BOM/NUL; block/flow and quoted/plain keys; every allowed/denied tag; no/safe/unresolved/duplicate/cyclic/fanout aliases; depth/node/reference exact boundary and N+1; pre/mid cancellation and deadline; development and mirrored add-on source. Slice C does not register tools, add the `/homeassistant` mount, package the native helper, bump a version, release, deploy, or access live Home Assistant.

## Phase 2 Slice D acceptance ledger

| ID    | Observable requirement                                                                                                                                                                                                                                                                               | Implementation evidence                                                                                                                                                          | Validation evidence                                                                                                                                                      |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-001 | Protected sources are denied by canonical path before content is read and by device/inode identity after the secure read; metadata binds path, identity, and exact SHA-256. Replacement, edit, deletion, or alias drift permanently latches repository reads unhealthy.                              | Protected registry staged path/identity/digest metadata and freshness checks.                                                                                                    | Canonical pre-read, hard-link alias, replacement, same-inode edit, deletion, during-output, digest, and latch tests.                                                     |
| D-002 | Linux catalog discovery uses only the fixed-argument native openat2 helper, two exactly matching validated passes, bounded execution/output/concurrency, no shell, and no Windows fallback.                                                                                                          | Pinned O_PATH root, separate readable openat2 traversal descriptors, strict zero-reserved HALIST2 decoder, complete globally ordered directory/file metadata, and helper source. | Protocol corruption/bounds/capability tests and source assertions; Linux runtime remains UNVERIFIED until Slice G.                                                       |
| D-003 | The deterministic index is bounded to 2,000 YAML files, 16 MiB content, and 200,000 search lines/matches; protected paths are excluded before reads, catalog-root/file identities and size are verified, and snapshots canonically length-frame path, device, inode, and exact content SHA-256.      | Secure one-file-at-a-time indexing with byte wiping.                                                                                                                             | Ordering, hash, protected exclusion, drift, bounds, and sanitized failure tests.                                                                                         |
| D-004 | Cursors are canonical unpadded base64url of exactly 136 characters encoding a fixed 70-byte payload plus 32-byte HMAC; operation, query, snapshot, offset, and service key are bound.                                                                                                                | Fixed cursor codec and strict schemas.                                                                                                                                           | Canonicalization and invalid_input for malformed/auth/operation/query/rotation/closure; stale_source for authenticated snapshot/offset drift, without registry latching. |
| D-005 | Unregistered list/read/search are deterministic and bounded; reads redact exact and heuristic secrets without legacy 16 KiB truncation; linear KMP search drops matches hidden by full-line redaction, clips safely, and charges preprocessing/comparisons/lines/matches to one 200,000-unit budget. | Repository read service, full-text redaction, and scalar-safe clipping.                                                                                                          | List/read/search, long-file, redacted match, case, emoji, and cancellation tests.                                                                                        |
| D-006 | Cancellation/deadline work is polled at least every 256 units and unexpected failures become stable allowlisted errors.                                                                                                                                                                              | Operation checks, permit pool, helper termination, polling, and sanitizer.                                                                                                       | Long-line cancellation, unavailable helper, malformed protocol, and exception-canary tests.                                                                              |
| D-007 | Slice D remains unregistered with no mount, registry/config wiring, version bump, release, deployment, or live access.                                                                                                                                                                               | Repository diff and exact source mirror.                                                                                                                                         | Diff inspection and pnpm verify; Linux runtime remains gated by Slice G.                                                                                                 |

Behavioural matrix: Linux helper available/unavailable/malformed/timed-out/cancelled versus Windows unavailable; zero/single/multiple/2,000/N+1 files and 16 MiB/N+1 aggregate; canonical protected path, hard-link alias, replacement, same-inode mutation, deletion, and during-output drift; list/read/search success, pagination, stale/tampered/wrong-operation/wrong-query/rotated cursor; ASCII/multibyte/surrogate data; visible and hidden matches; cancellation/deadline; root and add-on mirror. Consistency is observed and fail-closed, not atomic.

## Phase 2 Slice E acceptance ledger

Risk: HIGH. Slice E projects untrusted YAML and recursively resolves includes across a race-sensitive protected filesystem boundary, while preserving secret-name non-disclosure and bounded cancellation.

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Implementation evidence                                                                                                                                                                                                                | Validation evidence                                                                                                                     |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| E-001 | Strict YAML validation has one private parse/validate primitive; fixed typed projection runs only after every Slice C check and before document release. Existing validateStrictYaml metadata remains unchanged.                                                                                                                                                                                                                                                                       | Shared strict primitive plus validateAndProjectYaml fixed selective IR.                                                                                                                                                                | Existing Slice C suite unchanged plus projection freeze/surface/no-AST/no-secret-name tests.                                            |
| E-002 | Ordinary projection starts at configuration.yaml and resolves all five include tags relative to the containing file using authoritative catalog files/directories, beneath-root normalization, protected path/identity exclusion, a charged preindexed recursive lowercase-.yaml directory view in exact UTF-8 order, type-specific list/named/merge rules where explicit []/{} are no-ops but no-document input is invalid, repeated-file charging, and expansion-stack cycle denial. | Include graph resolver over validated catalog and secure reader.                                                                                                                                                                       | All tags, shapes, ordering, nested relative, packages, empty, cycle, missing, escape, wrong-kind, protected alias, and duplicate tests. |
| E-003 | Per operation: at most 2,000 unique parsed files, 20,000 edges, include depth 64, 200,000 projection/expansion work, 200,000 retained IR nodes, 20,000 resources, 4 MiB retained strings, 16 MiB aggregate unique source bytes, and bounded IDs; cancellation/deadline polls at most every 256 units and owned file bytes are processed one at a time then wiped.                                                                                                                      | Shared projection budget across expansion and all extraction/finish traversals, unique parse cache, occurrence expansion accounting, charged construction of immutable type-summary and exact-key indexes, and secure read validation. | Inclusive N/N+1 and pre/mid/post cancellation fixtures without checked-in blobs.                                                        |
| E-004 | Root/package domain keys support exact domain or domain plus ASCII-space label; homeassistant.packages accepts inline or include forms and package values are maps. Automation/scene identities, scripts, and exact helper domains obey the frozen ID rules and duplicates fail across all provenance paths.                                                                                                                                                                           | Typed resource extraction from selective map/sequence IR.                                                                                                                                                                              | Root/label/include/package, valid/absent/empty/invalid/tagged/alias/anchor/scalar/collection ID and duplicate matrix tests.             |
| E-005 | Blueprint projection independently scans authoritative blueprints/{automation,script,template}/\*_/_.yaml, validates blueprint name/domain and emits extension-preserving domain-relative IDs; malformed or duplicate blueprints fail only blueprint projection and ordinary orphan YAML is excluded.                                                                                                                                                                                  | Blueprint projection entry point and catalog prefix selection.                                                                                                                                                                         | All three domains, nested path, wrong/missing shape/domain/name, duplicate, and ordinary-orphan tests.                                  |
| E-006 | Resource summaries bind ID/type/provenance/path and exact unredacted source SHA-256. Get returns the full source file only after masking every retained !secret scalar source span, then exact-plus-heuristic nontruncating redaction; secret names never appear.                                                                                                                                                                                                                      | Source-span IR, exact source cache, span masker, and protected redactor.                                                                                                                                                               | Plain/quoted/multiple/boundary/canary spans, shared-file IDs, exact SHA, 512 KiB, and output freshness tests.                           |
| E-007 | Public resource IDs are NFC, control-free, at most 256 Unicode scalars and 512 UTF-8 bytes. resource_not_found is returned only after a successful fresh projection has no exact type/ID.                                                                                                                                                                                                                                                                                              | Tight schema and repository boundary error code.                                                                                                                                                                                       | Input/output boundary and malformed-vs-missing projection classification tests.                                                         |
| E-008 | Resource-list cursors retain the 102-byte/136-character format using operation byte 3 and a domain-separated resource-type query slot; list/search op bytes stay 1/2. Projection snapshots bind rules version, root, sorted source path/identity/SHA, ordered include edges, and sorted resource ID/provenance.                                                                                                                                                                        | Extended cursor codec and canonical framed projection snapshot.                                                                                                                                                                        | Width/backward-byte/op/type/MAC/rotation/stale snapshot/offset tests.                                                                   |
| E-009 | Every secure read validates catalog root, file identity, size, and exact digest; protected freshness runs before graph/catalog access and after held output. Reachable malformed YAML/include/resource data fails with sanitized stable errors; blueprint failures are isolated to blueprint projection.                                                                                                                                                                               | Repository projection service and error sanitizer.                                                                                                                                                                                     | Root/file/content drift, protected replacement/alias, malformed reachable/orphan, blueprint isolation, and canary-error tests.          |
| E-010 | Slice E remains unregistered and produces no tool/application/config/Docker/build/version/mount/package/release/deploy/commit/push change.                                                                                                                                                                                                                                                                                                                                             | Diff scope and exact root/add-on source mirror.                                                                                                                                                                                        | Prohibited-path diff, mirror hash, inventory, full verify, independent review, and clean-room gates.                                    |

Behavioural matrix: ordinary versus blueprint projection; root/label/package/nested includes; include/list/named/merge-list/merge-named; empty/single/multiple/repeated/cyclic/missing/escaped/protected/symlink/wrong-kind sources; automation/scene/script/helper/unknown/UI resource types; valid, absent, empty, duplicate, tagged, aliased, anchored, numeric, null, boolean, and collection identities; plain/quoted/multiple secret spans; fresh/stale/tampered/wrong-operation/wrong-type cursors; zero/N/N+1 files/edges/depth/work/nodes/resources/string bytes/ID scalars+bytes; cancellation/deadline before read, parse, expansion, yield, and output; Windows neutral tests versus Linux native runtime UNVERIFIED; root and exact add-on mirror. Registration and live Home Assistant access remain absent.

## Phase 2 Slice F acceptance ledger

Risk: HIGH. Slice F handles attacker-controlled Git metadata, configuration, object data, paths, subprocess behavior, and repository races while guaranteeing that protected YAML content and secrets cannot escape.

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Implementation evidence                                                                        | Validation evidence                                                                                                                                                                          |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | Git reads require an absolute Git executable and a packaged fixed-operation Linux native broker. The broker pins the repository and direct `.git` directory beneath root with no links, magic links, or cross-device traversal; rejects `.git` files, linked/bare/common-dir/submodule/gitlink repositories and ambiguous or network/indirection metadata; executes through pinned `/proc/self/fd` descriptors; and validates root plus Git identities before and after. Windows, missing/unpackaged broker, or any uncertain state returns `capability_unavailable` with no fallback.                                                    | `NativeGitBroker`, strict binary protocol, and Linux broker source contract.                   | Platform/unpackaged, malformed protocol, identity drift, forbidden topology/config, fixed invocation, and native source-contract tests; real Linux confinement remains Slice G `UNVERIFIED`. |
| F-002 | The broker uses no-new-privileges, read-only Landlock for only repository/runtime inputs, seccomp denial of networking and unrelated execution, CPU/address-space/file/output/fd/process limits, process-group termination, fixed arguments, and an exact non-inherited environment that disables prompts, locks, pagers, replacements, lazy fetch, hooks, filters, fsmonitor, attributes, excludes, global/system config, proxies, credentials, SSH/askpass, PATH lookup, and Git indirection. Sandbox write denial is authoritative.                                                                                                    | Fixed broker operation request plus native policy source and TypeScript invocation guard.      | Exact argv/environment/request tests; abort/timeout/kill/output/stderr tests; forbidden token and native-policy source tests. Linux enforcement remains Slice G `UNVERIFIED`.                |
| F-003 | Status is strict porcelain v2 `-z --branch` with untracked all, ignored submodules, no renames, and no ahead/behind. Paths are fatal UTF-8, NFC, control-free normalized relatives, UTF-8 sorted, and capped at 500 without truncation. Type-2 rename, unmerged, submodule, unknown, and malformed records fail closed. Detached branch is null and unborn branch keeps its name. Status may expose protected path and XY only, never hashes, identities, or content.                                                                                                                                                                     | Strict status parser and immutable public status model.                                        | Clean/dirty/staged/unstaged/untracked/unborn/detached/protected/malformed/N+1 fixtures.                                                                                                      |
| F-004 | Diff accepts only current securely catalogued regular `.yaml`/`.yml` paths whose identities are nonprotected. Caller paths, when present, are nonempty, unique, UTF-8 sorted, at most 100, YAML, and current-catalog backed. Protected paths fail `protected_resource`; missing, deleted, index-only, symlink, gitlink, conflict, or unsupported paths fail `path_denied` before blob access. Unscoped mode intersects eligible YAML and emits only a generic warning for omitted unsupported paths. `truncated` describes display shortening only.                                                                                       | Catalog/status eligibility planner with protected preflight.                                   | Explicit/unscoped eligibility, protected-before-blob, delete/rename/conflict/binary/non-YAML/symlink/submodule tests.                                                                        |
| F-005 | Diff never invokes `git diff`. It uses fixed plumbing only: status/raw metadata, stage-zero `ls-files`, full-tree `ls-tree` at HEAD, object-format discovery, and bounded `cat-file` batch check/read with literal path separators. Only regular stage-zero blobs are accepted; conflicts, gitlinks, and symlinks fail. SHA-1 and SHA-256 object formats are supported, sizes are checked before content, and lazy fetch is disabled.                                                                                                                                                                                                     | Fixed broker operations and strict plumbing parsers.                                           | Fake-broker operation/ordering/stdin/object-format/mode/stage/size/forbidden-command tests.                                                                                                  |
| F-006 | Canonical diff semantics are HEAD (empty tree when unborn) to stage zero for index, and stage zero to descriptor-stable current content for worktree. `both` emits staged then worktree and may emit a path twice. Untracked is empty to current; staged add is empty to index. Deletion and rename source are unsupported. Output is path-sorted, canonical LF, line-based bounded Myers with three context lines, fixed headers, no-final-newline markers, and at most 200,000 work units.                                                                                                                                              | Internal deterministic patch engine independent of Git rendering.                              | Golden clean/dirty/staged+unstaged/untracked/unborn/detached/rename-as-D+A and work-boundary tests.                                                                                          |
| F-007 | Every consumed HEAD/index/current side is bounded to 512 KiB, rejects NUL or invalid UTF-8, passes the strict YAML gate exactly once, masks retained `!secret` spans, then receives complete nontruncating exact-plus-heuristic redaction before diffing. Unique raw blob/current input is capped at 2 MiB, complete canonical redacted patch at 1 MiB, and public output at 512 KiB. Public truncation occurs only after redaction on complete UTF-8/LF lines with a fixed marker. `diffSha256` hashes the complete canonical redacted patch before truncation; empty output hashes empty bytes. Raw and intermediate buffers are wiped. | Side loader, strict projection, span masking, redactor, patch bounds, truncator, and digest.   | Secret-name/value/digest canaries, invalid encoding/NUL/YAML, exact N/N+1 byte/work/patch/public limits, truncation/hash/wipe tests.                                                         |
| F-008 | Protected freshness and authoritative catalog/root/Git identities are checked before and after held output. Snapshot evidence binds root/Git identities, object format, HEAD/index identities/digests, eligible current path identities/hashes, scope/filter digest, and consumed object IDs/content digests. Drift discards output as `stale_source`; protected drift latches unhealthy. Cooperative work polls at most every 256 units; processes are killed and bounded; errors are stable and sanitized.                                                                                                                              | Snapshot framing, freshness checks, broker identity records, operation polling, and sanitizer. | Root/Git/index/object/current/protected drift, cancellation/deadline/kill, snapshot determinism, and exception-canary tests.                                                                 |
| F-009 | Slice F remains unregistered and unavailable on Windows or without the packaged broker. It changes no application/tool/config/Docker/build/version/mount/package/release/deploy/commit/push path. Root and add-on sources are exact mirrors. Linux Git, malicious config/filter/fsmonitor/hooks, active races, Landlock/seccomp, and packaged runtime validation remain Slice G `UNVERIFIED`.                                                                                                                                                                                                                                             | Internal-only exports, exact mirror, and documentation gate.                                   | Inventory/prohibited-diff/mirror hashes, focused/full validation, independent review, and clean-room validation.                                                                             |

Behavioural matrix: broker packaged/unpackaged and Linux/Windows; clean/dirty/staged-only/unstaged-only/staged+unstaged/untracked; born/unborn/detached; SHA-1/SHA-256; explicit/unscoped and worktree/index/both; empty/single/multiple/100/N+1 paths and 500/N+1 status records; regular YAML versus missing/deleted/index-only/binary/non-YAML/symlink/gitlink/submodule/conflict/rename; protected canonical path and identity alias; valid/malformed/fatal-encoding status/config/index/tree/object/protocol; secret name/value/digest canaries; N/N+1 side/aggregate/work/complete-patch/public limits; stable/drifted root, Git dir, index, object, current file, and protected registry; success/cancellation/deadline/output overflow/broker crash; fake Windows coverage versus real Linux confinement and races `UNVERIFIED`; root and exact add-on mirror. Registration and live repository access remain absent.

## Phase 2 Slice G1 acceptance ledger

Risk: HIGH. Slice G1 introduces durable proposal and audit persistence, crash reconciliation, authenticated pagination, cancellation commit boundaries, and fail-closed storage security. It remains internal and unregistered.

| ID     | Observable requirement                                                                                                                                                                                                                                                                                |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1-001 | Phase 2 uses a separate version-2 audit adapter while the Phase 1 `JsonlAudit`, schema, path, and tools remain unchanged.                                                                                                                                                                             |
| G1-002 | Durable ordering is attempt, then effect/read, then durable outcome, then response. Validation requiring no protected access may fail before an attempt; every path after an attempt receives exactly one terminal or reconciled outcome.                                                             |
| G1-003 | Audit records follow the exact frozen schemas. A propose attempt excludes `diffSha256`; records never contain content, query, snippet, diff, secret, or exception data.                                                                                                                               |
| G1-004 | Audit files and appends are serialized, bounded, canonical, synced, and rotated with directory sync. Recovery repairs only a provably incomplete active tail; invalid complete records, permission failures, partial writes, `ENOSPC`, sync failures, and ambiguous rotation latch Phase 2 unhealthy. |
| G1-005 | Protected storage uses `0700` directories and `0600` files; rejects links and non-regular files; uses same-filesystem temporary files; writes canonical bounded digested content through fsync, rename, and directory sync; and wipes owned sensitive buffers.                                        |
| G1-006 | Exact candidate and diff bytes exist only in protected storage while public proposal data is frozen. Candidate handling enforces fatal UTF-8, the 512 KiB boundary, digest binding, strict YAML, secret-span masking, and complete redaction.                                                         |
| G1-007 | Propose authoritatively and securely rereads an existing nonprotected YAML source, verifies the expected hash, and computes a deterministic diff. It never mutates live configuration or reloads, restarts, commits, or pushes.                                                                       |
| G1-008 | Idempotency is durable and serialized: the same key and identity returns the original proposal, including terminal proposals; a different identity conflicts; concurrent identical requests produce one effect.                                                                                       |
| G1-009 | Only `pending` proposals transition to `discarded` or `expired`; identity and payload remain immutable. Expiry has an exact boundary and remains correct across restart and clock changes.                                                                                                            |
| G1-010 | Listing is deterministic UTF-8 order and uses authenticated snapshot/key-bound cursors. Malformed, cross-operation, stale, out-of-range, rotated-key, closed-session, N, N+1, concurrent, and expiry pagination paths are bounded and fail safely; cooperative work polls at most every 256 units.    |
| G1-011 | Crash journal recovery distinguishes three windows: pre-effect rename causes no effect and a failed/reconciled outcome; post-effect/pre-outcome retains the effect and emits one reconciled/committed-unconfirmed outcome; post-outcome/pre-cleanup performs cleanup only.                            |
| G1-012 | Repeated recovery never duplicates effects or outcomes; `operationId` is authoritative.                                                                                                                                                                                                               |
| G1-013 | Quarantine is same-filesystem, protected, and bounded by count, item bytes, aggregate bytes, and work. Ambiguity or overflow latches the service unhealthy.                                                                                                                                           |
| G1-014 | Cancellation before the commit boundary has no effect; after rename the operation finishes or reconciles and never claims rollback.                                                                                                                                                                   |
| G1-015 | Any Phase 2 audit failure persistently latches Phase 2 unhealthy and blocks further operations while Phase 1 behavior remains compatible.                                                                                                                                                             |
| G1-016 | Slice G1 changes no registry, application, transport, config, Docker, manifest, mount, version, release, deployment, commit, or push path; root and add-on source mirrors are exact.                                                                                                                  |

Behavioural matrix: list/get/propose/discard; pending/discarded/expired/missing/corrupt; sequential/concurrent/restart idempotency; stable/stale/missing/protected/alias/invalid-YAML/invalid-encoding/bounded sources; open/write/short-write/fsync/rename/directory-sync/close/permission/`ENOSPC` failures; all three crash windows; audit attempt/effect/outcome/rotation/incomplete-tail/invalid-complete/repeated recovery; expiry before/at/after and during paging; valid/malformed/stale/wrong-operation/wrong-key/rotated/closed/N/N+1 cursors; cancellation before and after commit; symlink/hard-link/directory replacement/nonregular/oversize/duplicate/quarantine N/N+1 storage; owned-buffer wiping on success and every failure path. Windows coverage is logical; Linux permission, link, atomicity, directory-sync, and process-crash validation remains Slice G2.

## Phase 2 Slice G2 acceptance ledger

Risk: HIGH. Slice G2 packages inert candidate-only Linux helpers and validates native confinement and durability. It does not enable Phase 2 capabilities.

| ID     | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G2-001 | Human approval precedes dependency or base-image resolution, compiler installation, Docker/WSL/native-runner setup, or aarch64 execution. Every preflight command records exact state, output, and exit code; missing evidence is `BLOCKED`.                                                                                                                                                                                                                                                                                                                  |
| G2-002 | Root and add-on sources for `openat2-read`, `openat2-list`, and `git-broker` remain exact mirrors and compile from the add-on context with warnings-as-errors and supported PIE, stack, FORTIFY, RELRO, NOW, and non-executable-stack hardening.                                                                                                                                                                                                                                                                                                              |
| G2-003 | Candidate helpers exist only at `/app/native/openat2-read`, `/app/native/openat2-list`, and `/app/native/git-broker`, owned by root and installed mode `0555`; Git is the exact `/usr/bin/git` runtime package.                                                                                                                                                                                                                                                                                                                                               |
| G2-004 | Build/runtime image digests, Alpine repositories and package versions, compiler/toolchain, Git path/version, helper hashes, ELF mode, loader/shared libraries, and broker runtime inputs are frozen as exact evidence. The broker invocation requires exactly one absolute runtime loader and at most 16 absolute runtime-library files; all are no-follow unique regular-file identities with loader read/execute and libraries read-only. Broad paths, wildcards, missing/extra closure, unresolved dependencies, or runtime installation block completion. |
| G2-005 | Candidate packaging is inert: `run.sh`, application construction, registry, transports, and environment do not reference native helpers. The add-on `0.1.5` and no-release/no-version-change statements are historical G2-slice constraints only; the active invariants are `map: []`, Phase 1 tool inventory, startup, permissions, and observable behavior.                                                                                                                                                                                                 |
| G2-006 | `openat2-read` rejects final/parent symlinks, magic links, cross-mount paths, root swaps, nonregular files, and same-size concurrent changes; unsupported kernels fail unavailable with no fallback.                                                                                                                                                                                                                                                                                                                                                          |
| G2-007 | `openat2-list` preserves pinned-root secure traversal, complete ordered bounded framing, identical two-pass output, and rejects hostile links, mounts, swaps, drift, malformed output, and unsupported kernels.                                                                                                                                                                                                                                                                                                                                               |
| G2-008 | Reader/catalog TypeScript invocation uses absolute helpers, fixed argv/cwd, empty environment, no shell, bounded concurrency/output, cancellation/deadlines, child termination/reaping, and stable sanitized failures.                                                                                                                                                                                                                                                                                                                                        |
| G2-009 | `git-broker` accepts only its five frozen operations, pins root and direct `.git`, revalidates identities, and rejects linked/bare/common-dir/worktree/submodule, alternates, replacements, promisor, partial-clone, and ambiguous topology.                                                                                                                                                                                                                                                                                                                  |
| G2-010 | Hostile local/global/system Git includes, hooks, filters, external diff, fsmonitor, attributes, excludes, aliases, credentials, proxy, SSH/askpass, pagers, editors, replacement, and lazy-fetch configuration cannot execute, influence output, access network, write, or disclose canaries.                                                                                                                                                                                                                                                                 |
| G2-011 | Git execution proves `no_new_privs`, read-only least-path Landlock, seccomp network/unrelated-exec denial, and CPU/address/file/output/fd/process rlimits. Sandbox setup failure is fatal.                                                                                                                                                                                                                                                                                                                                                                    |
| G2-012 | Helper crash, signal, malformed/truncated/oversized protocol, cancellation, timeout, kill escalation, and unsupported capability leave no descendants, partial public output, leaked buffers, or permits.                                                                                                                                                                                                                                                                                                                                                     |
| G2-013 | Strict Linux proposal/audit storage enforces real ownership/modes, rejects links/nonregular/identity races, and persistently latches on unsafe scan or durability ambiguity while duplicate-create and expected replacement mismatch remain non-latching.                                                                                                                                                                                                                                                                                                     |
| G2-014 | Linux fault tests cover open, write, short-write, file sync, rename, directory sync, close, permission, `ENOSPC`, partial audit, rotation, quarantine count/item/aggregate, and every latch path.                                                                                                                                                                                                                                                                                                                                                             |
| G2-015 | Separate-process kill tests cover proposal, journal, audit, rotation, and quarantine checkpoints; filesystem state is verified immediately, after first recovery, and after second recovery with exactly one effect/outcome.                                                                                                                                                                                                                                                                                                                                  |
| G2-016 | Linux/amd64 is development evidence only. The authoritative candidate gate requires proven native aarch64 `uname`/CPU/OS/runner provenance, arm64 OCI and ELF evidence, and absence of QEMU/binfmt translation. Emulation remains `UNVERIFIED`.                                                                                                                                                                                                                                                                                                               |
| G2-017 | Candidate-image validation checks digest, architecture, labels, command, exact helper paths/hashes/permissions/linkage, shared-library closure, protocol success/failure, no unexpected writable/setuid artifacts, offline startup, and no runtime install/network.                                                                                                                                                                                                                                                                                           |
| G2-018 | Windows-neutral tests prove Dockerfile/build manifest contracts, source mirrors, fixed paths/modes, startup inertness, unchanged manifest/tool inventory, and prohibited scope before Linux execution. They cannot claim syscall, race, sandbox, permission, crash, or durability proof.                                                                                                                                                                                                                                                                      |
| G2-019 | Historical G2-slice `addon/config.yaml` metadata remained version `0.1.5`, `arch: [aarch64]`, and `map: []`, with no release, deployment, commit, or push. Active invariants remain `arch: [aarch64]`, `map: []`, no registration, no `/homeassistant` mapping, no helper wiring, no added privilege, and no unauthorized release, deployment, commit, or push.                                                                                                                                                                                               |
| G2-020 | Completion requires focused tests, add-on mirror validation, formatting, lint, typecheck, build/package evidence, `pnpm verify`, diff inspection, independent review, amd64 clean room, and every native-aarch64 gate. Missing required Linux/aarch64 evidence is `BLOCKED`.                                                                                                                                                                                                                                                                                  |

Frozen preflight evidence for the candidate packaging slice: the repository-owned native-aarch64 provenance harness now fails closed on non-Linux/non-arm64 process and uname state, missing or emulation-indicating CPU evidence, enabled arm64/aarch64 binfmt translation, non-linux/non-arm64 Docker server state, and missing or malformed bounded self-attested runner identity. This is tooling evidence only: in-guest checks cannot prove absence of full-system emulation, so G2-016 and G2-020 remain `BLOCKED` pending approved native aarch64 execution, external immutable runner provenance, and frozen arm64 candidate-image evidence. Docker client/server 29.6.1, Buildx 0.35.0, BuildKit 0.31.1 on amd64; Node multiarch index `sha256:5539840ce9d013fa13e3b9814c9353024be7ac75aca5db6d039504a56c04ea59`, amd64 manifest `sha256:99351363debf40f3495cb7fc657a777334c3b21143e594dbfcc7de187439633c`, arm64 manifest `sha256:ddde8acdbc31cec9740194b86faacadc174f963c48e1f75a0c1d3918ed715aee`; Home Assistant `aarch64-base:3.22` index `sha256:03185a346a3e505e0aa102cb1e976a2e7be7421cf4a405abd2cd83e0a6bd0a58`, arm64 manifest `sha256:b530091383543737310fd0b3512123de3e3b5165f221f244d0171b858528d963`; Home Assistant `base:3.22` index `sha256:8f937e0b89e108f432d3cb140900f06e568f8f4fbdb7987472447f54d8b24495`, amd64 manifest `sha256:f243712420b3493e9c23d821e4a44c1a1c152c294a9242624de947041cf036cc`. The pinned amd64 development closure is Git `/usr/bin/git` SHA-256 `5b5cbd6facf5d86226063d69fe57064bc5ad79bdccee2af0ac787646c564a880`, loader `/lib/ld-musl-x86_64.so.1` SHA-256 `1cc5c3f502179e2a4befae29658fb109102effefc75742893aa4a449fd3fbb03`, PCRE2 real target `/usr/lib/libpcre2-8.so.0.14.0` SHA-256 `0eae946d1f2746b6c64cc8beb9230360dc935e8552f89b765c7e697bff232345`, and zlib real target `/usr/lib/libz.so.1.3.1` SHA-256 `09b1bbd6ffe274039cefaca595f55cec0af65fe90d9e285e5d57ff7ed96948d2`; the `/usr/lib/libz.so.1` symlink is never passed to the no-follow broker. Direct arm64 execution currently fails with `exec format error`, so native and emulated arm64 runtime validation remain `UNVERIFIED`. Packaged amd64 development image `ha-engineering-mcp:g2-amd64-candidate` resolves to `sha256:e71aec0b2b6d8b76bbeb751b1b0623325509d9665f62c6ebff885f995ae4dc03` (`linux/amd64`, command `/run.sh`, no `Config.Labels`); helper modes are root:root `0555` with SHA-256 `openat2-read` `59faab9a79575409e59b3672cb1ecb50a9f3b3a7d0db85f1065d499a3c7c425f`, `openat2-list` `6fe9587146b927b6f84c53a3d61efd87e6143c9ee95268b9c997d464260bab51`, and `git-broker` `01823637f02c49e685f84a2b371870945299e772b6dc37dbf9194b2f34f051f8`; packaged runtime closure is Git `/usr/bin/git` SHA-256 `5b5cbd6facf5d86226063d69fe57064bc5ad79bdccee2af0ac787646c564a880`, loader `/lib/ld-musl-x86_64.so.1` SHA-256 `7d221f4e17e8f7ebfc208d6e621bb7fc71bc99081bed47409d77048d9a69dbd5`, PCRE2 real target `/usr/lib/libpcre2-8.so.0.14.0` SHA-256 `0eae946d1f2746b6c64cc8beb9230360dc935e8552f89b765c7e697bff232345`, and zlib real target `/usr/lib/libz.so.1.3.1` SHA-256 `09b1bbd6ffe274039cefaca595f55cec0af65fe90d9e285e5d57ff7ed96948d2`; the preserved prior-context artifact `sha256:60d3d5d8fda4f2bee464e02cf99b6394cd787f62572a9a01934f100864c68cb1` (local tag `ha-engineering-mcp:g2-amd64-candidate-f94ddd1`) reran successfully after Docker restoration. The current working-tree add-on context based on `ce15a89` was then rebuilt on 2026-07-20 with both stages pinned to the amd64 Node manifest; Buildx record `4horxn7bmbgycgfhzlq2qnza0` produced OCI index `sha256:e71aec0b2b6d8b76bbeb751b1b0623325509d9665f62c6ebff885f995ae4dc03` and linux/amd64 manifest `sha256:47fe0079da3266f9d7819af7b60783bba09758156e6813d0da0b5183c1de279c`. The rebuilt artifact passed all 8/8 strict rows, including the packaged 74/74 Git protocol matrix, with unchanged frozen helper and runtime hashes; local NDJSON evidence is `.tmp/g2-candidate-image-results-amd64-current.ndjson` (SHA-256 `3319e8647e830076c468380d86d986ff791b00b27b7f1ed9ef6f40c5ce2cf468`). The amd64 Node-base offline startup probe is development-only and not HA-base authority because `bashio` is absent; `/bin/sh /run.sh` fails closed with invalid environment configuration.

Behavioural matrix: Windows-neutral contract versus Linux/amd64 development versus native aarch64 authority; source/build/final-image and packaged/missing/wrong-mode helpers; supported/unsupported `openat2`; regular/final-link/parent-link/magic-link/hard-link/mount-crossing/nonregular/root-swap/same-size-race topology; stable/drifted one-pass/two-pass catalogs; clean/dirty and born/unborn/detached SHA-1/SHA-256 Git with hostile config/topology; required read/execute versus denied network/unrelated exec/repository or unrelated write and every rlimit; success/crash/malformed/overflow/cancellation/deadline/kill helper lifecycle; proposal/journal/audit/quarantine open/write/short-write/fsync/rename/dirsync/close/permission/`ENOSPC` and process-kill checkpoints with immediate/first-restart/second-restart evidence; inert candidate versus shipped Phase 1 behavior. Registration, mounts, versioning, release, deployment, and live Home Assistant access remain absent.

## R1-001 Supervisor packaging repair contract

Objective: release add-on `0.1.7` with Supervisor-compatible immutable packaging after `0.1.6` rejected `addon/build.yaml` digest fields, fell back to reserved `BUILD_FROM=ghcr.io/home-assistant/base:latest`, and failed Dockerfile APK pins.

Risk: `HIGH`, because this repairs installer/package metadata and container base selection for the Home Assistant Supervisor add-on path.

Acceptance evidence:

| ID     | Requirement                                                                                                                                                           | Implementation evidence                                                                                                        | Validation evidence                                                                                  |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| R1-001 | `addon/build.yaml` is absent so Supervisor cannot reject digest-bearing `build_from` schema fields or inject reserved `BUILD_FROM` fallback behavior.                 | Delete `addon/build.yaml`; tests assert the file is absent.                                                                    | Focused add-on test and `git diff --check`.                                                          |
| R1-002 | The Dockerfile owns the final base image through private `HA_BASE_FROM`, retaining the pinned aarch64 HA base manifest digest and no reserved `BUILD_FROM` arg usage. | `addon/Dockerfile` uses `ARG HA_BASE_FROM=ghcr.io/home-assistant/aarch64-base:3.22@sha256:b530...` and `FROM ${HA_BASE_FROM}`. | Tests assert pinned `HA_BASE_FROM`, no `ARG BUILD_FROM`, and no `FROM ${BUILD_FROM}`.                |
| R1-003 | Build-stage Node input and amd64 development override remain immutable while runtime package pins remain unchanged.                                                   | Dockerfile keeps Node multiarch index, explicit amd64 override comment, and existing `apk add` package pins.                   | Tests assert Node index, amd64 override, HA base digest, and APK pin strings.                        |
| R1-004 | Release metadata records the packaging repair as `0.1.7` without adding mounts, privileges, helper wiring, release, deployment, commit, push, or live HA access.      | `addon/config.yaml` version `0.1.7`; changelog prepended; existing least-privilege config retained.                            | Focused add-on test asserts version, `arch: [aarch64]`, `map: []`, and prohibited privilege strings. |

Behavioural matrix:

| Row                       | Expected state                                                                                                                                                 | Evidence status                                |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| Windows-neutral contract  | Static tests prove metadata/Dockerfile contracts and absence of deprecated build manifest without claiming native Linux execution.                             | Planned focused test                           |
| Linux/amd64 development   | Dockerfile keeps explicit pinned amd64 Node override and private HA base override guidance for local development builds.                                       | Planned focused test                           |
| Supervisor schema path    | No `addon/build.yaml` is present, avoiding the rejected digest-bearing `build_from` schema path.                                                               | Planned focused test                           |
| Native aarch64 Supervisor | User-approved push/live Supervisor add-on build must prove the pinned final aarch64 base, APK pins, install, startup, and health behavior on HA OS/Supervisor. | `UNVERIFIED` until user-approved push/live run |

Scope: only `docs/requirements-ledger.md`, `addon/Dockerfile`, `addon/config.yaml`, `addon/CHANGELOG.md`, `tests/addon.test.ts`, and deletion of deprecated `addon/build.yaml`.

Validation: focused `pnpm vitest run tests/addon.test.ts`, formatting for changed files or repository format check where practical, `git diff --check`, and final diff inspection. Authoritative `pnpm verify` remains desired repository validation. Native Supervisor operational validation remains `UNVERIFIED` until the user approves push/live installation.

Prohibited actions: no commit, push, deploy, release, HA access, production mutation, destructive Git, privilege expansion, `/homeassistant` mapping, helper wiring, or dependency changes.

## Phase 3A guarded apply core acceptance ledger

Risk: HIGH. Phase 3A introduces the internal guarded-application state machine that will eventually protect live writes, but this slice remains unregistered, adapter-neutral, and disabled by default with no real live adapters or grant producer.

| ID      | Observable requirement                                                                                                                                                                                                              | Implementation evidence                                                                            | Validation evidence                                                                                              |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| P3A-001 | Strict transaction schemas define the exact states, legal transitions, CAS/version journal contract, commit point, recovery table, and terminal outcomes.                                                                           | `docs/phase3-contracts.md` and `src/phase3/contracts.ts`.                                          | `tests/phase3Contracts.test.ts`.                                                                                 |
| P3A-002 | Injected-only, atomic, single-use apply grants bind exact proposal storage, candidate, diff, risk, issued/expires, pending state, and operation; not-yet-valid, expiry, replay, wrong binding, drift, and cancellation fail closed. | `src/phase3/approval.ts`; no persistence, producer, or CLI.                                        | `tests/phase3Approval.test.ts`.                                                                                  |
| P3A-003 | Coordinator runs policy before lock, canonical locking, policy/identity recheck, source digest, local validation, approval consume, checkpoint, intent journal, atomic apply, validation, narrow reload, and verification.          | `src/phase3/applyCoordinator.ts` with injected ports and default-closed `GuardedPhase3PolicyPort`. | `tests/phase3ApplyCoordinator.test.ts`.                                                                          |
| P3A-004 | Every post-commit failure carries the latest durable record, writes rollback intent, verifies checkpoint integrity, restores checkpoint, validates and verifies rollback, or requires manual recovery without implicit restart.     | `Phase3ApplyCoordinator.rollbackAfterCommit`.                                                      | `tests/phase3ApplyCoordinator.test.ts` and `tests/phase3Recovery.test.ts`.                                       |
| P3A-005 | Precommit cancellation has no live effect; postcommit caller cancellation is ignored while internal verification or rollback finishes.                                                                                              | Approval/lock precommit signal checks and coordinator internal postcommit context.                 | `tests/phase3Approval.test.ts`, `tests/phase3ResourceLocks.test.ts`, and `tests/phase3ApplyCoordinator.test.ts`. |
| P3A-006 | Bounded keyed locks serialize same path, allow distinct paths, and clean cancelled/deadline waiters.                                                                                                                                | `src/phase3/resourceLocks.ts`.                                                                     | `tests/phase3ResourceLocks.test.ts`.                                                                             |
| P3A-007 | Startup recovery never reapplies candidate and returns explicit per-record recovery dispositions for digest-driven rollback, finish, manual attention, or terminal no-transition inspection for every state.                        | `Phase3ApplyCoordinator.recover` and `phase3RecoveryTable`.                                        | `tests/phase3Recovery.test.ts`.                                                                                  |
| P3A-008 | No MCP, registry, config, CLI, add-on runtime, version, mapping, deployment, or live-write enablement changes.                                                                                                                      | Phase 3A files are an unregistered source island; `phase3Contract.writesEnabled=false`.            | `tests/phase3Isolation.test.ts`; diff inspection.                                                                |
| P3A-009 | Root and add-on source mirrors are exact.                                                                                                                                                                                           | `src/phase3/*.ts` mirrored to `addon/app/src/phase3/*.ts`.                                         | `tests/phase3Isolation.test.ts` and `pnpm addon:context` through `pnpm verify`.                                  |
| P3A-010 | Exhaustive focused tests and full validation cover contracts, approval, locks, coordinator, recovery, and isolation.                                                                                                                | Six focused Phase 3A Vitest files plus repository validation.                                      | Focused Phase 3A Vitest command, `pnpm verify`, `git diff --check`, and final diff inspection.                   |

Behavioural matrix: pending/discarded/expired proposals; exact versus drifted proposal storage, candidate, and diff digests; grant valid/not-yet-valid/replayed/expired/wrong-risk/wrong-operation/wrong proposal/cancelled; writes disabled, missing capability, restart-required, domain reload supported/unsupported, no-impact; same path versus distinct path locking, cancelled waiters, deadline waiters, waiter overflow, invalid path; precommit source drift, candidate drift, identity drift, validation failure; apply committed, before-commit, and commit-unknown; post-validation/reload/verification failure; rollback success/failure/manual; caller cancellation before and after commit; startup recovery for every state with candidate, checkpoint/expected, unknown, and missing digest; root and add-on mirror. Phase 3A does not register tools, mutate Home Assistant, restart, commit, push, deploy, add mappings, change versions, or enable writes.

## Phase 3B protected proposal input acceptance ledger

Risk: HIGH. Phase 3B bridges protected proposal persistence into the Phase 3 core, but remains read-only, unregistered, and disconnected from every live-effect port.

| ID      | Observable requirement                                                                                                                                                                                  | Implementation evidence                               | Validation evidence                                                                                      |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| P3B-001 | Exact proposal lookup is bounded and non-mutating, validates canonical UUID filenames and existing protected-file/schema/integrity guarantees, and zeros raw bytes.                                     | ProtectedProposalStore.readExact.                     | tests/phase3ProposalAdapter.test.ts exact, missing, tamper, traversal, and directory non-mutation cases. |
| P3B-002 | Phase 2 public proposal identity maps exactly to the Phase 3 snapshot, preserving state and mapping reloadImpact to impact.                                                                             | ProtectedPhase3ProposalAdapter.load.                  | Pending/discarded/expired and all impact mapping tests.                                                  |
| P3B-003 | Candidate loading revalidates public/protected identities, canonical base64, strict UTF-8, candidate digest, and exact diff digest, returning independent caller-owned bytes while zeroing temporaries. | ProtectedPhase3ProposalAdapter.loadCandidate.         | Identity, encoding, digest, repeated-load, and caller-zeroization tests.                                 |
| P3B-004 | The adapter remains an inert source island with exact root/add-on mirrors and no runtime, registry, CLI, config, package, mapping, or write activation.                                                 | Root/add-on source mirrors and unchanged composition. | tests/phase3Isolation.test.ts, pnpm addon:context, and diff inspection.                                  |

Behavioural matrix: pending/discarded/expired; none/domain-reload/restart-required impact; exact/missing/wrong proposal identity; public/protected idempotency, candidate, and diff agreement/drift; canonical/noncanonical base64; valid/invalid UTF-8; valid/tampered storage envelope; repeated candidate loads and caller zeroization; exact-read success/failure without scan or quarantine; canonical UUID/traversal; root/add-on mirrors; runtime import absent.

Deferred to Phase 3C or later: crash-safe cross-process transaction journal CAS, immutable checkpoints, source and atomic-write adapters, validation/reload/verification adapters, approval production, registration, activation, packaging, and native Linux operational evidence for any new filesystem primitive.

## Slice H Phase 2 add-on activation contract

Objective: activate the 11 frozen Phase 2 repository/proposal tools only for the managed add-on after all local startup gates pass, while preserving the 15 Phase 1 tools and all mutation prohibitions.

Risk: `HIGH`, because this registers public MCP contracts, gates protected repository access, persists cursor/proposal state, and changes add-on packaging.

Slice H supersedes the historical G2 inertness invariants (`map: []`, no helper wiring, and `registered: false`) while retaining every no-mutation and least-privilege guarantee.

Acceptance evidence:

| ID    | Requirement                                                                                                                                                                                | Implementation evidence                                                                                   | Validation evidence                                                                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| H-001 | Phase 2 registers exactly the existing `phase2ToolNames` alongside the 15 Phase 1 tools only in add-on mode with `enable_phase2` true by default. `false` or local mode is Phase1-only.    | Shared registry/composite registry, `Phase2Tools`, config `enablePhase2`, and add-on option wiring.       | Registry inventory tests for enabled, disabled, local, and gate-failure paths.                            |
| H-002 | Any activation gate failure leaves the process healthy with Phase 1 only; diagnostics are stable closed enum outcomes and never expose exception causes.                                   | Add-on activation gate catches fixed artifact, key, proposal-store, secrets, catalog, and service checks. | Failing-gate and error-envelope tests.                                                                    |
| H-003 | Startup gates run in order: fixed artifact validation, protected persistent master key, proposal audit/store recovery, `secrets.yaml` strict scalar registration, catalog proof, register. | Injectable activation components with fixed `/homeassistant`, `/data`, and `/app/native` defaults.        | Unit tests for disabled/local/failing gates, key persistence/derivation, and strict secrets basics.       |
| H-004 | Phase 2 tools are add-on-only read/proposal metadata tools. They never mutate live config, reload, restart, apply, commit, push, or write Git state.                                       | Dispatcher calls repository/resource/Git read services and proposal metadata service only.                | Dispatch/schema tests plus retained Phase 1 fallback tests.                                               |
| H-005 | Add-on packaging moves to `0.2.0` and uses the official read-only `homeassistant_config` map only with no new privileges or RW mapping.                                                    | `addon/config.yaml`, `run.sh`, docs, changelog, and add-on mirror updates.                                | Updated add-on/config tests; live Supervisor validation remains `UNVERIFIED` until explicitly authorized. |

Behavioural matrix: add-on enabled success, add-on disabled, local mode, each init gate failure, fresh/valid/corrupt protected state, false-to-true rerun, 0.1.7 Phase1-only compatibility, HTTP and stdio dispatch cancellation/deadline, missing/hostile Git per-call failure, secrets/no-leak, proposal `/homeassistant` immutability, and unchanged Phase 1 audit/pairing/TLS behavior.

Validation: focused registry/activation/dispatcher/config/add-on tests, `pnpm typecheck`, `git diff --check`, and broader `pnpm verify`. The arm64 candidate image `sha256:57a705c22e9ed4b296ea434f3ea8b93c78669ffaabb5a130d964730b6f1614ce` passed 7/8 strict packaging rows under Docker Desktop emulation: metadata and exact HA labels, helper paths/hashes, runtime linkage, the exact expected HA-base set-id helper, writable directories, and offline startup. The Git protocol matrix remains `UNVERIFIED` because its sandbox/rlimit process execution fails under amd64-hosted arm64 emulation; G2-016 prohibits treating that as native evidence. Native aarch64, live Supervisor install, and live Home Assistant repository access remain `UNVERIFIED` until the approved deployment test.

Prohibited actions: no commit, push, deploy, release, live HA access, live config mutation, reload, restart, Git write, privilege expansion, RW mapping, or destructive operation.

## Constraints and assumptions

- TypeScript/Node.js with one registry exposed through local stdio and the Phase 1 add-on's TLS-only Streamable HTTP transport; the optional bridge preserves stdio-only Codex clients.
- The HA add-on/aarch64 image is the authoritative runtime. Platform-neutral core/bridge logic remains portable to Linux/Windows/macOS where supported.
- Credentials come only from environment/external secret storage. Token values are never accepted as MCP tool arguments.
- Direct filesystem access is a negotiated capability, not an assumption.
- API-managed resources and YAML-managed resources require separate repository adapters.
- `secrets.yaml` and configured/renamed secret-source files are categorically content-inaccessible through MCP. Their canonical identities, aliases, includes, and symlinks are tracked so alternate paths cannot bypass the rule.
- The actual target is Home Assistant OS 18.1 on Raspberry Pi 5 (`aarch64`), Core 2026.7.2, Supervisor 2026.06.2, with host configuration /config exposed to the add-on only through the official read-only homeassistant_config mapping at /homeassistant, and storage-mode dashboards.
- Home Assistant OS is not treated as a general-purpose Node host. The server is packaged as a least-privilege managed add-on; no direct OS access is required.
- The repository's authoritative deterministic validation command is `pnpm verify`.

## Behavioural matrix

| Dimension           | Required paths                                                                                                                                                             |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Policy              | reads enabled; writes disabled/enabled; restart disabled/enabled; deletes disabled and explicit approval if ever enabled                                                   |
| Identity            | authorized; missing/expired/invalid token; insufficient HA privileges; forged/replayed/expired out-of-band approval                                                        |
| Data size           | empty; single; multiple; paginated/truncated; oversized rejection                                                                                                          |
| Filesystem          | valid relative path; traversal; absolute outside root; symlink escape; changed inode/hash; unsupported/binary encoding                                                     |
| Proposal            | valid; expired; stale source; already applied/discarded; approval absent/mismatched; concurrent proposals touching same resource                                           |
| Apply               | success; local validation failure; HA validation failure; atomic write failure; reload failure; verification failure; rollback success/failure; crash recovery             |
| API                 | success; 4xx; 5xx; timeout; safe-read retry; mutation no-retry; WebSocket disconnect/reconnect/correlation                                                                 |
| Git                 | clean; scoped dirty; unrelated dirty; conflict; no repository; detached branch; checkpoint/final commit failure                                                            |
| Deployment          | HA OS 18.1 on aarch64 as first live target; OS/Supervised/Container/Core adapter contracts; capability unavailable; clean install vs existing config                       |
| Dashboard storage   | storage-mode discovery/read via supported API; mutation capability unavailable unless a supported API is verified; YAML-mode fixtures/adapters remain separate             |
| Platform            | HA add-on/aarch64 authoritative; amd64/Linux container for development and clean-room validation; Windows/macOS coverage for the optional bridge and platform-neutral core |
| Resource management | YAML storage; UI/API storage; include graph; resource missing/renamed/disabled/active                                                                                      |

Cancellation is relevant for CLI/MCP requests before side effects; once an apply transaction crosses its commit point it must finish verification or rollback rather than abandon state. Backward compatibility begins once Phase 1 tool schemas ship.

## Validation plan

Targeted: schema, policy, redaction, path, YAML, proposal, API, Git, and transaction tests. Full validation: format check, lint, `tsc --noEmit`, all non-E2E tests, package/build check, MCP smoke test, and security-focused suite. Environment: Ubuntu supported-adapter diagnostics and Docker HA E2E where practical. Clean room: required from Phase 1 onward because this is high-risk and cross-platform. The user authorized and completed the named Phase 1 read-only diagnostics recorded in [deployment.md](deployment.md). Deployed 0.1.4 recent-error and malformed-cursor checks failed historically; installed 0.1.5 repaired both paths and passed the complete live acceptance matrix against Core 2026.7.2. Phase 1 live validation is `PASSED`. Mutations remain prohibited.

## Required human approvals

1. Approve package/base-image resolution required for the add-on and bridge implementation (the user's “let's start” authorizes Phase 1 dependency resolution, not deployment).
2. Pair each local bridge/client through ingress and verify the TLS fingerprint during installation; public exposure remains prohibited.
3. Approve enabling writes after read-only phases and backups are verified.
4. Approve each high-risk/destructive proposal through the operator-controlled channel with matching proposal ID/diff digest; MCP-supplied identity fields are never sufficient.
5. Separately request any Git commit; pushing remains outside default scope.

## Prohibited actions

No production mutation, unrestricted shell, arbitrary file write, unrestricted HA service call, token exposure, commit, push, merge, rebase, history rewrite, publish, deploy, deletion, or restart during planning and initial development.
