# Requirements ledger

Status: Phase 1 validation contract, updated 2026-07-15

## Objective

Build a standalone, production-quality TypeScript MCP server that complements Home Assistant's official MCP server by providing bounded, auditable configuration inspection and a staged, validated, reversible mutation workflow without exposing generic shell, arbitrary file writes, secrets, or unrestricted service calls.

## Risk

`HIGH`. The project crosses authentication, authorization, filesystem, Git, process execution, smart-home availability, secret handling, public MCP contracts, and cross-platform deployment boundaries. Production mutation is explicitly outside the initial development phase.

## Acceptance criteria

| ID     | Observable requirement                                                                                                                                                                                                                                                                                                                                                                           | Planned implementation evidence                                                                                                                               | Planned validation evidence                                                                                                                                                                                                                                                                                                                     |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-001 | Codex connects using distinct `ha_*` engineering tools through local stdio and the HA add-on's authenticated Streamable HTTP endpoint; an optional local stdio bridge preserves stdio-only clients.                                                                                                                                                                                              | Shared tool registry with stdio, HTTP, and bridge transport adapters plus Codex examples.                                                                     | MCP client integration tests list/invoke identical tools over both transports and through the bridge.                                                                                                                                                                                                                                           |
| AC-002 | REST and WebSocket clients provide authenticated, typed, timed-out read access without leaking credentials through URLs, redirects, proxies, or cross-origin forwarding.                                                                                                                                                                                                                         | Separate clients with validated responses, strict endpoint policy, bounded retries for idempotent reads, and WebSocket reconnect policy.                      | Mock HTTP/WebSocket integration tests for success, 401, timeout, disconnect/reconnect, rejected URL userinfo/scheme, redirects, and cross-origin credential non-forwarding.                                                                                                                                                                     |
| AC-003 | Read-only tools inspect system information, entities, automations, scripts, helpers, dashboards, scenes, blueprints, config, errors, Git, and pending diffs with filtering/pagination.                                                                                                                                                                                                           | Read services and bounded result contracts.                                                                                                                   | Unit and integration tests for empty/single/multiple and truncation paths.                                                                                                                                                                                                                                                                      |
| AC-004 | Configuration access is confined to configured roots and safe text formats.                                                                                                                                                                                                                                                                                                                      | Canonical-path policy rejects traversal, out-of-root paths, symlink escapes, disallowed symlink writes, oversized/binary/unsupported files.                   | Temporary-filesystem adversarial tests on Ubuntu and Windows.                                                                                                                                                                                                                                                                                   |
| AC-005 | Home Assistant YAML tags and split includes are handled without resolving `!secret`; structured edits preserve comments/order and avoid unrelated reformatting where feasible.                                                                                                                                                                                                                   | Round-trip YAML document model and include graph scanner.                                                                                                     | Fixtures for all requested include tags, duplicate keys, comments, anchors, and secrets.                                                                                                                                                                                                                                                        |
| AC-006 | Every write begins as an immutable, expiring proposal containing all requested metadata, content hashes, exact diff, validation/risk/reload information, and redacted output.                                                                                                                                                                                                                    | Proposal repository and state machine.                                                                                                                        | Hash, expiry, stale proposal, tampering, redaction, and serialization tests.                                                                                                                                                                                                                                                                    |
| AC-007 | Applying a proposal rechecks hashes and approval, creates a recoverable checkpoint, validates, applies atomically, revalidates, reloads the narrowest domain, verifies, and automatically restores on failure.                                                                                                                                                                                   | Transaction coordinator with injected filesystem, validator, reload, verification, backup, and Git ports.                                                     | Full success and failure workflow tests, including crash/failure injection at every state transition.                                                                                                                                                                                                                                           |
| AC-008 | Writes, restarts, and deletes default disabled; high-risk/destructive actions require explicit approval metadata; deletion is unavailable by default.                                                                                                                                                                                                                                            | Central policy engine evaluated before side effects.                                                                                                          | Authorization matrix and configuration-default tests.                                                                                                                                                                                                                                                                                           |
| AC-009 | Service calls are explicit allowlisted operations; no generic service-call or shell/write tool exists.                                                                                                                                                                                                                                                                                           | Capability registry and deployment command allowlist.                                                                                                         | MCP tool inventory snapshot and policy rejection tests.                                                                                                                                                                                                                                                                                         |
| AC-010 | Validation has local, deployment-aware Home Assistant, and post-apply levels with machine and human summaries.                                                                                                                                                                                                                                                                                   | Validator composition and deployment adapters.                                                                                                                | Parser, check-config result, timeout, unavailable-adapter, and post-apply tests.                                                                                                                                                                                                                                                                |
| AC-011 | Reloads prefer affected domains; restart is separate, disabled by default, and never used merely to obtain validation.                                                                                                                                                                                                                                                                           | Change-impact mapper and reload/restart policy.                                                                                                               | Mapping tests and restart-denial tests.                                                                                                                                                                                                                                                                                                         |
| AC-012 | Git operations are scoped to proposal files, detect unrelated changes/conflicts, avoid history rewriting and remote pushes, and commit only through separately authorized `ha_commit_change`; apply checkpoints are non-commit backups/journals.                                                                                                                                                 | Git adapter using argument arrays/scoped pathspecs plus separate backup store.                                                                                | Temporary-repository tests for dirty trees, conflicts, scoped commits, rollback, no apply-time commits, and no remote operations.                                                                                                                                                                                                               |
| AC-013 | Every tool call is durably represented by a redacted JSONL audit record; if the audit sink is unavailable, the call fails before protected data or side effects are returned.                                                                                                                                                                                                                    | Fail-closed audit middleware, startup health check, serialized append sink, and safe rotation contract.                                                       | Schema, redaction, partial-write recovery, concurrency, injection, permissions, disk/write failure, correlation, and rotation tests.                                                                                                                                                                                                            |
| AC-014 | Structured automation/script/helper/dashboard/scene tools generate proposals and cannot bypass workflow.                                                                                                                                                                                                                                                                                         | Domain builders call the same proposal service.                                                                                                               | Contract tests proving no direct write path.                                                                                                                                                                                                                                                                                                    |
| AC-015 | Deployment-specific validation, reload, restart, and config access are isolated for OS, Supervised, Container, and Core; first supported target is selected only after environment discovery.                                                                                                                                                                                                    | Deployment adapter interfaces and capability negotiation.                                                                                                     | Adapter contract tests plus supported-target integration/E2E tests.                                                                                                                                                                                                                                                                             |
| AC-016 | CLI provides `doctor`, `check-auth`, `check-config-path`, `check-git`, `validate`, `list-capabilities`, and `show-policy` without disclosing secrets.                                                                                                                                                                                                                                            | CLI composition over the same application services.                                                                                                           | CLI exit-code, output, redaction, and unavailable-capability tests.                                                                                                                                                                                                                                                                             |
| AC-017 | Configured secret-source files are metadata-only and excluded from reads/search/index/diff/Git-content/diagnostic paths; known exact secret values and credential patterns are redacted from all outputs, while raw HA logs are never returned by default.                                                                                                                                       | Secret-source registry, canonical identity checks, exact-value redactor held only in memory, heuristic credential redactor, and safe error summarizer.        | Direct/alias/symlink read and search denials plus canary tests across diffs, logs, responses, audit, diagnostics, and Git output.                                                                                                                                                                                                               |
| AC-022 | High-risk approval cannot be satisfied by MCP-client assertions; it uses a separate operator-controlled out-of-band channel and binds one-time authorization to proposal ID and diff digest.                                                                                                                                                                                                     | Phase 3 approval port, initially local CLI confirmation producing a short-lived single-use server-side grant.                                                 | Forgery, replay, expiry, restart, wrong digest/proposal, and cancellation tests.                                                                                                                                                                                                                                                                |
| AC-023 | The project installs as a custom Home Assistant add-on on HA OS/aarch64 without `/config` in Phase 1, host access, privileged mode, Docker socket, general Supervisor API, or public exposure; all mutation options default false.                                                                                                                                                               | Add-on repository metadata, container, minimal permissions, schema/options, health/start scripts, and ingress diagnostics/setup.                              | Metadata/schema validation, aarch64 container build, startup/health tests, manifest capability inspection, and absence-of-mount tests.                                                                                                                                                                                                          |
| AC-024 | The disabled-by-default HTTPS transport uses generated/pinned TLS plus distinct paired client credentials and enforces secure ingress bootstrap, hashed secrets, per-client rotation/revocation, Host/Origin/proxy, body/output, session ownership, timeout, rate/concurrency, topology, and exposure policies; neither credential leaves its trust boundary or crosses plaintext LAN transport. | Separate ingress/HTTPS listeners, certificate/client credential stores under `/data`, HTTP security middleware, and bridge OS credential/pinning integration. | Startup-default, TLS generation/storage/fingerprint/rotation, entropy/hash/constant-time, unauthorized/query credential, non-loopback plaintext refusal, Host/Origin/DNS-rebinding/forwarded-header, oversized, replay/cross-client/session expiry, rotation, concurrency/overload, logging-canary, topology, MITM, and identity-pinning tests. |
| AC-025 | In add-on mode the HA client uses only the Supervisor-injected runtime token against the fixed Home Assistant Core API proxy and never accepts arbitrary origins/paths or exposes/persists the token.                                                                                                                                                                                            | `homeassistant_api: true` manifest plus fixed-origin/path HA client credential provider.                                                                      | Manifest denial assertions, fixed-origin/path tests, 401/403 redaction, and token canaries across options, diagnostics, audit, logs, MCP and bridge traffic.                                                                                                                                                                                    |
| AC-018 | Documentation covers architecture, security, deployment types, setup, tools, least privilege, backup, mutation kill switch, audit rotation, and all requested recovery cases.                                                                                                                                                                                                                    | Required documentation set.                                                                                                                                   | Documentation link/command/example review.                                                                                                                                                                                                                                                                                                      |
| AC-019 | Tests are layered: unit, mocked API integration, temporary filesystem/Git, complete workflow, rollback, and practical Docker E2E without production access.                                                                                                                                                                                                                                      | Test suites and fixtures.                                                                                                                                     | Phase gates and clean-room validation.                                                                                                                                                                                                                                                                                                          |
| AC-020 | Each phase runs formatting, linting, type checking, relevant tests, and security tests; critical safety failure stops progress.                                                                                                                                                                                                                                                                  | Package scripts and CI phase gates.                                                                                                                           | Exact command evidence and clean-room runs.                                                                                                                                                                                                                                                                                                     |
| AC-021 | No live Home Assistant change occurs without explicit approval of the exact change; no commit/push/deploy occurs unless separately requested.                                                                                                                                                                                                                                                    | Operational policy and human gates.                                                                                                                           | Review of audit trail and prohibited-action checks.                                                                                                                                                                                                                                                                                             |

## Phase 2 Slice B acceptance ledger

| ID    | Observable requirement                                                                                                                                                                        | Implementation evidence                                                                              | Validation evidence                                                                                                                    |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| B-001 | Linux file content uses authoritative beneath-root openat2 confinement; Windows, missing helper, unsupported kernel, or unsafe packaging fails unavailable with no Node race-safety fallback. | Native helper source and NativeOpenat2Reader protocol boundary.                                      | Protocol/unit tests now; Linux active-race and packaged-image checks remain UNVERIFIED until Slice G.                                  |
| B-002 | Only strict relative paths and existing bounded regular strict-UTF-8 non-NUL files beneath /homeassistant are eligible.                                                                       | Frozen path schema, helper path validation, resolution flags, descriptor metadata and output bounds. | Invalid-path/protocol/encoding tests; native filesystem adversarial suite in Slice G.                                                  |
| B-003 | secrets.yaml and configured sources are metadata-only by device/inode; hard-link aliases are denied and registration failures latch unhealthy.                                                | ProtectedIdentityRegistry with bounded staged identity registration.                                 | Direct/alias/readiness/duplicate-path/duplicate-identity/failure-latch tests.                                                          |
| B-004 | Exact known values plus heuristic shapes are removed from every nested output class; content is unavailable until a safe value provider succeeds.                                             | SecretValueProvider readiness gate and ExactSecretRedactor.                                          | Canary tests across response/error/snippet/diff/diagnostic/Git-shaped data plus bounds/cycles/cancellation.                            |
| B-005 | Helper work and redaction honor deadlines/cancellation, bounded concurrency/queues/output, child termination, byte cleanup, and stale-result isolation.                                       | BoundedPermitPool, child lifecycle, post-read operation checks, staged registry commit.              | Queue cancellation/deadline/handoff, byte-zeroing, malformed/oversized output, root identity tests; live child termination in Slice G. |

Behavioural matrix: Linux helper present/absent/unsupported; Windows unavailable; valid/invalid/symlink/hard-link/cross-mount paths; empty/small/oversized/binary/NUL files; protected registry unready/ready/failed; exact provider success/failure; immediate/queued/cancelled/deadline helper work; unchanged/changed root and file identity; development boundary versus packaged add-on. macOS is unavailable like Windows for this repository capability. No tool registration, mount, version, release, deployment, or live Home Assistant access is part of Slice B.

## Scope

In scope: the standalone engineering MCP server, read-only HA API integration, bounded config repository access, proposal lifecycle, validation/reload/verification/rollback, scoped Git, audit, CLI, adapters, tests, and documentation.

Out of scope for initial delivery: replacing the official Home Assistant MCP server, arbitrary shell/file/service access, remote Git push, cloud deployment, production configuration mutation, delete tools enabled by default, token management UI, and supporting undocumented Home Assistant internals without a versioned adapter and fallback.

Public contracts: MCP tool names/input/output schemas; environment variable names; proposal/audit schemas; CLI commands/exit codes; deployment adapter capability model.

## Phase 2 Slice C acceptance ledger

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                              | Implementation evidence                                                                                     | Validation evidence                                                                                                                             |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| C-001 | YAML validation uses exact direct dependency `yaml@2.9.0`, the strict YAML 1.2 core parser profile, and remains unregistered/capability unavailable.                                                                                                                                                                                                                                | Root/add-on package and lock mirrors; `strictYamlGate.ts`; no tool-registry or add-on manifest change.      | Add-on mirror check, dependency lock inspection, full repository validation.                                                                    |
| C-002 | A synchronous immutable byte snapshot no larger than 512 KiB is hashed, fatal-UTF-8 decoded, and incrementally parsed without mutating or reserializing caller bytes; BOM, NUL, bare CR, mixed endings, directives, and multiple documents fail closed.                                                                                                                             | Snapshot/final wipe, preflight, 4096-code-unit surrogate-safe Parser + Composer loop.                       | Empty/explicit-empty, LF/CRLF, digest/nonmutation, invalid byte/directive/document, exact byte/multibyte/surrogate fixtures.                    |
| C-003 | Only scalar Home Assistant include/input/secret tags are accepted; raw references are bounded to 100, 512 bytes each, and 32 KiB aggregate, while secret names are represented only by digest and byte length.                                                                                                                                                                      | Explicit scalar custom-tag profile and bounded reference metadata.                                          | Every allowed tag, unknown/known-denied/collection tag, N/N+1, aggregate N/N+1, and secret canary tests.                                        |
| C-004 | Keys are untagged, unanchored primitive scalars with typed canonical identity; duplicate lexical equivalents, non-finite values, complex keys, and `<<` merge syntax are rejected.                                                                                                                                                                                                  | Structural map/pair/key validation independent of JS object conversion.                                     | Block/flow, string-vs-number, duplicate numeric/boolean/-0, tagged/anchored/collection/nonfinite/merge fixtures.                                |
| C-005 | Syntax depth is at most 64; structural nodes and separately alias-expanded nodes are each at most 100,000; aliases are acyclic, resolved, unique-anchor, at most 100 total and at most 100 per target.                                                                                                                                                                              | Structural and expansion traversals with resolved-node identity stacks and fanout accounting.               | Depth/node/reference inclusive N and N+1 fixtures, duplicate/unresolved/cyclic anchors, and expansion bomb.                                     |
| C-006 | Cancellation and a projected monotonic deadline are checked before parsing, around every incremental chunk, after each event-loop yield, at validation phase boundaries, and at least every 256 structural/expanded/reference work units; diagnostics expose only stable allowlisted code plus numeric line/column, and unexpected library failures become safe `internal_failure`. | Internal operation clock, `setImmediate` yields, bounded validation polling, and sanitized `YamlGateError`. | Pre/mid-parse and post-parse structural/expanded cancellation/deadline tests plus unexpected-error canary and bounded diagnostic surface tests. |
| C-007 | Successful output is deeply frozen bounded metadata no larger than 32 KiB and contains no AST, content, parser diagnostics, or raw secret name.                                                                                                                                                                                                                                     | Frozen digest/count/reference metadata and serialized-size gate.                                            | Metadata freeze/size and secret non-disclosure assertions.                                                                                      |

Behavioural matrix: zero-byte, whitespace/comment-only, explicit-empty, and populated documents; LF/CRLF/none versus bare/mixed CR; ASCII/multibyte/surrogate boundaries; valid/invalid UTF-8 and BOM/NUL; block/flow and quoted/plain keys; every allowed/denied tag; no/safe/unresolved/duplicate/cyclic/fanout aliases; depth/node/reference exact boundary and N+1; pre/mid cancellation and deadline; development and mirrored add-on source. Slice C does not register tools, add the `/homeassistant` mount, package the native helper, bump a version, release, deploy, or access live Home Assistant.

## Phase 2 Slice D acceptance ledger

| ID    | Observable requirement                                                                                                                                                                                                                                                                               | Implementation evidence                                                                                                                                                          | Validation evidence                                                                                                                                                      |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D-001 | Protected sources are denied by canonical path before content is read and by device/inode identity after the secure read; metadata binds path, identity, and exact SHA-256. Replacement, edit, deletion, or alias drift permanently latches repository reads unhealthy.                              | Protected registry staged path/identity/digest metadata and freshness checks.                                                                                                    | Canonical pre-read, hard-link alias, replacement, same-inode edit, deletion, during-output, digest, and latch tests.                                                     |
| D-002 | Linux catalog discovery uses only the fixed-argument native openat2 helper, two exactly matching validated passes, bounded execution/output/concurrency, no shell, and no Windows fallback.                                                                                                          | Pinned O_PATH root, separate readable openat2 traversal descriptors, strict zero-reserved HALIST2 decoder, complete globally ordered directory/file metadata, and helper source. | Protocol corruption/bounds/capability tests and source assertions; Linux runtime remains UNVERIFIED until Slice G.                                                       |
| D-003 | The deterministic index is bounded to 2,000 YAML files, 16 MiB content, and 200,000 search lines/matches; protected paths are excluded before reads, catalog-root/file identities and size are verified, and snapshots canonically length-frame path, device, inode, and exact content SHA-256.      | Secure one-file-at-a-time indexing with byte wiping.                                                                                                                             | Ordering, hash, protected exclusion, drift, bounds, and sanitized failure tests.                                                                                         |
| D-004 | Cursors are canonical unpadded base64url of exactly 136 characters encoding a fixed 70-byte payload plus 32-byte HMAC; operation, query, snapshot, offset, and service key are bound.                                                                                                                | Fixed cursor codec and strict schemas.                                                                                                                                           | Canonicalization and invalid_input for malformed/auth/operation/query/rotation/closure; stale_source for authenticated snapshot/offset drift, without registry latching. |
| D-005 | Unregistered list/read/search are deterministic and bounded; reads redact exact and heuristic secrets without legacy 16 KiB truncation; linear KMP search drops matches hidden by full-line redaction, clips safely, and charges preprocessing/comparisons/lines/matches to one 200,000-unit budget. | Repository read service, full-text redaction, and scalar-safe clipping.                                                                                                          | List/read/search, long-file, redacted match, case, emoji, and cancellation tests.                                                                                        |
| D-006 | Cancellation/deadline work is polled at least every 256 units and unexpected failures become stable allowlisted errors.                                                                                                                                                                              | Operation checks, permit pool, helper termination, polling, and sanitizer.                                                                                                       | Long-line cancellation, unavailable helper, malformed protocol, and exception-canary tests.                                                                              |
| D-007 | Slice D remains unregistered with no mount, registry/config wiring, version bump, release, deployment, or live access.                                                                                                                                                                               | Repository diff and exact source mirror.                                                                                                                                         | Diff inspection and pnpm verify; Linux runtime remains gated by Slice G.                                                                                                 |

Behavioural matrix: Linux helper available/unavailable/malformed/timed-out/cancelled versus Windows unavailable; zero/single/multiple/2,000/N+1 files and 16 MiB/N+1 aggregate; canonical protected path, hard-link alias, replacement, same-inode mutation, deletion, and during-output drift; list/read/search success, pagination, stale/tampered/wrong-operation/wrong-query/rotated cursor; ASCII/multibyte/surrogate data; visible and hidden matches; cancellation/deadline; root and add-on mirror. Consistency is observed and fail-closed, not atomic.

## Phase 2 Slice E acceptance ledger

Risk: HIGH. Slice E projects untrusted YAML and recursively resolves includes across a race-sensitive protected filesystem boundary, while preserving secret-name non-disclosure and bounded cancellation.

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Implementation evidence                                                                                                                                                                                                                | Validation evidence                                                                                                                     |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| E-001 | Strict YAML validation has one private parse/validate primitive; fixed typed projection runs only after every Slice C check and before document release. Existing validateStrictYaml metadata remains unchanged.                                                                                                                                                                                                                                                                       | Shared strict primitive plus validateAndProjectYaml fixed selective IR.                                                                                                                                                                | Existing Slice C suite unchanged plus projection freeze/surface/no-AST/no-secret-name tests.                                            |
| E-002 | Ordinary projection starts at configuration.yaml and resolves all five include tags relative to the containing file using authoritative catalog files/directories, beneath-root normalization, protected path/identity exclusion, a charged preindexed recursive lowercase-.yaml directory view in exact UTF-8 order, type-specific list/named/merge rules where explicit []/{} are no-ops but no-document input is invalid, repeated-file charging, and expansion-stack cycle denial. | Include graph resolver over validated catalog and secure reader.                                                                                                                                                                       | All tags, shapes, ordering, nested relative, packages, empty, cycle, missing, escape, wrong-kind, protected alias, and duplicate tests. |
| E-003 | Per operation: at most 2,000 unique parsed files, 20,000 edges, include depth 64, 200,000 projection/expansion work, 200,000 retained IR nodes, 20,000 resources, 4 MiB retained strings, 16 MiB aggregate unique source bytes, and bounded IDs; cancellation/deadline polls at most every 256 units and owned file bytes are processed one at a time then wiped.                                                                                                                      | Shared projection budget across expansion and all extraction/finish traversals, unique parse cache, occurrence expansion accounting, charged construction of immutable type-summary and exact-key indexes, and secure read validation. | Inclusive N/N+1 and pre/mid/post cancellation fixtures without checked-in blobs.                                                        |
| E-004 | Root/package domain keys support exact domain or domain plus ASCII-space label; homeassistant.packages accepts inline or include forms and package values are maps. Automation/scene identities, scripts, and exact helper domains obey the frozen ID rules and duplicates fail across all provenance paths.                                                                                                                                                                           | Typed resource extraction from selective map/sequence IR.                                                                                                                                                                              | Root/label/include/package, valid/absent/empty/invalid/tagged/alias/anchor/scalar/collection ID and duplicate matrix tests.             |
| E-005 | Blueprint projection independently scans authoritative blueprints/{automation,script,template}/\*_/_.yaml, validates blueprint name/domain and emits extension-preserving domain-relative IDs; malformed or duplicate blueprints fail only blueprint projection and ordinary orphan YAML is excluded.                                                                                                                                                                                  | Blueprint projection entry point and catalog prefix selection.                                                                                                                                                                         | All three domains, nested path, wrong/missing shape/domain/name, duplicate, and ordinary-orphan tests.                                  |
| E-006 | Resource summaries bind ID/type/provenance/path and exact unredacted source SHA-256. Get returns the full source file only after masking every retained !secret scalar source span, then exact-plus-heuristic nontruncating redaction; secret names never appear.                                                                                                                                                                                                                      | Source-span IR, exact source cache, span masker, and protected redactor.                                                                                                                                                               | Plain/quoted/multiple/boundary/canary spans, shared-file IDs, exact SHA, 512 KiB, and output freshness tests.                           |
| E-007 | Public resource IDs are NFC, control-free, at most 256 Unicode scalars and 512 UTF-8 bytes. resource_not_found is returned only after a successful fresh projection has no exact type/ID.                                                                                                                                                                                                                                                                                              | Tight schema and repository boundary error code.                                                                                                                                                                                       | Input/output boundary and malformed-vs-missing projection classification tests.                                                         |
| E-008 | Resource-list cursors retain the 102-byte/136-character format using operation byte 3 and a domain-separated resource-type query slot; list/search op bytes stay 1/2. Projection snapshots bind rules version, root, sorted source path/identity/SHA, ordered include edges, and sorted resource ID/provenance.                                                                                                                                                                        | Extended cursor codec and canonical framed projection snapshot.                                                                                                                                                                        | Width/backward-byte/op/type/MAC/rotation/stale snapshot/offset tests.                                                                   |
| E-009 | Every secure read validates catalog root, file identity, size, and exact digest; protected freshness runs before graph/catalog access and after held output. Reachable malformed YAML/include/resource data fails with sanitized stable errors; blueprint failures are isolated to blueprint projection.                                                                                                                                                                               | Repository projection service and error sanitizer.                                                                                                                                                                                     | Root/file/content drift, protected replacement/alias, malformed reachable/orphan, blueprint isolation, and canary-error tests.          |
| E-010 | Slice E remains unregistered and produces no tool/application/config/Docker/build/version/mount/package/release/deploy/commit/push change.                                                                                                                                                                                                                                                                                                                                             | Diff scope and exact root/add-on source mirror.                                                                                                                                                                                        | Prohibited-path diff, mirror hash, inventory, full verify, independent review, and clean-room gates.                                    |

Behavioural matrix: ordinary versus blueprint projection; root/label/package/nested includes; include/list/named/merge-list/merge-named; empty/single/multiple/repeated/cyclic/missing/escaped/protected/symlink/wrong-kind sources; automation/scene/script/helper/unknown/UI resource types; valid, absent, empty, duplicate, tagged, aliased, anchored, numeric, null, boolean, and collection identities; plain/quoted/multiple secret spans; fresh/stale/tampered/wrong-operation/wrong-type cursors; zero/N/N+1 files/edges/depth/work/nodes/resources/string bytes/ID scalars+bytes; cancellation/deadline before read, parse, expansion, yield, and output; Windows neutral tests versus Linux native runtime UNVERIFIED; root and exact add-on mirror. Registration and live Home Assistant access remain absent.

## Phase 2 Slice F acceptance ledger

Risk: HIGH. Slice F handles attacker-controlled Git metadata, configuration, object data, paths, subprocess behavior, and repository races while guaranteeing that protected YAML content and secrets cannot escape.

| ID    | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Implementation evidence                                                                        | Validation evidence                                                                                                                                                                          |
| ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F-001 | Git reads require an absolute Git executable and a packaged fixed-operation Linux native broker. The broker pins the repository and direct `.git` directory beneath root with no links, magic links, or cross-device traversal; rejects `.git` files, linked/bare/common-dir/submodule/gitlink repositories and ambiguous or network/indirection metadata; executes through pinned `/proc/self/fd` descriptors; and validates root plus Git identities before and after. Windows, missing/unpackaged broker, or any uncertain state returns `capability_unavailable` with no fallback.                                                    | `NativeGitBroker`, strict binary protocol, and Linux broker source contract.                   | Platform/unpackaged, malformed protocol, identity drift, forbidden topology/config, fixed invocation, and native source-contract tests; real Linux confinement remains Slice G `UNVERIFIED`. |
| F-002 | The broker uses no-new-privileges, read-only Landlock for only repository/runtime inputs, seccomp denial of networking and unrelated execution, CPU/address-space/file/output/fd/process limits, process-group termination, fixed arguments, and an exact non-inherited environment that disables prompts, locks, pagers, replacements, lazy fetch, hooks, filters, fsmonitor, attributes, excludes, global/system config, proxies, credentials, SSH/askpass, PATH lookup, and Git indirection. Sandbox write denial is authoritative.                                                                                                    | Fixed broker operation request plus native policy source and TypeScript invocation guard.      | Exact argv/environment/request tests; abort/timeout/kill/output/stderr tests; forbidden token and native-policy source tests. Linux enforcement remains Slice G `UNVERIFIED`.                |
| F-003 | Status is strict porcelain v2 `-z --branch` with untracked all, ignored submodules, no renames, and no ahead/behind. Paths are fatal UTF-8, NFC, control-free normalized relatives, UTF-8 sorted, and capped at 500 without truncation. Type-2 rename, unmerged, submodule, unknown, and malformed records fail closed. Detached branch is null and unborn branch keeps its name. Status may expose protected path and XY only, never hashes, identities, or content.                                                                                                                                                                     | Strict status parser and immutable public status model.                                        | Clean/dirty/staged/unstaged/untracked/unborn/detached/protected/malformed/N+1 fixtures.                                                                                                      |
| F-004 | Diff accepts only current securely catalogued regular `.yaml`/`.yml` paths whose identities are nonprotected. Caller paths, when present, are nonempty, unique, UTF-8 sorted, at most 100, YAML, and current-catalog backed. Protected paths fail `protected_resource`; missing, deleted, index-only, symlink, gitlink, conflict, or unsupported paths fail `path_denied` before blob access. Unscoped mode intersects eligible YAML and emits only a generic warning for omitted unsupported paths. `truncated` describes display shortening only.                                                                                       | Catalog/status eligibility planner with protected preflight.                                   | Explicit/unscoped eligibility, protected-before-blob, delete/rename/conflict/binary/non-YAML/symlink/submodule tests.                                                                        |
| F-005 | Diff never invokes `git diff`. It uses fixed plumbing only: status/raw metadata, stage-zero `ls-files`, full-tree `ls-tree` at HEAD, object-format discovery, and bounded `cat-file` batch check/read with literal path separators. Only regular stage-zero blobs are accepted; conflicts, gitlinks, and symlinks fail. SHA-1 and SHA-256 object formats are supported, sizes are checked before content, and lazy fetch is disabled.                                                                                                                                                                                                     | Fixed broker operations and strict plumbing parsers.                                           | Fake-broker operation/ordering/stdin/object-format/mode/stage/size/forbidden-command tests.                                                                                                  |
| F-006 | Canonical diff semantics are HEAD (empty tree when unborn) to stage zero for index, and stage zero to descriptor-stable current content for worktree. `both` emits staged then worktree and may emit a path twice. Untracked is empty to current; staged add is empty to index. Deletion and rename source are unsupported. Output is path-sorted, canonical LF, line-based bounded Myers with three context lines, fixed headers, no-final-newline markers, and at most 200,000 work units.                                                                                                                                              | Internal deterministic patch engine independent of Git rendering.                              | Golden clean/dirty/staged+unstaged/untracked/unborn/detached/rename-as-D+A and work-boundary tests.                                                                                          |
| F-007 | Every consumed HEAD/index/current side is bounded to 512 KiB, rejects NUL or invalid UTF-8, passes the strict YAML gate exactly once, masks retained `!secret` spans, then receives complete nontruncating exact-plus-heuristic redaction before diffing. Unique raw blob/current input is capped at 2 MiB, complete canonical redacted patch at 1 MiB, and public output at 512 KiB. Public truncation occurs only after redaction on complete UTF-8/LF lines with a fixed marker. `diffSha256` hashes the complete canonical redacted patch before truncation; empty output hashes empty bytes. Raw and intermediate buffers are wiped. | Side loader, strict projection, span masking, redactor, patch bounds, truncator, and digest.   | Secret-name/value/digest canaries, invalid encoding/NUL/YAML, exact N/N+1 byte/work/patch/public limits, truncation/hash/wipe tests.                                                         |
| F-008 | Protected freshness and authoritative catalog/root/Git identities are checked before and after held output. Snapshot evidence binds root/Git identities, object format, HEAD/index identities/digests, eligible current path identities/hashes, scope/filter digest, and consumed object IDs/content digests. Drift discards output as `stale_source`; protected drift latches unhealthy. Cooperative work polls at most every 256 units; processes are killed and bounded; errors are stable and sanitized.                                                                                                                              | Snapshot framing, freshness checks, broker identity records, operation polling, and sanitizer. | Root/Git/index/object/current/protected drift, cancellation/deadline/kill, snapshot determinism, and exception-canary tests.                                                                 |
| F-009 | Slice F remains unregistered and unavailable on Windows or without the packaged broker. It changes no application/tool/config/Docker/build/version/mount/package/release/deploy/commit/push path. Root and add-on sources are exact mirrors. Linux Git, malicious config/filter/fsmonitor/hooks, active races, Landlock/seccomp, and packaged runtime validation remain Slice G `UNVERIFIED`.                                                                                                                                                                                                                                             | Internal-only exports, exact mirror, and documentation gate.                                   | Inventory/prohibited-diff/mirror hashes, focused/full validation, independent review, and clean-room validation.                                                                             |

Behavioural matrix: broker packaged/unpackaged and Linux/Windows; clean/dirty/staged-only/unstaged-only/staged+unstaged/untracked; born/unborn/detached; SHA-1/SHA-256; explicit/unscoped and worktree/index/both; empty/single/multiple/100/N+1 paths and 500/N+1 status records; regular YAML versus missing/deleted/index-only/binary/non-YAML/symlink/gitlink/submodule/conflict/rename; protected canonical path and identity alias; valid/malformed/fatal-encoding status/config/index/tree/object/protocol; secret name/value/digest canaries; N/N+1 side/aggregate/work/complete-patch/public limits; stable/drifted root, Git dir, index, object, current file, and protected registry; success/cancellation/deadline/output overflow/broker crash; fake Windows coverage versus real Linux confinement and races `UNVERIFIED`; root and exact add-on mirror. Registration and live repository access remain absent.

## Phase 2 Slice G1 acceptance ledger

Risk: HIGH. Slice G1 introduces durable proposal and audit persistence, crash reconciliation, authenticated pagination, cancellation commit boundaries, and fail-closed storage security. It remains internal and unregistered.

| ID     | Observable requirement                                                                                                                                                                                                                                                                                |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1-001 | Phase 2 uses a separate version-2 audit adapter while the Phase 1 `JsonlAudit`, schema, path, and tools remain unchanged.                                                                                                                                                                             |
| G1-002 | Durable ordering is attempt, then effect/read, then durable outcome, then response. Validation requiring no protected access may fail before an attempt; every path after an attempt receives exactly one terminal or reconciled outcome.                                                             |
| G1-003 | Audit records follow the exact frozen schemas. A propose attempt excludes `diffSha256`; records never contain content, query, snippet, diff, secret, or exception data.                                                                                                                               |
| G1-004 | Audit files and appends are serialized, bounded, canonical, synced, and rotated with directory sync. Recovery repairs only a provably incomplete active tail; invalid complete records, permission failures, partial writes, `ENOSPC`, sync failures, and ambiguous rotation latch Phase 2 unhealthy. |
| G1-005 | Protected storage uses `0700` directories and `0600` files; rejects links and non-regular files; uses same-filesystem temporary files; writes canonical bounded digested content through fsync, rename, and directory sync; and wipes owned sensitive buffers.                                        |
| G1-006 | Exact candidate and diff bytes exist only in protected storage while public proposal data is frozen. Candidate handling enforces fatal UTF-8, the 512 KiB boundary, digest binding, strict YAML, secret-span masking, and complete redaction.                                                         |
| G1-007 | Propose authoritatively and securely rereads an existing nonprotected YAML source, verifies the expected hash, and computes a deterministic diff. It never mutates live configuration or reloads, restarts, commits, or pushes.                                                                       |
| G1-008 | Idempotency is durable and serialized: the same key and identity returns the original proposal, including terminal proposals; a different identity conflicts; concurrent identical requests produce one effect.                                                                                       |
| G1-009 | Only `pending` proposals transition to `discarded` or `expired`; identity and payload remain immutable. Expiry has an exact boundary and remains correct across restart and clock changes.                                                                                                            |
| G1-010 | Listing is deterministic UTF-8 order and uses authenticated snapshot/key-bound cursors. Malformed, cross-operation, stale, out-of-range, rotated-key, closed-session, N, N+1, concurrent, and expiry pagination paths are bounded and fail safely; cooperative work polls at most every 256 units.    |
| G1-011 | Crash journal recovery distinguishes three windows: pre-effect rename causes no effect and a failed/reconciled outcome; post-effect/pre-outcome retains the effect and emits one reconciled/committed-unconfirmed outcome; post-outcome/pre-cleanup performs cleanup only.                            |
| G1-012 | Repeated recovery never duplicates effects or outcomes; `operationId` is authoritative.                                                                                                                                                                                                               |
| G1-013 | Quarantine is same-filesystem, protected, and bounded by count, item bytes, aggregate bytes, and work. Ambiguity or overflow latches the service unhealthy.                                                                                                                                           |
| G1-014 | Cancellation before the commit boundary has no effect; after rename the operation finishes or reconciles and never claims rollback.                                                                                                                                                                   |
| G1-015 | Any Phase 2 audit failure persistently latches Phase 2 unhealthy and blocks further operations while Phase 1 behavior remains compatible.                                                                                                                                                             |
| G1-016 | Slice G1 changes no registry, application, transport, config, Docker, manifest, mount, version, release, deployment, commit, or push path; root and add-on source mirrors are exact.                                                                                                                  |

Behavioural matrix: list/get/propose/discard; pending/discarded/expired/missing/corrupt; sequential/concurrent/restart idempotency; stable/stale/missing/protected/alias/invalid-YAML/invalid-encoding/bounded sources; open/write/short-write/fsync/rename/directory-sync/close/permission/`ENOSPC` failures; all three crash windows; audit attempt/effect/outcome/rotation/incomplete-tail/invalid-complete/repeated recovery; expiry before/at/after and during paging; valid/malformed/stale/wrong-operation/wrong-key/rotated/closed/N/N+1 cursors; cancellation before and after commit; symlink/hard-link/directory replacement/nonregular/oversize/duplicate/quarantine N/N+1 storage; owned-buffer wiping on success and every failure path. Windows coverage is logical; Linux permission, link, atomicity, directory-sync, and process-crash validation remains Slice G2.

## Phase 2 Slice G2 acceptance ledger

Risk: HIGH. Slice G2 packages inert candidate-only Linux helpers and validates native confinement and durability. It does not enable Phase 2 capabilities.

| ID     | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G2-001 | Human approval precedes dependency or base-image resolution, compiler installation, Docker/WSL/native-runner setup, or aarch64 execution. Every preflight command records exact state, output, and exit code; missing evidence is `BLOCKED`.                                                                                                                                                                                                                                                                                                                  |
| G2-002 | Root and add-on sources for `openat2-read`, `openat2-list`, and `git-broker` remain exact mirrors and compile from the add-on context with warnings-as-errors and supported PIE, stack, FORTIFY, RELRO, NOW, and non-executable-stack hardening.                                                                                                                                                                                                                                                                                                              |
| G2-003 | Candidate helpers exist only at `/app/native/openat2-read`, `/app/native/openat2-list`, and `/app/native/git-broker`, owned by root and installed mode `0555`; Git is the exact `/usr/bin/git` runtime package.                                                                                                                                                                                                                                                                                                                                               |
| G2-004 | Build/runtime image digests, Alpine repositories and package versions, compiler/toolchain, Git path/version, helper hashes, ELF mode, loader/shared libraries, and broker runtime inputs are frozen as exact evidence. The broker invocation requires exactly one absolute runtime loader and at most 16 absolute runtime-library files; all are no-follow unique regular-file identities with loader read/execute and libraries read-only. Broad paths, wildcards, missing/extra closure, unresolved dependencies, or runtime installation block completion. |
| G2-005 | Candidate packaging is inert: `run.sh`, application construction, registry, transports, and environment do not reference native helpers. The add-on `0.1.5` and no-release/no-version-change statements are historical G2-slice constraints only; the active invariants are `map: []`, Phase 1 tool inventory, startup, permissions, and observable behavior.                                                                                                                                                                                                 |
| G2-006 | `openat2-read` rejects final/parent symlinks, magic links, cross-mount paths, root swaps, nonregular files, and same-size concurrent changes; unsupported kernels fail unavailable with no fallback.                                                                                                                                                                                                                                                                                                                                                          |
| G2-007 | `openat2-list` preserves pinned-root secure traversal, complete ordered bounded framing, identical two-pass output, and rejects hostile links, mounts, swaps, drift, malformed output, and unsupported kernels.                                                                                                                                                                                                                                                                                                                                               |
| G2-008 | Reader/catalog TypeScript invocation uses absolute helpers, fixed argv/cwd, empty environment, no shell, bounded concurrency/output, cancellation/deadlines, child termination/reaping, and stable sanitized failures.                                                                                                                                                                                                                                                                                                                                        |
| G2-009 | `git-broker` accepts only its five frozen operations, pins root and direct `.git`, revalidates identities, and rejects linked/bare/common-dir/worktree/submodule, alternates, replacements, promisor, partial-clone, and ambiguous topology.                                                                                                                                                                                                                                                                                                                  |
| G2-010 | Hostile local/global/system Git includes, hooks, filters, external diff, fsmonitor, attributes, excludes, aliases, credentials, proxy, SSH/askpass, pagers, editors, replacement, and lazy-fetch configuration cannot execute, influence output, access network, write, or disclose canaries.                                                                                                                                                                                                                                                                 |
| G2-011 | Git execution proves `no_new_privs`, read-only least-path Landlock, seccomp network/unrelated-exec denial, and CPU/address/file/output/fd/process rlimits. Sandbox setup failure is fatal.                                                                                                                                                                                                                                                                                                                                                                    |
| G2-012 | Helper crash, signal, malformed/truncated/oversized protocol, cancellation, timeout, kill escalation, and unsupported capability leave no descendants, partial public output, leaked buffers, or permits.                                                                                                                                                                                                                                                                                                                                                     |
| G2-013 | Strict Linux proposal/audit storage enforces real ownership/modes, rejects links/nonregular/identity races, and persistently latches on unsafe scan or durability ambiguity while duplicate-create and expected replacement mismatch remain non-latching.                                                                                                                                                                                                                                                                                                     |
| G2-014 | Linux fault tests cover open, write, short-write, file sync, rename, directory sync, close, permission, `ENOSPC`, partial audit, rotation, quarantine count/item/aggregate, and every latch path.                                                                                                                                                                                                                                                                                                                                                             |
| G2-015 | Separate-process kill tests cover proposal, journal, audit, rotation, and quarantine checkpoints; filesystem state is verified immediately, after first recovery, and after second recovery with exactly one effect/outcome.                                                                                                                                                                                                                                                                                                                                  |
| G2-016 | Linux/amd64 is development evidence only. The authoritative candidate gate requires proven native aarch64 `uname`/CPU/OS/runner provenance, arm64 OCI and ELF evidence, and absence of QEMU/binfmt translation. Emulation remains `UNVERIFIED`.                                                                                                                                                                                                                                                                                                               |
| G2-017 | Candidate-image validation checks digest, architecture, labels, command, exact helper paths/hashes/permissions/linkage, shared-library closure, protocol success/failure, no unexpected writable/setuid artifacts, offline startup, and no runtime install/network.                                                                                                                                                                                                                                                                                           |
| G2-018 | Windows-neutral tests prove Dockerfile/build manifest contracts, source mirrors, fixed paths/modes, startup inertness, unchanged manifest/tool inventory, and prohibited scope before Linux execution. They cannot claim syscall, race, sandbox, permission, crash, or durability proof.                                                                                                                                                                                                                                                                      |
| G2-019 | Historical G2-slice `addon/config.yaml` metadata remained version `0.1.5`, `arch: [aarch64]`, and `map: []`, with no release, deployment, commit, or push. Active invariants remain `arch: [aarch64]`, `map: []`, no registration, no `/homeassistant` mapping, no helper wiring, no added privilege, and no unauthorized release, deployment, commit, or push.                                                                                                                                                                                               |
| G2-020 | Completion requires focused tests, add-on mirror validation, formatting, lint, typecheck, build/package evidence, `pnpm verify`, diff inspection, independent review, amd64 clean room, and every native-aarch64 gate. Missing required Linux/aarch64 evidence is `BLOCKED`.                                                                                                                                                                                                                                                                                  |

Frozen preflight evidence for the candidate packaging slice: the repository-owned native-aarch64 provenance harness now fails closed on non-Linux/non-arm64 process and uname state, missing or emulation-indicating CPU evidence, enabled arm64/aarch64 binfmt translation, non-linux/non-arm64 Docker server state, and missing or malformed bounded self-attested runner identity. This is tooling evidence only: in-guest checks cannot prove absence of full-system emulation, so G2-016 and G2-020 remain `BLOCKED` pending approved native aarch64 execution, external immutable runner provenance, and frozen arm64 candidate-image evidence. Docker client/server 29.6.1, Buildx 0.35.0, BuildKit 0.31.1 on amd64; Node multiarch index `sha256:5539840ce9d013fa13e3b9814c9353024be7ac75aca5db6d039504a56c04ea59`, amd64 manifest `sha256:99351363debf40f3495cb7fc657a777334c3b21143e594dbfcc7de187439633c`, arm64 manifest `sha256:ddde8acdbc31cec9740194b86faacadc174f963c48e1f75a0c1d3918ed715aee`; Home Assistant `aarch64-base:3.22` index `sha256:03185a346a3e505e0aa102cb1e976a2e7be7421cf4a405abd2cd83e0a6bd0a58`, arm64 manifest `sha256:b530091383543737310fd0b3512123de3e3b5165f221f244d0171b858528d963`; Home Assistant `base:3.22` index `sha256:8f937e0b89e108f432d3cb140900f06e568f8f4fbdb7987472447f54d8b24495`, amd64 manifest `sha256:f243712420b3493e9c23d821e4a44c1a1c152c294a9242624de947041cf036cc`. The pinned amd64 development closure is Git `/usr/bin/git` SHA-256 `5b5cbd6facf5d86226063d69fe57064bc5ad79bdccee2af0ac787646c564a880`, loader `/lib/ld-musl-x86_64.so.1` SHA-256 `1cc5c3f502179e2a4befae29658fb109102effefc75742893aa4a449fd3fbb03`, PCRE2 real target `/usr/lib/libpcre2-8.so.0.14.0` SHA-256 `0eae946d1f2746b6c64cc8beb9230360dc935e8552f89b765c7e697bff232345`, and zlib real target `/usr/lib/libz.so.1.3.1` SHA-256 `09b1bbd6ffe274039cefaca595f55cec0af65fe90d9e285e5d57ff7ed96948d2`; the `/usr/lib/libz.so.1` symlink is never passed to the no-follow broker. Direct arm64 execution currently fails with `exec format error`, so native and emulated arm64 runtime validation remain `UNVERIFIED`. Packaged amd64 development image `ha-engineering-mcp:g2-amd64-candidate` resolves to `sha256:e71aec0b2b6d8b76bbeb751b1b0623325509d9665f62c6ebff885f995ae4dc03` (`linux/amd64`, command `/run.sh`, no `Config.Labels`); helper modes are root:root `0555` with SHA-256 `openat2-read` `59faab9a79575409e59b3672cb1ecb50a9f3b3a7d0db85f1065d499a3c7c425f`, `openat2-list` `6fe9587146b927b6f84c53a3d61efd87e6143c9ee95268b9c997d464260bab51`, and `git-broker` `01823637f02c49e685f84a2b371870945299e772b6dc37dbf9194b2f34f051f8`; packaged runtime closure is Git `/usr/bin/git` SHA-256 `5b5cbd6facf5d86226063d69fe57064bc5ad79bdccee2af0ac787646c564a880`, loader `/lib/ld-musl-x86_64.so.1` SHA-256 `7d221f4e17e8f7ebfc208d6e621bb7fc71bc99081bed47409d77048d9a69dbd5`, PCRE2 real target `/usr/lib/libpcre2-8.so.0.14.0` SHA-256 `0eae946d1f2746b6c64cc8beb9230360dc935e8552f89b765c7e697bff232345`, and zlib real target `/usr/lib/libz.so.1.3.1` SHA-256 `09b1bbd6ffe274039cefaca595f55cec0af65fe90d9e285e5d57ff7ed96948d2`; the preserved prior-context artifact `sha256:60d3d5d8fda4f2bee464e02cf99b6394cd787f62572a9a01934f100864c68cb1` (local tag `ha-engineering-mcp:g2-amd64-candidate-f94ddd1`) reran successfully after Docker restoration. The current working-tree add-on context based on `ce15a89` was then rebuilt on 2026-07-20 with both stages pinned to the amd64 Node manifest; Buildx record `4horxn7bmbgycgfhzlq2qnza0` produced OCI index `sha256:e71aec0b2b6d8b76bbeb751b1b0623325509d9665f62c6ebff885f995ae4dc03` and linux/amd64 manifest `sha256:47fe0079da3266f9d7819af7b60783bba09758156e6813d0da0b5183c1de279c`. The rebuilt artifact passed all 8/8 strict rows, including the packaged 74/74 Git protocol matrix, with unchanged frozen helper and runtime hashes; local NDJSON evidence is `.tmp/g2-candidate-image-results-amd64-current.ndjson` (SHA-256 `3319e8647e830076c468380d86d986ff791b00b27b7f1ed9ef6f40c5ce2cf468`). The amd64 Node-base offline startup probe is development-only and not HA-base authority because `bashio` is absent; `/bin/sh /run.sh` fails closed with invalid environment configuration.

Behavioural matrix: Windows-neutral contract versus Linux/amd64 development versus native aarch64 authority; source/build/final-image and packaged/missing/wrong-mode helpers; supported/unsupported `openat2`; regular/final-link/parent-link/magic-link/hard-link/mount-crossing/nonregular/root-swap/same-size-race topology; stable/drifted one-pass/two-pass catalogs; clean/dirty and born/unborn/detached SHA-1/SHA-256 Git with hostile config/topology; required read/execute versus denied network/unrelated exec/repository or unrelated write and every rlimit; success/crash/malformed/overflow/cancellation/deadline/kill helper lifecycle; proposal/journal/audit/quarantine open/write/short-write/fsync/rename/dirsync/close/permission/`ENOSPC` and process-kill checkpoints with immediate/first-restart/second-restart evidence; inert candidate versus shipped Phase 1 behavior. Registration, mounts, versioning, release, deployment, and live Home Assistant access remain absent.

## R1-001 Supervisor packaging repair contract

Objective: release add-on `0.1.7` with Supervisor-compatible immutable packaging after `0.1.6` rejected `addon/build.yaml` digest fields, fell back to reserved `BUILD_FROM=ghcr.io/home-assistant/base:latest`, and failed Dockerfile APK pins.

Risk: `HIGH`, because this repairs installer/package metadata and container base selection for the Home Assistant Supervisor add-on path.

Acceptance evidence:

| ID     | Requirement                                                                                                                                                           | Implementation evidence                                                                                                        | Validation evidence                                                                                  |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| R1-001 | `addon/build.yaml` is absent so Supervisor cannot reject digest-bearing `build_from` schema fields or inject reserved `BUILD_FROM` fallback behavior.                 | Delete `addon/build.yaml`; tests assert the file is absent.                                                                    | Focused add-on test and `git diff --check`.                                                          |
| R1-002 | The Dockerfile owns the final base image through private `HA_BASE_FROM`, retaining the pinned aarch64 HA base manifest digest and no reserved `BUILD_FROM` arg usage. | `addon/Dockerfile` uses `ARG HA_BASE_FROM=ghcr.io/home-assistant/aarch64-base:3.22@sha256:b530...` and `FROM ${HA_BASE_FROM}`. | Tests assert pinned `HA_BASE_FROM`, no `ARG BUILD_FROM`, and no `FROM ${BUILD_FROM}`.                |
| R1-003 | Build-stage Node input and amd64 development override remain immutable while runtime package pins remain unchanged.                                                   | Dockerfile keeps Node multiarch index, explicit amd64 override comment, and existing `apk add` package pins.                   | Tests assert Node index, amd64 override, HA base digest, and APK pin strings.                        |
| R1-004 | Release metadata records the packaging repair as `0.1.7` without adding mounts, privileges, helper wiring, release, deployment, commit, push, or live HA access.      | `addon/config.yaml` version `0.1.7`; changelog prepended; existing least-privilege config retained.                            | Focused add-on test asserts version, `arch: [aarch64]`, `map: []`, and prohibited privilege strings. |

Behavioural matrix:

| Row                       | Expected state                                                                                                                                                 | Evidence status                                |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| Windows-neutral contract  | Static tests prove metadata/Dockerfile contracts and absence of deprecated build manifest without claiming native Linux execution.                             | Planned focused test                           |
| Linux/amd64 development   | Dockerfile keeps explicit pinned amd64 Node override and private HA base override guidance for local development builds.                                       | Planned focused test                           |
| Supervisor schema path    | No `addon/build.yaml` is present, avoiding the rejected digest-bearing `build_from` schema path.                                                               | Planned focused test                           |
| Native aarch64 Supervisor | User-approved push/live Supervisor add-on build must prove the pinned final aarch64 base, APK pins, install, startup, and health behavior on HA OS/Supervisor. | `UNVERIFIED` until user-approved push/live run |

Scope: only `docs/requirements-ledger.md`, `addon/Dockerfile`, `addon/config.yaml`, `addon/CHANGELOG.md`, `tests/addon.test.ts`, and deletion of deprecated `addon/build.yaml`.

Validation: focused `pnpm vitest run tests/addon.test.ts`, formatting for changed files or repository format check where practical, `git diff --check`, and final diff inspection. Authoritative `pnpm verify` remains desired repository validation. Native Supervisor operational validation remains `UNVERIFIED` until the user approves push/live installation.

Prohibited actions: no commit, push, deploy, release, HA access, production mutation, destructive Git, privilege expansion, `/homeassistant` mapping, helper wiring, or dependency changes.

## Phase 3A guarded apply core acceptance ledger

Risk: HIGH. Phase 3A introduces the internal guarded-application state machine that will eventually protect live writes, but this slice remains unregistered, adapter-neutral, and disabled by default with no real live adapters or grant producer.

| ID      | Observable requirement                                                                                                                                                                                                              | Implementation evidence                                                                            | Validation evidence                                                                                              |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| P3A-001 | Strict transaction schemas define the exact states, legal transitions, CAS/version journal contract, commit point, recovery table, and terminal outcomes.                                                                           | `docs/phase3-contracts.md` and `src/phase3/contracts.ts`.                                          | `tests/phase3Contracts.test.ts`.                                                                                 |
| P3A-002 | Injected-only, atomic, single-use apply grants bind exact proposal storage, candidate, diff, risk, issued/expires, pending state, and operation; not-yet-valid, expiry, replay, wrong binding, drift, and cancellation fail closed. | `src/phase3/approval.ts`; no persistence, producer, or CLI.                                        | `tests/phase3Approval.test.ts`.                                                                                  |
| P3A-003 | Coordinator runs policy before lock, canonical locking, policy/identity recheck, source digest, local validation, approval consume, checkpoint, intent journal, atomic apply, validation, narrow reload, and verification.          | `src/phase3/applyCoordinator.ts` with injected ports and default-closed `GuardedPhase3PolicyPort`. | `tests/phase3ApplyCoordinator.test.ts`.                                                                          |
| P3A-004 | Every post-commit failure carries the latest durable record, writes rollback intent, verifies checkpoint integrity, restores checkpoint, validates and verifies rollback, or requires manual recovery without implicit restart.     | `Phase3ApplyCoordinator.rollbackAfterCommit`.                                                      | `tests/phase3ApplyCoordinator.test.ts` and `tests/phase3Recovery.test.ts`.                                       |
| P3A-005 | Precommit cancellation has no live effect; postcommit caller cancellation is ignored while internal verification or rollback finishes.                                                                                              | Approval/lock precommit signal checks and coordinator internal postcommit context.                 | `tests/phase3Approval.test.ts`, `tests/phase3ResourceLocks.test.ts`, and `tests/phase3ApplyCoordinator.test.ts`. |
| P3A-006 | Bounded keyed locks serialize same path, allow distinct paths, and clean cancelled/deadline waiters.                                                                                                                                | `src/phase3/resourceLocks.ts`.                                                                     | `tests/phase3ResourceLocks.test.ts`.                                                                             |
| P3A-007 | Startup recovery never reapplies candidate and returns explicit per-record recovery dispositions for digest-driven rollback, finish, manual attention, or terminal no-transition inspection for every state.                        | `Phase3ApplyCoordinator.recover` and `phase3RecoveryTable`.                                        | `tests/phase3Recovery.test.ts`.                                                                                  |
| P3A-008 | No MCP, registry, config, CLI, add-on runtime, version, mapping, deployment, or live-write enablement changes.                                                                                                                      | Phase 3A files are an unregistered source island; `phase3Contract.writesEnabled=false`.            | `tests/phase3Isolation.test.ts`; diff inspection.                                                                |
| P3A-009 | Root and add-on source mirrors are exact.                                                                                                                                                                                           | `src/phase3/*.ts` mirrored to `addon/app/src/phase3/*.ts`.                                         | `tests/phase3Isolation.test.ts` and `pnpm addon:context` through `pnpm verify`.                                  |
| P3A-010 | Exhaustive focused tests and full validation cover contracts, approval, locks, coordinator, recovery, and isolation.                                                                                                                | Six focused Phase 3A Vitest files plus repository validation.                                      | Focused Phase 3A Vitest command, `pnpm verify`, `git diff --check`, and final diff inspection.                   |

Behavioural matrix: pending/discarded/expired proposals; exact versus drifted proposal storage, candidate, and diff digests; grant valid/not-yet-valid/replayed/expired/wrong-risk/wrong-operation/wrong proposal/cancelled; writes disabled, missing capability, restart-required, domain reload supported/unsupported, no-impact; same path versus distinct path locking, cancelled waiters, deadline waiters, waiter overflow, invalid path; precommit source drift, candidate drift, identity drift, validation failure; apply committed, before-commit, and commit-unknown; post-validation/reload/verification failure; rollback success/failure/manual; caller cancellation before and after commit; startup recovery for every state with candidate, checkpoint/expected, unknown, and missing digest; root and add-on mirror. Phase 3A does not register tools, mutate Home Assistant, restart, commit, push, deploy, add mappings, change versions, or enable writes.

## Phase 3B protected proposal input acceptance ledger

Risk: HIGH. Phase 3B bridges protected proposal persistence into the Phase 3 core, but remains read-only, unregistered, and disconnected from every live-effect port.

| ID      | Observable requirement                                                                                                                                                                                  | Implementation evidence                               | Validation evidence                                                                                      |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| P3B-001 | Exact proposal lookup is bounded and non-mutating, validates canonical UUID filenames and existing protected-file/schema/integrity guarantees, and zeros raw bytes.                                     | ProtectedProposalStore.readExact.                     | tests/phase3ProposalAdapter.test.ts exact, missing, tamper, traversal, and directory non-mutation cases. |
| P3B-002 | Phase 2 public proposal identity maps exactly to the Phase 3 snapshot, preserving state and mapping reloadImpact to impact.                                                                             | ProtectedPhase3ProposalAdapter.load.                  | Pending/discarded/expired and all impact mapping tests.                                                  |
| P3B-003 | Candidate loading revalidates public/protected identities, canonical base64, strict UTF-8, candidate digest, and exact diff digest, returning independent caller-owned bytes while zeroing temporaries. | ProtectedPhase3ProposalAdapter.loadCandidate.         | Identity, encoding, digest, repeated-load, and caller-zeroization tests.                                 |
| P3B-004 | The adapter remains an inert source island with exact root/add-on mirrors and no runtime, registry, CLI, config, package, mapping, or write activation.                                                 | Root/add-on source mirrors and unchanged composition. | tests/phase3Isolation.test.ts, pnpm addon:context, and diff inspection.                                  |

Behavioural matrix: pending/discarded/expired; none/domain-reload/restart-required impact; exact/missing/wrong proposal identity; public/protected idempotency, candidate, and diff agreement/drift; canonical/noncanonical base64; valid/invalid UTF-8; valid/tampered storage envelope; repeated candidate loads and caller zeroization; exact-read success/failure without scan or quarantine; canonical UUID/traversal; root/add-on mirrors; runtime import absent.

Deferred to Phase 3C or later: crash-safe cross-process transaction journal CAS, immutable checkpoints, source and atomic-write adapters, validation/reload/verification adapters, approval production, registration, activation, packaging, and native Linux operational evidence for any new filesystem primitive.

## Phase 3C durable transaction journal acceptance ledger

Risk: HIGH. Phase 3C persists the Phase 3 transaction state machine with cross-instance CAS and crash reconciliation, while remaining unregistered and disconnected from all live effects.

| ID      | Observable requirement                                                                                                                                                                                 | Implementation evidence                                                       | Validation evidence                                                                                         |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| P3C-001 | Strict canonical integrity envelopes form contiguous append-only histories beginning at intent_prepared v0, preserving immutable identity and following exact legal adjacency.                         | DurablePhase3Journal scan and history validation.                             | Restart, every-state, re-signed tamper, gap/fork, digest, and malformed-artifact tests.                     |
| P3C-002 | Intent and transition writes are create-only durable commits; transition checks expected version, increments once, preserves failure patch semantics, and never overwrites evidence.                   | Same-parent pending entry plus close-before-atomic-hard-link commit protocol. | Duplicate, illegal, stale-version, hook-order, and restart tests.                                           |
| P3C-003 | Independent instances racing the same next version produce exactly one winner and one CAS conflict; every public read refreshes disk state.                                                            | Create-only final file link and bounded inode-aware refresh.                  | Two-instance race and paused-pending observer tests on Windows and native Linux overlayfs.                  |
| P3C-004 | Precommit handled failures remove only their own pending entry; simulated crash pending entries are retained and ignored; post-link failures report commit-unknown and reconcile exactly.              | Phase3JournalSimulatedCrash hooks and commit-point classification.            | Pre/post-commit hook, pending retention, same-instance, fresh-instance, and non-destructive evidence tests. |
| P3C-005 | Private ownership, no-follow reads, stable file identity, full writes, close ordering, file/directory sync, canonical names, and bounded transaction/final/pending/record/aggregate scans fail closed. | Journal filesystem and durability boundaries plus exported limits.            | Short-write, EINTR, hard-link, unsafe artifact, N/N+1, Windows, and Linux tests.                            |
| P3C-006 | Root and add-on mirrors are exact and no runtime/config/CLI/registry/package/mapping/activation path imports the journal.                                                                              | src/phase3/journal.ts and addon/app/src/phase3/journal.ts only.               | Phase 3 isolation, addon context, full diff, and clean-room validation.                                     |

Behavioural matrix: fresh/restarted; every transaction state including terminal evidence; initial/duplicate/missing/stale/illegal/legal; same-instance and independent-instance race; empty/active/orphan pending; handled precommit and simulated process crash; before/after link and parent sync; exact/tampered/noncanonical/re-signed/gapped/forked history; unknown artifact; hard link and unsafe identity; short write/EINTR; transaction and pending limits at N/N+1; stable sorting; Windows injected-durability logic and native Linux Docker overlayfs; root/add-on mirror and runtime isolation.

Deferred to later slices: immutable checkpoints, protected source and atomic-write adapters, validation/reload/verification adapters, approval production, registration, activation, packaging, and live Home Assistant mutation.

## Phase 3D durable immutable checkpoint acceptance ledger

Risk: HIGH. Phase 3D persists rollback checkpoint bytes with immutable integrity envelopes and crash-safe create-only durability, while remaining unregistered and disconnected from all live effects.

| ID      | Observable requirement                                                                                                                                                                                                                                                                        | Implementation evidence                                       | Validation evidence                                                                                               |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| P3D-001 | Strict immutable storage envelopes record and internally validate lowercase checkpoint UUID, canonical Phase 3 relative path, expected/source/content digests, canonical base64, storage digest, and filename/envelope UUID equality. Restore-time path enforcement is not claimed.           | `DurablePhase3Checkpoints` envelope and parse checks.         | `tests/phase3Checkpoints.test.ts` create/load, tamper, re-signed tamper, filename mismatch, and encoding cases.   |
| P3D-002 | `create` validates canonical path, lowercase SHA-256, content size, and `sha256(content)==expected` before effects; writes a complete canonical record through pending fsync/close/no-follow recheck/hard-link/parent-fsync/cleanup/second-fsync without overwriting final artifacts.         | `DurablePhase3Checkpoints.create` and append protocol.        | Invalid-input no-disk-effect, ordering, no-overwrite, short-write/EINTR, and link-race tests.                     |
| P3D-003 | `load` requires exact lowercase UUID, bounded no-follow descriptor stat/read/stat/lstat, strict schema/canonical JSON/base64/storage/content integrity, filename/envelope UUID equality, fresh caller-owned bytes, and zeroed temporary buffers.                                              | `DurablePhase3Checkpoints.load` and `readRecord`.             | Restart/load, repeated independent loads, caller zeroization, tamper, malformed, and noncanonical tests.          |
| P3D-004 | Initialize and every operation refresh bounded disk state; unknown, unsafe, symlink, nonregular, unrelated hardlink, malformed/tampered/noncanonical/oversize/count/aggregate/scan violations fail closed and latch unhealthy; recognized pending evidence is retained/ignored.               | Bounded `scan`, pending validation, and hard-link checks.     | Bounds, unknown artifact, symlink, unrelated hardlink, pending, malformed race, and native mode tests.            |
| P3D-005 | Cancellation/deadline checks apply before staging, during writes, and before link; after link cancellation is ignored. Handled precommit failures clean owned pending; simulated precommit crash retains pending; post-link failure returns `checkpoint_commit_unknown` and retains evidence. | Hook stages, simulated crash type, and commit classification. | Abort/deadline, write cancellation, handled and simulated pre-link failure, post-link failure, after-link cancel. |
| P3D-006 | Root and add-on mirrors are exact and no runtime/config/CLI/registry/package/mapping/activation path imports checkpoints.                                                                                                                                                                     | `src/phase3/checkpoints.ts` mirrored to add-on source.        | `tests/phase3Isolation.test.ts`, focused checks, and diff inspection.                                             |

Behavioural matrix: fresh/restarted; empty and nonempty content; exact/missing/uppercase checkpoint ID; valid/invalid path; valid/uppercase/mismatched expected digest; content at N and N+1; concurrent independent creates; before/after link visibility; short write and EINTR; abort/deadline before staging and during write; handled precommit failure, simulated precommit crash, cleanup failure, immediate post-link failure, cancellation after link; ordinary tamper, canonically re-signed tamper, filename UUID mismatch, noncanonical JSON and base64; unknown artifact, symlink, unrelated hardlink, empty and malformed final races; checkpoint, pending, record, aggregate, and scan bounds at N/N+1; Windows logical durability plus native fail-closed; native Linux private-mode evidence; root/add-on mirror and runtime isolation.

Deferred to later slices: atomic-write adapter, validation/reload/verification adapters, approval production, registration, activation, packaging, and live Home Assistant mutation.

## Phase 3E protected source adapter acceptance ledger

Risk: HIGH. Phase 3E connects the Phase 3 source port to protected repository catalog and content boundaries, preserving cancellation/deadline identity and secret-source exclusion while remaining unregistered and disconnected from live write effects.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                             | Implementation evidence                                                                         | Validation evidence                                                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| P3E-001 | `ProtectedPhase3SourceAdapter` implements `Phase3SourcePort`, injects `RepositoryCatalogProvider` plus a narrow `ProtectedIdentityRegistry`-compatible boundary, validates canonical Phase 3 paths before effects, derives one Phase 2 context with fresh UUIDs and exact caller signal/deadline, and reads only exact catalog entries that pass protected path/inode precheck and post-read identity/size bounds. | `src/phase3/sourceAdapter.ts` and add-on mirror.                                                | `tests/phase3SourceAdapter.test.ts` success, path, absence, protected, race, identity, size, and context tests.              |
| P3E-002 | Boundary-owned bytes are zeroed on every path; successful `read` returns fresh caller-owned bytes only after final checks; caller zeroization cannot affect later reads; existing boundary error codes are preserved except accepted-read missing/protected races become generic `service_unhealthy`; unknown errors are generic and errors disclose neither raw bytes nor target paths.                           | Source adapter `finally` zeroization, copy-after-final-freshness, and safe error normalization. | Zeroization, repeated read, caller-copy, race, unknown/boundary error, and non-disclosure tests.                             |
| P3E-003 | `readDigest` preserves the Phase 3 operation-context API by taking only `path`, validating it before effects, creating an internal Phase 2 context with fresh UUIDs, a fresh non-aborted signal, and a 60 second deadline, then following the same classification and zeroization rules. `null` is only catalog absence.                                                                                           | `ProtectedPhase3SourceAdapter.readDigest`.                                                      | Digest success, absence-null, context identity/UUID/signal/deadline, race, bounds, and zeroization tests.                    |
| P3E-004 | Phase 3E is an exact root/add-on mirror and remains inert: `phase3Contract.registered=false`, `writesEnabled=false`, `liveAdapters=absent`, with no runtime, config, CLI, registry, package, mapping, activation import or mention, dependencies, version changes, atomic writes, or live mutation.                                                                                                                | Root/add-on source mirrors and unchanged composition/config/package files.                      | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused checks, full diff inspection, and parent native Linux checks. |

Behavioural matrix: `read` and `readDigest`; success and catalog absence; empty, `PHASE2_MAX_TEXT_BYTES`, and N+1 byte content; valid and invalid canonical paths; preknown protected path and protected inode; accepted-read missing/protected races; root identity, file identity, and size drift; registry unready, unhealthy, and freshness drift; caller cancellation/deadline and after-read cancellation; preserved boundary errors and unknown exceptions; exact lowercase SHA-256; caller-owned copies and zeroization; repeated reads; same derived context object per call with fresh IDs; Windows logical fake coverage and parent native Linux composition.

Deferred to later slices: atomic apply in Phase 3F, validation/reload/verification adapters, approval production, registration, activation, packaging, and live Home Assistant mutation.

## Slice H Phase 2 add-on activation contract

Objective: activate the 11 frozen Phase 2 repository/proposal tools only for the managed add-on after all local startup gates pass, while preserving the 15 Phase 1 tools and all mutation prohibitions.

Risk: `HIGH`, because this registers public MCP contracts, gates protected repository access, persists cursor/proposal state, and changes add-on packaging.

Slice H supersedes the historical G2 inertness invariants (`map: []`, no helper wiring, and `registered: false`) while retaining every no-mutation and least-privilege guarantee.

Acceptance evidence:

| ID    | Requirement                                                                                                                                                                                | Implementation evidence                                                                                   | Validation evidence                                                                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| H-001 | Phase 2 registers exactly the existing `phase2ToolNames` alongside the 15 Phase 1 tools only in add-on mode with `enable_phase2` true by default. `false` or local mode is Phase1-only.    | Shared registry/composite registry, `Phase2Tools`, config `enablePhase2`, and add-on option wiring.       | Registry inventory tests for enabled, disabled, local, and gate-failure paths.                            |
| H-002 | Any activation gate failure leaves the process healthy with Phase 1 only; diagnostics are stable closed enum outcomes and never expose exception causes.                                   | Add-on activation gate catches fixed artifact, key, proposal-store, secrets, catalog, and service checks. | Failing-gate and error-envelope tests.                                                                    |
| H-003 | Startup gates run in order: fixed artifact validation, protected persistent master key, proposal audit/store recovery, `secrets.yaml` strict scalar registration, catalog proof, register. | Injectable activation components with fixed `/homeassistant`, `/data`, and `/app/native` defaults.        | Unit tests for disabled/local/failing gates, key persistence/derivation, and strict secrets basics.       |
| H-004 | Phase 2 tools are add-on-only read/proposal metadata tools. They never mutate live config, reload, restart, apply, commit, push, or write Git state.                                       | Dispatcher calls repository/resource/Git read services and proposal metadata service only.                | Dispatch/schema tests plus retained Phase 1 fallback tests.                                               |
| H-005 | Add-on packaging moves to `0.2.0` and uses the official read-only `homeassistant_config` map only with no new privileges or RW mapping.                                                    | `addon/config.yaml`, `run.sh`, docs, changelog, and add-on mirror updates.                                | Updated add-on/config tests; live Supervisor validation remains `UNVERIFIED` until explicitly authorized. |

Behavioural matrix: add-on enabled success, add-on disabled, local mode, each init gate failure, fresh/valid/corrupt protected state, false-to-true rerun, 0.1.7 Phase1-only compatibility, HTTP and stdio dispatch cancellation/deadline, missing/hostile Git per-call failure, secrets/no-leak, proposal `/homeassistant` immutability, and unchanged Phase 1 audit/pairing/TLS behavior.

Validation: focused registry/activation/dispatcher/config/add-on tests, `pnpm typecheck`, `git diff --check`, and broader `pnpm verify`. The arm64 candidate image `sha256:57a705c22e9ed4b296ea434f3ea8b93c78669ffaabb5a130d964730b6f1614ce` passed 7/8 strict packaging rows under Docker Desktop emulation: metadata and exact HA labels, helper paths/hashes, runtime linkage, the exact expected HA-base set-id helper, writable directories, and offline startup. The Git protocol matrix remains `UNVERIFIED` because its sandbox/rlimit process execution fails under amd64-hosted arm64 emulation; G2-016 prohibits treating that as native evidence. Native aarch64, live Supervisor install, and live Home Assistant repository access remain `UNVERIFIED` until the approved deployment test.

Prohibited actions: no commit, push, deploy, release, live HA access, live config mutation, reload, restart, Git write, privilege expansion, RW mapping, or destructive operation.

## Constraints and assumptions

- TypeScript/Node.js with one registry exposed through local stdio and the Phase 1 add-on's TLS-only Streamable HTTP transport; the optional bridge preserves stdio-only Codex clients.
- The HA add-on/aarch64 image is the authoritative runtime. Platform-neutral core/bridge logic remains portable to Linux/Windows/macOS where supported.
- Credentials come only from environment/external secret storage. Token values are never accepted as MCP tool arguments.
- Direct filesystem access is a negotiated capability, not an assumption.
- API-managed resources and YAML-managed resources require separate repository adapters.
- `secrets.yaml` and configured/renamed secret-source files are categorically content-inaccessible through MCP. Their canonical identities, aliases, includes, and symlinks are tracked so alternate paths cannot bypass the rule.
- The actual target is Home Assistant OS 18.1 on Raspberry Pi 5 (`aarch64`), Core 2026.7.2, Supervisor 2026.06.2, with host configuration /config exposed to the add-on only through the official read-only homeassistant_config mapping at /homeassistant, and storage-mode dashboards.
- Home Assistant OS is not treated as a general-purpose Node host. The server is packaged as a least-privilege managed add-on; no direct OS access is required.
- The repository's authoritative deterministic validation command is `pnpm verify`.

## Behavioural matrix

| Dimension           | Required paths                                                                                                                                                             |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Policy              | reads enabled; writes disabled/enabled; restart disabled/enabled; deletes disabled and explicit approval if ever enabled                                                   |
| Identity            | authorized; missing/expired/invalid token; insufficient HA privileges; forged/replayed/expired out-of-band approval                                                        |
| Data size           | empty; single; multiple; paginated/truncated; oversized rejection                                                                                                          |
| Filesystem          | valid relative path; traversal; absolute outside root; symlink escape; changed inode/hash; unsupported/binary encoding                                                     |
| Proposal            | valid; expired; stale source; already applied/discarded; approval absent/mismatched; concurrent proposals touching same resource                                           |
| Apply               | success; local validation failure; HA validation failure; atomic write failure; reload failure; verification failure; rollback success/failure; crash recovery             |
| API                 | success; 4xx; 5xx; timeout; safe-read retry; mutation no-retry; WebSocket disconnect/reconnect/correlation                                                                 |
| Git                 | clean; scoped dirty; unrelated dirty; conflict; no repository; detached branch; checkpoint/final commit failure                                                            |
| Deployment          | HA OS 18.1 on aarch64 as first live target; OS/Supervised/Container/Core adapter contracts; capability unavailable; clean install vs existing config                       |
| Dashboard storage   | storage-mode discovery/read via supported API; mutation capability unavailable unless a supported API is verified; YAML-mode fixtures/adapters remain separate             |
| Platform            | HA add-on/aarch64 authoritative; amd64/Linux container for development and clean-room validation; Windows/macOS coverage for the optional bridge and platform-neutral core |
| Resource management | YAML storage; UI/API storage; include graph; resource missing/renamed/disabled/active                                                                                      |

Cancellation is relevant for CLI/MCP requests before side effects; once an apply transaction crosses its commit point it must finish verification or rollback rather than abandon state. Backward compatibility begins once Phase 1 tool schemas ship.

## Validation plan

Targeted: schema, policy, redaction, path, YAML, proposal, API, Git, and transaction tests. Full validation: format check, lint, `tsc --noEmit`, all non-E2E tests, package/build check, MCP smoke test, and security-focused suite. Environment: Ubuntu supported-adapter diagnostics and Docker HA E2E where practical. Clean room: required from Phase 1 onward because this is high-risk and cross-platform. The user authorized and completed the named Phase 1 read-only diagnostics recorded in [deployment.md](deployment.md). Deployed 0.1.4 recent-error and malformed-cursor checks failed historically; installed 0.1.5 repaired both paths and passed the complete live acceptance matrix against Core 2026.7.2. Phase 1 live validation is `PASSED`. Mutations remain prohibited.

## Required human approvals

1. Approve package/base-image resolution required for the add-on and bridge implementation (the user's “let's start” authorizes Phase 1 dependency resolution, not deployment).
2. Pair each local bridge/client through ingress and verify the TLS fingerprint during installation; public exposure remains prohibited.
3. Approve enabling writes after read-only phases and backups are verified.
4. Approve each high-risk/destructive proposal through the operator-controlled channel with matching proposal ID/diff digest; MCP-supplied identity fields are never sufficient.
5. Separately request any Git commit; pushing remains outside default scope.

## Prohibited actions

No production mutation, unrestricted shell, arbitrary file write, unrestricted HA service call, token exposure, commit, push, merge, rebase, history rewrite, publish, deploy, deletion, or restart during planning and initial development.

## Phase 3F inert native atomic apply acceptance ledger

Risk: HIGH. Phase 3F introduces the future native atomic replacement adapter and Linux helper source, but the slice is inert source/tests/docs only. It remains unregistered, unconfigured, unmapped, unreachable, and live writes remain disabled.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                         | Implementation evidence                                                                     | Validation evidence                                                                                                                  |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| P3F-001 | Source/tests/docs only; `NativePhase3AtomicApply` implements `Phase3AtomicApplyPort` but is not imported, registered, configured, mapped, activated, or live. `phase3Contract` remains unchanged; packaging, libcrypto, aarch64, RW mapping, activation, and live mutation are deferred/unverified.            | `src/phase3/atomicApply.ts`, add-on mirror, unchanged runtime/config/package files.         | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused tests, diff inspection.                                               |
| P3F-002 | TypeScript adapter validates canonical path, lowercase digests, candidate size and digest, active context, Linux platform, absolute normalized root/helper, bounded concurrency and waiters before fixed no-shell helper spawn, exact framed stdout, bounded/discarded stderr, no retry, cleanup, and zeroing. | `NativePhase3AtomicApply`, `SpawnPhase3AtomicApplyRunner`, stable `Phase3AtomicApplyError`. | `tests/phase3AtomicApply.test.ts` validation, frames, child failures, cancellation, queueing, and zeroing cases.                     |
| P3F-003 | C helper protocol is Linux-only, fixed-argument and exact-stdin, uses system OpenSSL 3 `SHA256`, openat2 confinement, regular single-link target checks, xattr denial, digest/identity validation, `O_TMPFILE` staging, no fallback named creation, and pre-exchange cancellation cleanup.                     | `src/phase3/native/openat2-replace.c` and exact add-on mirror.                              | Source assertions in `tests/phase3AtomicApply.test.ts`; parent native Linux compile/run remains supplemental.                        |
| P3F-004 | Commit is atomic replacement, not CAS: immediate pre-exchange revalidation, sole `renameat2(RENAME_EXCHANGE)` commit point, no exchange-back or second replacement, post-commit verification, displaced unlink only after confirmed committed, and unknown classification preserving evidence.                 | Helper exchange/verification/cleanup sequence and compile-time test failure hooks.          | Source assertions for exchange-only flow and failure hooks; deterministic adapter classification tests.                              |
| P3F-005 | Tests cover adapter no-spawn validation, exact frame statuses and errors, malformed/missing/trailing stdout, stdin/EPIPE, cancellation/deadlines before and after start, child exit/signal/timeout, zeroing, waiter N/N+1, coordinator-compatible statuses, helper source constraints, and isolation mirrors.  | `tests/phase3AtomicApply.test.ts` and `tests/phase3Isolation.test.ts`.                      | Focused Vitest, format/lint/typecheck/build where authorized, addon context, and `git diff --check`; full verify deferred to parent. |

Behavioural matrix: valid and invalid canonical path; lowercase and uppercase expected/content digests; content empty, N, and N+1; content digest match/drift; active, cancelled, and expired contexts; Linux and non-Linux; absolute normalized and invalid root/helper; max concurrency and waiter N/N+1; spawn failure before start; exact `before_commit`, `committed`, and `commit_unknown` frames; closed helper error enum; missing, malformed, trailing, truncated, and unknown-error stdout; child exit, signal, timeout, forced kill, stdin/EPIPE; pre-spawn and queued cancellation; in-flight pre-exchange cancellation; post-exchange completion despite caller cancellation; caller byte preservation and owned-copy zeroing; C root/parent/target confinement, xattr/special-mode/hardlink denial, expected digest drift, pre-exchange race, exchange-window unknown, post-exchange unknown, no exchange-back, no fallback named temp; root/add-on mirror and runtime/package/config/Dockerfile/mapping isolation.

Deferred and unverified: helper compilation into the add-on image, explicit `libcrypto.so.3` packaging/link validation, native aarch64 execution, RW `/homeassistant` mapping, activation, registration, validation/reload/verification adapters, approval production, and any live Home Assistant mutation.

## Phase 3G inert strict YAML validation adapter acceptance ledger

Risk: HIGH. Phase 3G introduces the future strict YAML validation adapter for the guarded apply workflow, but this slice is inert source/tests/docs only. It remains unregistered, unconfigured, unmapped, unreachable from runtime composition, and live writes remain disabled.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                      | Implementation evidence                                                                                                               | Validation evidence                                                                                                                                                     |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3G-001 | `StrictYamlPhase3Validation` implements `Phase3ValidationPort` for only `candidate_pre_apply`, `candidate_post_apply`, and `checkpoint_post_rollback`; invalid runtime phases, pre-cancelled contexts, and expired deadlines fail before the strict YAML boundary is invoked.                               | `src/phase3/validationAdapter.ts` and exact add-on mirror.                                                                            | `tests/phase3ValidationAdapter.test.ts` phase, cancellation, deadline, and no-boundary tests.                                                                           |
| P3G-002 | Each accepted call copies caller bytes, derives exactly one frozen Phase 2 context with fresh distinct valid UUID request/operation IDs and the caller's exact signal/deadline, passes only the owned copy to a narrow injected `validateStrictYaml` boundary, and zeros the owned copy in `finally`.       | Adapter copy/context derivation/finally zeroization and injectable boundary.                                                          | Success/failure zeroization, caller preservation, context identity, frozen context, UUID, and real strict YAML validation tests.                                        |
| P3G-003 | Known `YamlGateError` values map to `Phase3ValidationError` while preserving gate-derived code, phase, sanitized finite positive line/column, and bounded allowlisted messages; runtime bad bytes become `unsupported_encoding`; unknown failures become safe `internal_failure` at 1/1 without disclosure. | `Phase3ValidationError`, `Phase3ValidationErrorCode` derived from `YamlGateErrorCode` plus `invalid_phase`, and safe message mapping. | Exhaustive `yamlGateErrorCodes x phase` tests, malformed YAML, bad bytes, nonfinite position sanitization, unknown error sanitization, and canary/cause non-disclosure. |
| P3G-004 | Phase 3G is an exact root/add-on mirror and remains inert: no runtime, config, CLI, registry, package, Dockerfile, add-on context, mapping, activation import, live adapter, write enablement, HA API/deployment validation, reload, verification, approval production, or live mutation is added.          | Root/add-on source mirrors and unchanged composition/config/package/deployment files.                                                 | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused tests, lint/typecheck, diff inspection, and `git diff --check`.                                          |

Behavioural matrix: all three validation phases; valid YAML, malformed YAML, implicit empty, explicit empty, exact `PHASE2_MAX_TEXT_BYTES`, and N+1 input; runtime bad bytes; invalid runtime phase; pre-cancelled and expired contexts; strict YAML gate error cross-product for every current gate code and phase; unknown boundary failure; finite and nonfinite source positions; caller byte preservation; owned buffer zeroing on success and failure; one frozen derived Phase 2 context per accepted call; fresh distinct UUIDs; exact caller signal/deadline; root/add-on mirror and import-denial isolation.

Deferred and unverified: HA API validation, deployment-aware validation, reload adapters, verification adapters, approval production, registration, activation, package/container changes, RW mapping changes, live Home Assistant access, and any live Home Assistant mutation.

## Phase 3H/3I inert narrow reload and durable reload-recovery foundation acceptance ledger

Risk: HIGH. Phase 3H/3I defines the future narrow Home Assistant domain reload boundary and durable reload-recovery foundation, but this slice is inert source/tests/docs only. It remains unregistered, unconfigured, unmapped, unreachable from runtime composition, and live writes and reloads remain disabled.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                              | Implementation evidence                                                                                  | Validation evidence                                                                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3H-001 | `NarrowPhase3ReloadAdapter` implements `Phase3ReloadPort` without inferring a service from a filename or directory. Phase 3I callers provide an exact durable target and the injected catalog must re-resolve the canonical path to the same frozen closed target or the closed `unavailable`, `ambiguous`, or `unhealthy` status; every non-resolved or malformed result fails before dispatch.                    | `Phase3ReloadCatalogPort`, exact runtime result checks, and the closed target/resolution contracts.      | Arbitrary/deceptive path tests; all resolution statuses; malformed, stale, unfrozen, and excess-key results; no-dispatch assertions.                           |
| P3H-002 | The only targets are `automation.reload`, `script.reload`, and `scene.reload`; v2 proposal snapshots, grants, and transaction records bind the exact target or null. A narrow injected service port accepts only that target union and returns exactly one frozen `completed`, `not_dispatched`, or `outcome_unknown` result. There is no generic HA request dependency, retry, fallback, broad reload, or restart. | Frozen exported target/outcome arrays and `Phase3ReloadServicePort`; no HA REST/WebSocket client import. | Exhaustive target x outcome tests, source prohibition checks, exact single-dispatch assertions, malformed outcome tests, and typecheck.                        |
| P3H-003 | Canonical path and active context checks occur before catalog effects, and cancellation/deadline are rechecked before dispatch. The exact context crosses both boundaries. Dispatch exceptions and malformed outcomes are conservatively outcome-unknown; errors expose only closed structured classifications and fixed messages.                                                                                  | Adapter ordering and `Phase3ReloadError` extending `Phase3CoordinatorError`.                             | Invalid path, pre/post-resolution cancellation/deadline, context identity, resolver/dispatch exceptions, no-retry, and path/token/canary non-disclosure tests. |
| P3H-004 | Phase 3H/3I is an exact root/add-on mirror and remains inert. `phase3Contract` is unchanged and no runtime, config, registry, package, Dockerfile, add-on mapping, activation, write enablement, live HA access, or mutation is added.                                                                                                                                                                              | Root/add-on source mirrors and unchanged composition/config/package/deployment files.                    | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused tests, full verification, diff inspection, and clean-room validation.                           |

Behavioural matrix: all three targets; resolved, unavailable, ambiguous, unhealthy, stale, malformed, unfrozen, and excess-key catalog results; completed, not-dispatched, outcome-unknown, malformed, unfrozen, excess-key, and thrown dispatch results; arbitrary canonical and invalid paths; active, pre-cancelled, pre-expired, nonfinite, post-resolution cancelled, and post-resolution expired contexts; exact context identity; single resolution and dispatch; no retry, filename inference, generic request, broad reload, fallback, or restart; fixed sanitized errors; exact mirrors and runtime isolation.

At Phase 3I, activation remained blocked pending service-catalog, transport, and verification adapters, approval production, registration, activation wiring, package/container changes, RW mapping changes, live Home Assistant access, and every live mutation. Phase 3I supplies only the durable reload intent and rollback-reload recovery foundation; it does not make reloads reachable from runtime composition.

## Phase 3J inert narrow post-effect verification adapter acceptance ledger

Risk: HIGH. Phase 3J introduces the future post-effect verification adapter, but this slice is inert source/tests/docs only. It remains unregistered, unconfigured, unmapped, unreachable from runtime composition, and live writes and reloads remain disabled.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                | Implementation evidence                                                                                                                 | Validation evidence                                                                                                                                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P3J-001 | `NarrowPhase3VerificationAdapter` implements `Phase3VerificationPort` only for the exact legal durable matrix after strict transaction assertion: candidate `reload_succeeded` with no rollback reload and exact target/predecessor binding; checkpoint `rollback_validation_succeeded` after `rollback_committed` with flag false, or `rollback_reload_succeeded` after `rollback_reload_intent` with flag true and non-null target. | Exact state predicate in `src/phase3/verificationAdapter.ts` and add-on mirror; no coordinator, journal, schema, or observation change. | Every transaction state x both outcomes plus exhaustive predecessor/flag/target matrix in `tests/phase3VerificationAdapter.test.ts`.                         |
| P3J-002 | Verification is exactly source digest, trusted probe, source digest with one exact context, two reads, and no retry. Both reads equal the outcome-selected digest. The frozen request binds transaction, path, outcome, expected digest, impact, reload target, and rollback reload flag.                                                                                                                                             | Injected `Phase3PostEffectSourceDigestPort`, `Phase3TrustedVerificationProbePort`, and ordered adapter flow.                            | Exact order, digest selection, request keys/value/freeze, context identity, call-count, first/final source failure, and no-retry tests.                      |
| P3J-003 | Probe evidence is an exact frozen own-data object with only status, transaction, outcome, and expected digest. Descriptor snapshotting rejects getters/accessors, symbols, excess keys, unfrozen values, hostile ownKeys/descriptor proxies, malformed values, and stale bindings without retaining foreign values.                                                                                                                   | Safe descriptor snapshot and the closed verified, rejected, unavailable, and unhealthy status contract.                                 | Status, stale binding, unfrozen, excess, accessor, symbol, primitive, invalid, hostile proxy, no-property-get, mutation, and sanitization tests.             |
| P3J-004 | Cancellation/deadline are checked before effects and after every fulfilled or rejected boundary before return/error classification. Errors distinguish input/state, source classes, evidence classes, cancellation/deadline, and internal failure with deterministic sanitized code/stage evidence and no causes, paths, content, messages, foreign objects, or unexpected observed digest.                                           | Frozen closed status/stage/code arrays and typed `Phase3VerificationError` extending `Phase3CoordinatorError`.                          | Before/between/after cancellation and deadline return/throw precedence, closed-error, mutation-resistance, source/evidence classification, and canary tests. |
| P3J-005 | Phase 3J is an exact root/add-on mirror on a positive narrow import allowlist and remains inert. It imports no HA REST/WebSocket, application composition, generic request client, system-log listing, entity/log inference, and adds no runtime/config/registry/package/container/mapping/write/live change.                                                                                                                         | Root/add-on verification adapter mirrors; imports limited to Phase 3 coordinator types and contracts.                                   | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused tests, format/lint/typecheck/build, changed-file inspection, and `git diff --check`.          |

Behavioural matrix: every transaction state x candidate/checkpoint; all predecessor, rollback flag, and null/non-null target combinations for the three potentially accepted states; exact candidate/checkpoint digest selection; source first/final malformed, missing, mismatch, and throw; all probe statuses; stale transaction/outcome/digest; unfrozen, excess, accessor, symbol, primitive, invalid, hostile ownKeys/descriptor, and get-trapping evidence; cancellation/deadline before, after each fulfilled boundary, and after each rejected boundary; cancellation-over-deadline precedence; exact context identity, call order/count, two reads, no retry; frozen request and exported contracts; canary, path, upstream message/content, cause, foreign object, and unexpected digest non-disclosure; exact mirrors, positive import allowlist, prohibited broad clients/inference, and runtime isolation.

Trust boundary: exact frozen request/evidence binding proves shape, immutability, and staleness only, not authenticity. The injected probe is trusted semantically. Activation remains blocked pending production Home Assistant loaded-state observation and reload-causality proof for every supported reload target, plus approval production, registration, activation wiring, package/container changes, RW mapping changes, and explicit authorization for any live mutation.

Deferred and unverified in this lane: production probe/source adapters, loaded-state authenticity, reload-causality proof, activation, live Home Assistant access, mutations, full-suite validation, clean-room validation, and independent parent review.

## Phase 3K contextual protected post-effect source digest bridge acceptance ledger

Risk: HIGH. Phase 3K connects two inert source contracts at a hostile runtime shape boundary while preserving protected repository semantics. It changes source/tests/docs only and remains unregistered, unreachable from runtime composition, and unable to perform Home Assistant access, reload, or mutation.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                 | Implementation evidence                                                                                                           | Validation evidence                                                                                                                                                              |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3K-001 | `ProtectedPhase3SourceAdapter` implements both source interfaces with explicit legacy and contextual digest overloads. The verification-port import is type-only and emits no runtime dependency.                                                                                                                                                                                                                                      | Root/add-on `sourceAdapter.ts` exact mirrors and overload declarations.                                                           | Compile-time assignments, execution of both forms, transpilation isolation assertion, typecheck, build, and mirror checks.                                                       |
| P3K-002 | Canonical path validation precedes effects and supplied-context access. Supplied context is accepted as unknown internally and snapshotted once. The signal must be intrinsic-brand-compatible and have no own `aborted` descriptor; non-object, malformed, shadowed, descriptor-trapping, and non-finite/non-number deadline values fail sanitized without retry or fallback.                                                         | Strict `context === undefined` selection; descriptor-first signal shape check; intrinsic getter probe; shared sanitized boundary. | Invalid-path hostile context, malformed values, copied-symbol forged signal with hostile own getter, descriptor proxy trap, getter counts, no-disclosure, and zero-effect tests. |
| P3K-003 | Each valid contextual call derives one frozen Phase 2 context with fresh distinct valid UUID request/operation IDs and exact captured signal/deadline; that same object reaches first freshness, catalog, content read, and final freshness. Legacy one-argument behavior and valid `read(path, context)` remain unchanged. Signal provenance is trusted under the internal coordinator contract, not proven by JavaScript inspection. | Single derived-context construction before shared `readSource`; unchanged legacy/internal and read paths; no provenance claim.    | Context identity/freeze/value/UUID/freshness tests, repeat calls, retained Phase 3E suite, and both interface forms.                                                             |
| P3K-004 | Contextual digest preserves catalog-absence-only null, protected prechecks/races, identity/size bounds, first/final freshness, cancellation/deadline, lowercase digest, sanitization, zero returned digest bytes, boundary-byte zeroization, and no retry/cache.                                                                                                                                                                       | Existing shared protected `readSource` and `finally` zeroization.                                                                 | Success/repeat, absence, protected path/inode, accepted-read races, empty/N/N+1, drift, known/unknown failures, pre/during cancellation/deadline, and zeroization matrix.        |
| P3K-005 | Phase 3K remains an exact inert root/add-on source island with no HA client, observation, reload, runtime/config/registry/package/container/mapping, activation, or mutation dependency. Trusted context provenance remains a future activation/composition prerequisite.                                                                                                                                                              | Type-only bridge plus unchanged runtime composition and exact mirror.                                                             | `tests/phase3Isolation.test.ts`, `pnpm addon:context`, focused tests, format/lint/typecheck/build, exact-path diff inspection, and `git diff --check`.                           |

Behavioural matrix: legacy and contextual digest success/repeats; valid and invalid paths; catalog absence; protected path/inode; accepted-read missing/protected races; empty, N, and N+1 content; root/file/size drift; first/final freshness; known/unknown sanitization and non-disclosure; cancellation/deadline before and during reads; same frozen context at all boundaries; exact signal/deadline snapshot; valid, distinct, fresh UUIDs; no digest bytes returned; zeroization on every bytes-obtained path; null/primitive/malformed signal and deadline values; throwing outer getters; intrinsic-brand-compatible copied-symbol forgery with a hostile own `aborted` getter; signal descriptor proxy trap; context proxy trap; no fallback, retry, cache, canary/path/cause disclosure, or pre-boundary effect.

Shape and provenance boundary: the signal check prevents malformed or shadowed values from reaching effects, including the concrete hostile own-`aborted` getter path. It does not authenticate provenance. A complete unshadowed forgery may be indistinguishable through JavaScript inspection, so `Phase3OperationContext` remains an internal trusted coordinator contract. Activation is `BLOCKED` until runtime composition uses a trusted context factory or equivalent provenance boundary.

Supersession and trust boundary: Phase 3K supersedes Phase 3J's stale deferred `production probe/source adapters` wording only for structural satisfaction of the source port. It proves protected disk/source consistency only within the injected boundaries and trusted context contract, never signal provenance, Home Assistant loaded-state authenticity, or reload causality. Trusted context provenance, runtime composition/package reachability, native-live filesystem proof, production probe, Home Assistant loaded-state observation, reload-causality proof, service transport, approval production, registration, activation, and mutation remain `BLOCKED` or `UNVERIFIED`. This ledger makes no operational-production-readiness claim.

## Phase 3L inert authenticated durable single-use approval source acceptance ledger

Risk: HIGH. Phase 3L adds authenticated durable approval source code and injected logical restart/cross-instance evidence. It remains uncomposed and unable to access or mutate Home Assistant.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Implementation evidence                                                                                                          | Validation evidence                                                                                                                                                                                                                                       |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P3L-001 | Shared strict validation preserves cancellation, lookup/replay, proposal state/expiry, grant issuance/expiry, exact binding, and final-cancellation precedence. Proposal schema/property access is foreign: thrown proxy/getter/accessor values become fixed `proposal_not_pending`, while successful parsing returns a frozen copied snapshot for downstream use. Context/clock values are finite safe integers and signal checks make no provenance claim.                                                           | Root/add-on `approval.ts` exact mirrors and durable copied-snapshot handoff.                                                     | Existing approval suite plus issue/consume foreign `Phase3ApprovalError` proxy/getter sanitization, post-parse revocation, store-reuse, malformed-time, cancellation, and replay tests.                                                                   |
| P3L-002 | `DurablePhase3ApprovalGrants` implements `Phase3ApprovalPort`, requires an existing absolute normalized private root and copied 32-byte key, never creates root/parents, exposes initialize/issue/consume/close, and restricts native durability to Linux.                                                                                                                                                                                                                                                             | Root/add-on `durableApproval.ts` exact mirrors with injected filesystem/durability/hooks/clock/UUID.                             | Platform, lifecycle, issue/consume/restart, mirror, typecheck, and build evidence.                                                                                                                                                                        |
| P3L-003 | Exact strict canonical v1 header/grant/receipt wire uses lower-hex HMAC-SHA256 over exact trailing-NUL domain plus canonical UTF-8 core. Fatal UTF-8, strict schemas, timing-safe compare, key identity, and filename/envelope/receipt binding fail tamper, substitution, wrong key, noncanonical bytes, duplicate IDs, and mismatch closed.                                                                                                                                                                           | Exact schemas, MAC domains, canonical comparison, stable no-follow reads.                                                        | Wire/domain/HMAC, wrong-key, noncanonical/tamper, topology, and restart receipt tests.                                                                                                                                                                    |
| P3L-004 | Fixed root/slot topology is bounded at 293/6. Frozen limits are 256 slots, 4 header stages, 32 grant stages, 4 receipt stages, 1024/4096/1024 bytes, and 120000 ms TTL. Exact maxima stay healthy; unavailable required capacity is non-latching `approval_capacity_exhausted`.                                                                                                                                                                                                                                        | Exact name/range scans and fixed claims.                                                                                         | Frozen export values, exact four header/receipt stage exhaustion and retry, and out-of-range grant-stage tests.                                                                                                                                           |
| P3L-005 | Header initialization uses exclusive staging, stable read, hard link, root sync, authenticated winner reconciliation, same-key race acceptance, different-key rejection, and non-latching stage exhaustion.                                                                                                                                                                                                                                                                                                            | Header commit state machine.                                                                                                     | Same/different-key race, wrong-key, restart, and header-stage tests.                                                                                                                                                                                      |
| P3L-006 | Issuance validates strict pending/unexpired proposal and context at start/precommit, fixes expiry to `min(issue+TTL, proposal expiry)`, and syncs/rereads a complete stage before first-free-slot rename. Authenticated durable refresh rejects an existing grant ID before rename, cleans only the proven owned stage, and regenerates within four total UUID attempts. Reconciliation covers contention, own commit, collision, conflict/absence, and commit-unknown; cancellation is ignored after possible commit. | Grant rename state machine, durable precommit collision admission, stable inode/content checks, four UUID attempts.              | Exact TTL and representative binding/time tests, forced existing-ID collision with pre-regeneration topology observation, two-instance issue contention, committed-but-threw, post-syscall observation failure, and abort/expiry-in-precommit-hook tests. |
| P3L-007 | Consumption checks cancellation, refresh/lookup, replay before validation, then fresh clock/signal pre-link. Receipt reconciliation uses pending/final inode and authenticated content so one race winner succeeds and losers replay.                                                                                                                                                                                                                                                                                  | Receipt hard-link state machine and fixed used stages.                                                                           | Restart replay precedence, two-instance consume contention, representative boundary/binding/cancellation, stale-stage, committed-but-threw, observation-failure, and abort/expiry-in-precommit-hook tests.                                                |
| P3L-008 | Parent sync establishes durability. After possible commit, observation/reconciliation failures are commit-unknown unless stable noncommit or durable evidence exists. Filesystem/durability wrappers and hook handling prevent injected values from selecting trusted approval codes or disclosing boundary text. Post-parent failures cannot become commit-unknown.                                                                                                                                                   | Explicit commit states, private boundary-failure separation, conservative classification, hook stages, and stable cleanup proof. | Header/grant/receipt committed-but-threw and reconciliation-read failures, boundary-thrown `Phase3ApprovalError` code/path/canary probes, precommit sanitization, concurrent scan-window retries, and throwing post-parent hook tests.                    |
| P3L-009 | Repeated or overlapping initialize calls install scans through a zeroing ownership handoff, leaving only the current authenticated buffers retained. Close gates new operations, waits in-flight work, shares a promise, zeros current retained and temporary buffers, and clears state without caller-key/heap/OpenSSL claims.                                                                                                                                                                                        | Lifecycle gate, `replaceScanned` initialization handoff, buffer fills, and failure-path ownership transfer.                      | Sequential and barrier-controlled concurrent initialize tracking, close zeroing, failed parse tracking, idempotent close, post-close rejection, caller-key preservation, and handle-close failure probes.                                                 |
| P3L-010 | HMAC authenticates present artifacts only. Root honesty/non-rollback/deletion protection is assumed; deleting `used.json` or restoring pre-consumption state can re-enable a grant and is unsupported.                                                                                                                                                                                                                                                                                                                 | Explicit Phase 3L threat boundary.                                                                                               | Documentation and source review.                                                                                                                                                                                                                          |
| P3L-011 | Source producer remains inert. The root/add-on modules are built and copied/package-present through existing repository flows, but `phase3Contract.grantProducer` stays `"absent"` because runtime composition imports and activates no producer. No CLI/MCP/config/registry/package-manifest/container/mapping/activation/write/live-HA change occurs.                                                                                                                                                                | Exact eight-path change set, existing build/add-on copy inclusion, and unchanged runtime contracts/composition.                  | Isolation test, add-on context, build/typecheck, exact status/full diff.                                                                                                                                                                                  |

Behavioural evidence in this slice covers issue/consume success; representative proposal state, binding, caller time, malformed trusted clocks, cancellation, and exact expiry boundaries; restart replay; same/different-key initialization; forced existing-ID regeneration and two-instance issue/consume contention; canonical header/grant HMAC and domain values; tamper/noncanonical/topology/range failures; exact header/receipt fixed-stage exhaustion and retry; committed-but-threw and post-syscall observation failures for all three commit types; abort/expiry changes in issue/consume precommit hooks; representative open/write/file-sync/mkdir/hook sanitization including boundary-thrown `Phase3ApprovalError`; deterministic header-cleanup, stage-creation, and rename scan windows; close/parse failure buffer zeroization; post-parent failures; close/key ownership; exact mirrors; and runtime absence. Receipt mismatch, wrong-key authentication, hardlink/nofollow, every N/N+1 topology limit, every hook/crash interleaving, and native Linux process/power-loss behaviour remain requirements enforced by implementation/source review or activation-blocking evidence where not directly exercised by this focused suite; no broader test claim is made.

Activation remains `BLOCKED` on root deletion/rollback protection, native Linux separate-process and power-loss validation, stale-stage retention/remediation, production key provisioning/rotation/zeroization wiring, operator CLI authorization/audit, runtime composition, registration, activation, and explicit authorization for live mutation. Existing module build/copy/package presence is acknowledged but supplies no runtime producer. Native Linux evidence is `UNVERIFIED`; injected Windows tests prove logical state-machine behavior only.

## Phase 3M native durable approval evidence acceptance ledger

Risk: HIGH. Phase 3M adds an inert, repository-owned Linux evidence harness around the already-built Phase 3L approval source. It does not compose or expose an approval producer, change the durable implementation or wire format, provision a key, access Home Assistant, or enable mutation.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Implementation or planned evidence                                                                             | Validation evidence                                                                                                                           | Status      |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| P3M-001 | The harness executes the built `DurablePhase3ApprovalGrants` with default filesystem and durability adapters through strict separate-process `initialize`, `issue`, `consume`, and `close` IPC; hook paths are root-relative and sanitized, and the harness does not duplicate approval HMAC logic.                                                                                                                                                                                                                                                                                                                                        | `scripts/linux/phase3-approval-worker.mjs` and `scripts/linux/phase3-approval-harness.mjs`.                    | Windows source/IPC-contract checks plus external UID 0 Linux harness execution.                                                               | IMPLEMENTED |
| P3M-002 | A frozen registry contains exactly the approved 56 IDs. Evidence is strict JSONL with the manifest first, each registered row exactly once, and the summary last. Malformed evidence or any `FAILED`, `BLOCKED`, or `UNVERIFIED` result exits nonzero.                                                                                                                                                                                                                                                                                                                                                                                     | Frozen registry, strict parser, deterministic emitter, and summary consistency checks in the approval harness. | Parser, registry, duplicate/missing/order, malformed, summary, and exit-status regression checks.                                             | VERIFIED    |
| P3M-003 | Every controlled worker has bounded stdout, stderr, IPC count/bytes, and completion time. Spawn lifecycle handling precedes PID inspection. Timeout, overflow, protocol failure, and requested crash kill the detached process group, await close, and prove no group survivor; cleanup failure remains surfaced and tracked until absence is proven. The detached process-group prerequisite probe unconditionally performs bounded close and absence cleanup, retains probe/termination/absence failures, and passes only after group absence is proven. Linux, UID 0, compiler, and process-group support are completion prerequisites. | Bounded tracked worker controller and prerequisite probe in the approval harness.                              | Controlled missing-executable, live malformed/overflow/timeout/worker-survivor, and probe-survivor tests plus external UID 0 Linux execution. | IMPLEMENTED |
| P3M-004 | Native separate-process evidence covers same-key convergence, different-key winner/loser classification, wrong-key restart recovery, cross-process issue/consume/replay, colliding UUID regeneration, and one-winner consume contention.                                                                                                                                                                                                                                                                                                                                                                                                   | `init:*`, `cross:*`, and `race:*` row handlers.                                                                | External UID 0 Linux harness rows.                                                                                                            | VERIFIED    |
| P3M-005 | Native evidence covers authenticated header/grant/receipt tamper rejection, same-instance fixed-stage exhaustion and retry after one fixture removal, and only the named root/header/scan topology claims.                                                                                                                                                                                                                                                                                                                                                                                                                                 | `tamper:*`, `exhaustion:*`, and `topology:*` row handlers.                                                     | External UID 0 Linux harness rows.                                                                                                            | VERIFIED    |
| P3M-006 | Header, grant, and receipt process kills are classified at precommit, postcommit, and parent-synced hooks: precommit leaves no final and permits retry; postcommit/parent-synced state is authenticated by a fresh process, with receipt replay where applicable.                                                                                                                                                                                                                                                                                                                                                                          | Nine `kill:*` row handlers using sanitized hook IPC.                                                           | External UID 0 Linux harness rows and no-survivor proof.                                                                                      | VERIFIED    |
| P3M-007 | Scoped native fault evidence covers open, failed/short/ENOSPC positioned writes, file/stage/parent sync, link-family, and rename branches. Short writes complete and authenticate; post-final parent-sync failure is `approval_commit_unknown`. The shared shim adds `link`/`linkat` wrappers and a bounded embedded C probe without changing existing persistence validation behavior.                                                                                                                                                                                                                                                    | Twenty-two `fault:*` rows, two `shim:*` rows, `persistence-fault-shim.c`, and embedded probe source.           | External compiler/harness execution and unchanged `validate:linux:persistence`.                                                               | VERIFIED    |
| P3M-008 | The package script is an exact root/add-on manifest mirror. Windows-safe tests exercise parser, registry, fake gating, missing-executable containment, and source contracts; controlled Linux tests exercise live IPC and process-group survivor cleanup. Actual power-cut behavior and grant/receipt native topology beyond named rows remain explicitly `UNVERIFIED`; missing external UID 0 Linux runs are `BLOCKED`, never passed.                                                                                                                                                                                                     | Root/add-on `package.json`, `tests/linuxHarness.test.ts`, `docs/phase3-contracts.md`, and this ledger.         | Mirror/add-on checks, focused Windows/Linux tests, `pnpm verify`, external approval harness, and existing persistence harness.                | VERIFIED    |

Behavioural matrix: Linux/non-Linux; UID 0/non-root; compiler/process-group available/unavailable; missing executable before PID assignment; strict valid/malformed/oversized output and live IPC; normal close/timeout/kill; close with a same-group survivor; same/different/wrong key; issue/consume/replay and contention; header/grant/receipt tamper; fixed-capacity failure and same-instance retry; named private-root/header/scan topology; precommit/postcommit/parent-synced process death; scoped syscall failure, short write, ENOSPC, and parent durability uncertainty; fake seam disabled/enabled. Cancellation, migration, rollback, Home Assistant access, production key provisioning, runtime composition, activation, publication, and live mutation are outside this evidence-only slice.

Validation contract: focused `pnpm exec vitest run tests/linuxHarness.test.ts`; changed-file format/lint/typecheck/build and `git diff --check`; authoritative `pnpm verify`; clean-room `pnpm verify`; and, on an external UID 0 native Linux runner with a compiler and process-group support, `pnpm validate:linux:approval` followed by unchanged `pnpm validate:linux:persistence -- --cc <compiler> --tmpfs-root <tmpfs>`. The current bounded, network-disabled UID 0 Linux runner supplied the compiler, non-tmpfs approval root, process-group support, and bounded persistence tmpfs prerequisites.

Local final-repair evidence on 2026-07-22: focused `linuxHarness.test.ts` contains 14 tests; Windows passed 11 with the three Linux-only cases skipped, and the network-disabled UID 0 Linux runner passed 14/14 with read-only source and dependencies plus an init reaper and external hard timeout. The controlled cases prove bounded missing-executable failure with complete 56-row output and no unhandled rejection; error-before-close with delayed and missing close; missing-PID close observation; exact probe/lifecycle/termination/close/absence aggregation; retained bounded sanitized diagnostics; live malformed/oversized/timeout IPC cleanup; surfaced close failure with retained tracking; and enforced process-group disappearance. Changed-file Prettier and ESLint, Node syntax checks, and `git diff --check` passed; authoritative `pnpm verify` passed 38 files with 1040 tests passed and 9 skipped. After the final controller repair, the external UID 0 Linux approval harness passed 56/56 and the unchanged persistence harness passed 45/45. Independent high-risk review approved the final eight-file change set with no actionable local blocker. Actual power-cut behavior and grant/receipt native topology beyond the named rows remain `UNVERIFIED` and activation-blocking.

## Phase 3N inert exclusive-custody stale-stage remediation acceptance ledger

Risk: HIGH. Phase 3N adds an inert, source-only stale-stage remediation operation under an injected acquisition-first exclusive-root custody lease. It does not change `Phase3ApprovalPort`, automate cleanup, provide a default or production custody provider, or change CLI/MCP/runtime/configuration/package/container/mount/add-on mapping/Home Assistant access or mutation.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Implementation or planned evidence                                                                                                                                | Validation evidence                                                                                                                                                                                      | Status      |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| P3N-001 | Export custody lease/provider and remediation-count types; the option is only `acquireExclusiveRootCustody(root)`. Acquisition precedes scan/effect. Missing, rejected, or malformed acquisition is sanitized, no-effect, and latching. No default/production provider or callback-started concept exists.                                                                                                                                                                                                                                            | Optional provider and exact lease-copy boundary in mirrored `durableApproval.ts`; no composition caller.                                                          | Provider absent/rejected and malformed mutable/excess/symbol/accessor/proxy lease plus proxy-release tests; isolation/import review.                                                                     | IMPLEMENTED |
| P3N-002 | A trusted lease is an exact frozen ordinary object with one own enumerable immutable `release` data property containing a non-proxy function. Release is copied once, called once with the lease receiver in `finally`, awaited, and must fulfill `undefined`; malformed-provider resources remain provider-owned.                                                                                                                                                                                                                                    | `types.isProxy`, descriptor validation, copied release call, and sanitized latching failure path.                                                                 | Exact receiver/once, deferred release, throw, invalid-result, proxy, accessor, and malformed-ownership tests.                                                                                            | IMPLEMENTED |
| P3N-003 | Remediation is class-only and reserves synchronously only while open, healthy, never successfully initialized, and with no in-flight/reserved operation. The reservation remains in flight through acquisition/effects/release; close waits. Capacity-failed healthy initialization permits remediation; unhealthy initialization blocks; overlapping initialize remains when no reservation exists.                                                                                                                                                  | `remediationReserved`, shared in-flight completion, unchanged approval port and normal initialize scheduling.                                                     | Deferred acquisition/release/close, overlapping method rejection, overlapping initialize, initialized/capacity/unhealthy/closed ordering tests.                                                          | IMPLEMENTED |
| P3N-004 | Add narrow filesystem `rmdir(path)` with exact `undefined` result sanitization. Remediation never recursively removes a grant stage and never deletes/rewrites header, slots, final grant, or final receipt.                                                                                                                                                                                                                                                                                                                                          | Public/default/protected filesystem boundaries and grant-stage child-then-`rmdir` implementation.                                                                 | Invalid/no-op/throw/committed-then-threw `rmdir`, exact operation-order, residual topology, and final-artifact tests.                                                                                    | IMPLEMENTED |
| P3N-005 | One remediation-specific scan has no root/slot retry, authenticates every existing final, captures exact root/slot/final/stage/child identity and topology, accepts headerless recognized-stage-only roots for cleanup but never success, and aborts before deletion for transient failure, wrong key, tamper, unknown/unsafe/duplicate/unstable topology. Empty headerless detection syncs root before rejecting. Buffers are zeroed and scan state is never installed.                                                                              | `scanRemediationOnce`, stable reads, existing canonical HMAC parsers, alias validation, local buffer ownership, and empty-root sync/reject path.                  | Root/slot fail-once; complete wrong-key/tampered stages; out-of-range/unknown/unsafe/duplicate/instability; tracked failure zeroization; headerless cleanup, sync/reject, and fresh initialization.      | IMPLEMENTED |
| P3N-006 | Candidate order is numeric header stages, numeric grant stages, numeric slots/receipt stages. Before each candidate the parent retains dev/ino/type/mode/uid/gid and exact expected entries; expected metadata/topology refreshes after owned mutations.                                                                                                                                                                                                                                                                                              | Sorted fixed-width scan arrays plus parent proof/refresh helpers.                                                                                                 | Mixed all-family operation trace, exact-capacity run, same-parent native metadata updates, substitution, and extra-child tests.                                                                          | IMPLEMENTED |
| P3N-007 | File stages are full-identity/content revalidated, non-forcibly removed, explicitly proven absent, immediately parent-synced, and counted only afterward. Grant stages permit exact zero-or-one `grant.json`, remove/sync/prove the child and same immutable empty directory, then narrow-rmdir/prove/sync root.                                                                                                                                                                                                                                      | Effect reconciliation plus a no-follow directory handle pinned through child cleanup, `rmdir`, absence proof, and root sync.                                      | Family matrix; disappearance/no-op/path substitution under descriptor pinning; committed-then-threw; child/sync/rmdir/root-sync/retry faults.                                                            | IMPLEMENTED |
| P3N-008 | Under custody, removal-throw absence is an occurred effect that is synced; exact unchanged is before-effect failure; changed/unknown latches. Sync failure is durability-unknown. Before established-store success, a fresh instance reconciliation-syncs root and every existing slot in deterministic order, closing prior deletion uncertainty even when the stage is absent. Failure after valid acquisition may leave partial durable progress, latches, and requires an idempotent fresh-instance retry. Power loss before sync is not claimed. | Conservative observation, parent sync before count, ordered terminal reconciliation syncs, instance latch, no rollback attempt.                                   | File/receipt/root sync faults, visibly ordered fresh-instance parent resync, second reconciliation failure, partial progress, residual state, and fresh retry.                                           | IMPLEMENTED |
| P3N-009 | A two-link header/receipt stage must be the single alias of its authenticated final. After unlink/sync, the final is reauthenticated with identical dev/ino/type/mode/uid/gid/size/bytes and link count two-to-one; only time changes are allowed. Unlinked stage removal leaves finals exact. Ordered root/slot reconciliation syncs and a strict final full scan with immutable root/slot proof precede frozen exact count-only success.                                                                                                            | Alias topology proof, post-unlink final reread/authentication, ordered reconciliation syncs, mutable expected alias metadata, and final strict rescan/comparison. | Mixed nlink-one/nlink-two header/receipt stages, zero-candidate reconciliation, final byte/metadata/link assertions, injected final mutation, frozen exact count keys, and final topology tests.         | IMPLEMENTED |
| P3N-010 | Phase 3N remains an exact six-file inert change. Native production remediation/custody, process death before sync, actual power cut, production provider, key lifecycle, operator authorization/audit, rollback protection, runtime composition, registration, activation, and live mutation remain blocked or unverified.                                                                                                                                                                                                                            | Exact mirrored source, focused tests, isolation allowlist, contract, and this ledger only.                                                                        | Exact-path diff versus `2847044`, byte mirror, focused/static/build/full/clean-room validation, Linux image probe, and independent high-risk review; unavailable evidence remains explicitly unverified. | IMPLEMENTED |

Behavioural matrix: absent/rejected/malformed/valid acquisition; exact/mutable/excess/symbol/accessor/proxy lease and proxy release; release success/throw/invalid result/deferred settlement/receiver/once; open/closing/closed, initialized/uninitialized, healthy/capacity-failed/unhealthy, idle/in-flight/reserved lifecycle; overlapping initialize with and without remediation; headerless/header/slots; empty/partial/complete and one-link/two-link header/grant/receipt stages; empty-headerless sync/reject/fresh initialization; zero/exact/mixed capacity; root/slot scan transient failure without retry; wrong key, complete invalid stage, final/stage tamper, unknown/out-of-range/unsafe/duplicate/unstable topology; deterministic candidate order; parent metadata changes; file absence/no-op/substitution/committed-then-threw; grant child/stage/rmdir/root-sync faults; ordered terminal root/slot reconciliation, second sync failure, partial progress, and fresh retry; final mutation/rescan; buffer zeroization on success/failure; result shape/freeze; Windows injected logical behavior and native Linux evidence boundary.

Validation contract: focused `pnpm exec vitest run tests/phase3DurableApproval.test.ts tests/phase3Isolation.test.ts --maxWorkers=1 --testTimeout=30000`; changed-file Prettier/ESLint, repository typecheck/build, `git diff --check`, exact six paths versus `2847044`, and byte-mirror proof; authoritative `CI=true pnpm verify`; clean-room exact source snapshot excluding `.git`, `node_modules`, and `dist`, followed by `pnpm install --offline --frozen-lockfile` and `CI=true pnpm verify`; independent high-risk review; and a bounded probe for an available offline UID 0 Linux compiler image capable of full `CI=true pnpm verify`. Phase 3M's historical `56/56` approval and `45/45` persistence evidence remains evidence at base `2847044`, not current Phase 3N native regression evidence. If full Linux verification is unavailable or fails only on an established baseline prerequisite, current native Phase 3N regression status is `UNVERIFIED`, never passed.

Blocked boundary: Phase 3N satisfies only inert source/state-machine stale-stage remediation. Native production remediation and custody proof, production custody-provider implementation, process-death and power-loss behavior before containing-directory sync, actual power-cut evidence, key provisioning/rotation/zeroization, operator workflow/authorization/audit, root deletion and rollback protection, runtime composition, registration, activation, publication, release, and live mutation remain `BLOCKED` or `UNVERIFIED`.

Local implementation evidence on 2026-07-23: the mandated focused pnpm command was blocked before Vitest by the Windows PowerShell execution policy (`PSSecurityException`), and the native pnpm shim was separately blocked inside the restricted sandbox by an unavailable user-state database. The equivalent installed local Vitest binary passed 49/49 focused tests after repair. Changed-file Prettier, root/tests ESLint, add-on ESLint with `--no-ignore`, repository typecheck, alternate-output build, mirror SHA-256, and `git diff --check` passed. Authoritative elevated `CI=true pnpm verify` passed 38 files with 1053 tests passed and 9 skipped. A unique system-temp source copy excluding `.git`, `node_modules`, and `dist` completed an offline frozen install with 256 packages reused and passed the same full verification.

The available network-disabled UID 0 amd64 Linux build-runner supplied Node 22.17.1, pnpm 11.7.0, Alpine C compiler 14.2.0, an offline pnpm store, an init reaper, read-only source, and a tmpfs clean room. Phase 3N focused evidence passed 47 tests with its two Windows-only cases skipped. Full Linux verification passed the complete Phase 3N file and 1052 repository tests, but remained `BLOCKED` overall by four unrelated `spawn openssl ENOENT` tests and one existing case-sensitive proposal assertion; it exited with 5 failures and 5 skips. This is native Linux regression evidence for the inert state machine only, not native production custody/remediation, process-death-before-sync, or power-loss evidence.

Repair-cycle 1 evidence on 2026-07-23: the installed focused Vitest command passed 51/51 tests after one causal test-fixture assertion repair. Six-file Prettier, root/test ESLint, add-on ESLint with `--no-ignore`, and repository `tsc --noEmit` passed. Authoritative Windows `CI=true pnpm.cmd verify` exited zero with 38 files passed, 1055 tests passed, and 9 skipped; it included add-on context, whole-repository format/lint/typecheck/build, and tests. In the existing offline network-disabled UID 0 Linux image, `pnpm validate:linux:approval` exited zero with 56/56 rows passed. `pnpm validate:linux:persistence --cc cc --tmpfs-root /evidence` exited zero with 45/45 rows passed after the runner's evidence tmpfs was explicitly capped at its required 128 MiB safety bound. The known-failing full Linux baseline was not rerun.

Final repaired-snapshot clean-room evidence on 2026-07-23: an external source-only copy excluded `.git`, `node_modules`, `dist`, and `.tmp`; `pnpm.cmd install --offline --frozen-lockfile` reused all 256 packages and downloaded zero; and `CI=true pnpm.cmd verify` exited zero with 38 files passed, 1055 tests passed, and 9 skipped. The root/add-on `durableApproval.ts` SHA-256 was `4BDF4FDD9C254CA44E913BBE364378A12F353688077B2C8AE2B131E010F4D97C`. This clean-room run validates the repaired source snapshot but does not supply native production custody/remediation, process-death-before-sync, or power-loss evidence.

## Phase 3O inert conditional Linux advisory-custody acceptance ledger

Risk: HIGH. Phase 3O adds only an explicitly constructed, uncomposed advisory-custody adapter implementing the existing Phase 3N provider contract plus native evidence. It is not production custody and does not remove Phase 3N's production-custody or activation blockers.

| ID      | Observable requirement                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Implementation or planned evidence                                                                                                                    | Validation evidence                                                                                                                                                                                                                                                                          | Status      |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| P3O-001 | Exact nine-file inert scope; no package/lock/Docker/config/mount/runtime/index/CLI/MCP/audit/durable-approval change, activation, packaging, or Home Assistant access/mutation.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Mirrored standalone adapter/helper, focused/isolation tests, harness, and two docs only.                                                              | Exact-path diff from `e886832`; isolation composition/import allowlists; add-on mirror hashes; full diff review.                                                                                                                                                                             | IMPLEMENTED |
| P3O-002 | Linux direct helper installs parent-death signal before locking and parent recheck; no fork/exec/thread/dlopen; no-follow directory open; filesystem allowlist precedes current-owner/private-mode checks; exclusive advisory `flock`.                                                                                                                                                                                                                                                                                                                                                                                                     | `approval-custody.c` in exact mirrors.                                                                                                                | U033 source invariant; strict hardened compile; N001–N020 including `/proc` exact frame/67, parent death, UID drop, contention, handoff, and rejection matrix.                                                                                                                               | VERIFIED    |
| P3O-003 | Exact bounded ASCII startup/control protocol, canonical UINT fields, closed failure codes/exits, no stderr or post-ready output, and only proved running-state `stdin.end(0x52)`+EOF/exit0/close fulfills release.                                                                                                                                                                                                                                                                                                                                                                                                                         | C frame/control state, TypeScript parser, and explicit `controlSent` proof.                                                                           | U007–U010/U017/U018/U026 including exit-0-before-release/close; N020–N024; strict frame/exit assertions.                                                                                                                                                                                     | VERIFIED    |
| P3O-004 | Actual platform for real runner; fake-only platform substitution; normalized path/options; asynchronous pre/post helper/root/fs checks under one invocation-started monotonic acquisition deadline; late preflight cannot spawn and late post-ready validation cannot resolve; no helper-authenticity claim.                                                                                                                                                                                                                                                                                                                               | Promise-based native/fake runner boundary and shared deadline race.                                                                                   | U001–U006/U011/U016/U027 including deferred preflight and post-ready timeout regressions; N015–N020.                                                                                                                                                                                         | VERIFIED    |
| P3O-005 | Direct-child lifecycle installs listeners before PID, signals only a running child through `child.kill`, disables signals after observed exit, settles within operation+grace, retains records until close, and uses one atomic `terminationStarted` entry for at most one TERM, grace timer, and KILL while deferred ready resolution requires no termination/failure. Release after termination preserves the active grace timer and sends no control; grace expiry durably retains `cleanup_unproved`, detaches handles once, and gives later shared release calls an immediate bounded result without releasing capacity before close. | Per-child `running`/`exited_waiting_close`/`closed` controller, termination latch, retained terminal release error, and idempotent handle detachment. | U011–U022/U030–U032 including deferred-ready trailing stdout/stderr/child/stdin failure, release before/after grace expiry, ignored-TERM exact TERM/KILL, missing PID, error-before-close, causal close-before-release, queued late events, shared release, and late close capacity release. | VERIFIED    |
| P3O-006 | Public failures are the seven closed classifications with fixed sanitized messages; matching startup failure may retain only a closed helper code; protocol causality is not overwritten by an induced signal.                                                                                                                                                                                                                                                                                                                                                                                                                             | `Phase3ApprovalCustodyError` and precedence rules.                                                                                                    | U008–U020/U026–U027; N015 repaired regression.                                                                                                                                                                                                                                               | VERIFIED    |
| P3O-007 | Lease is exactly frozen with one immutable enumerable `release`; repeated/concurrent release shares one promise; success requires clean protocol; failures use identical cleanup lifecycle.                                                                                                                                                                                                                                                                                                                                                                                                                                                | Controller-owned exact lease and cached release promise.                                                                                              | U023–U025/U031; N007/N011/N025.                                                                                                                                                                                                                                                              | VERIFIED    |
| P3O-008 | Capacity is eight live records per provider and 32 module-global before spawn, with rejected/unproved records retained until later close.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Pre-spawn counters released only by child close.                                                                                                      | U022/U029/U030.                                                                                                                                                                                                                                                                              | VERIFIED    |
| P3O-009 | Native evidence is exactly 25 mandatory ordered JSONL rows with manifest first and summary last; unknown/missing/duplicate/reordered/nonpassed evidence exits nonzero; libc collects affirmative Node-report, successful identifying `ldd`, and loaded-library families and accepts exactly one unique family; environment, filesystem family, compiler, and helper hash are recorded.                                                                                                                                                                                                                                                     | Import-safe `phase3-custody-harness.mjs` with injectable libc consensus boundary.                                                                     | U033 missing/nonzero/empty/ambiguous and mixed-ldd/mixed-library/cross-source contradiction regressions; offline UID 0 Linux run: valid registry, 25/25 passed, tmpfs native-tested, four other allowlisted families permitted only.                                                         | VERIFIED    |
| P3O-010 | Claim remains conditional cooperative same-inode/same-mount-namespace advisory exclusion while helper lives. Initialize/issue/consume do not participate. Holder death mid-remediation, pathname replacement/rollback/deletion/transient restore, namespaces, nonparticipants, authentic race-safe helper launch, descriptor-relative operations, packaging, and production custody remain blocked.                                                                                                                                                                                                                                        | Explicit contract/ledger ceilings; no composition caller.                                                                                             | Isolation review, N012–N015 bounded evidence, and full diff; independent high-risk review remains required for a final `PASSED` classification.                                                                                                                                              | IMPLEMENTED |

Behavioural matrix: actual/fake platform; root/helper absolute normalized and invalid paths; asynchronous/deferred preflight and post-ready validation; root/helper symlink/type/owner/mode/link/metadata/fs drift; exact/malformed/oversized/trailing/duplicate startup and control frames; matching/mismatched failure exits; spawn throw, missing PID, child error, exit, close, signal, stdin error, stdout/stderr; ready and pre-ready failure; contention and independent roots; latched operation/protocol/stderr/child/stdin termination, exact TERM/KILL bounds, failed kill, observed-exit signal suppression, exit-0-before-control, final-settlement late events, late close and retained capacity; concurrent/repeated release success/failure; provider/global capacity; affirmative/absent/nonzero/empty/ambiguous/mixed-source libc provenance; current UID 0 and UID drop; helper/holder/waiter parent death; pathname replacement; Phase 3N acquisition/release mechanics; supported filesystem family reporting.

Validation contract: focused `pnpm exec vitest run tests/phase3ApprovalCustody.test.ts tests/phase3Isolation.test.ts --maxWorkers=1 --testTimeout=30000`; changed-file Prettier/ESLint, repository typecheck/build/add-on context, exact mirror/scope/diff checks, and direct `pnpm build && node scripts/linux/phase3-custody-harness.mjs`; authoritative `CI=true pnpm verify`; external source clean room excluding `.git`, `node_modules`, `dist`, and `.tmp`, offline frozen install, full verify, build, and the native harness from that same snapshot; independent high-risk review. Missing platform/family evidence is `UNVERIFIED`, never passed.

Native evidence on 2026-07-23: the existing network-disabled UID 0 amd64 Linux build-runner used kernel `6.18.33.2-microsoft-standard-WSL2`, Node 22.17.1, affirmative `musl loaded-library /lib/ld-musl-x86_64.so.1` provenance, Alpine C 14.2.0, an init reaper, read-only source, and a bounded executable tmpfs. After bounded strict-compile, protocol-causality, and lifecycle repairs, the final manifest, all N001–N025 rows, and summary were valid and the harness exited zero with 25/25 passed. The final helper SHA-256 was `0fcaebf42af2f7a0532df1a77702ee8e14db3740e0456a1ce871f2f3fd19c32e`. Tmpfs `0x01021994` was `native_tested`; ext2/3/4, XFS, Btrfs, and OverlayFS remain only `permitted`, not native-tested.

Repair-cycle-1 evidence on 2026-07-23: deferred fake boundaries proved preflight timeout with no late spawn and post-ready timeout with no late lease; queued late stdout, stderr, child error, and stdin EPIPE after `cleanup_unproved` caused no later signal while every child/stdio handle was destroyed and unreferenced, and later close retained sole capacity-release authority; exit 0 before release/close failed `holder_lost` without sending control; and import-safe libc tests rejected missing, nonzero, empty, and ambiguous `ldd` evidence. Focused tests exited zero with 41/41 passed. Changed-file formatting, ESLint, add-on ESLint/context, typecheck, build, syntax, mirror, and diff checks passed. Authoritative `CI=true pnpm.cmd verify` exited zero with 39 files passed, 1090 tests passed, and 9 skipped. A refreshed external source snapshot excluded `.git`, `node_modules`, `dist`, `.tmp`, and `.pnpm-store`; its offline frozen install reused 256 packages and downloaded zero, full verify again passed 39 files/1090 tests with 9 skipped, and its network-disabled UID 0 native harness passed the valid ordered N001–N025 registry 25/25. Root/add-on TypeScript SHA-256 was `43bf0f4173e37c527cdb4a6bddb919fa3430fd5d4bc37f09572892b1dfd6cc40`; root/add-on C-source SHA-256 was `8822a8c4c1c25a927433e70354136ccfc57c8ac9ab7d37f8dfb564565148467d`; compiled-helper SHA-256 remained `0fcaebf42af2f7a0532df1a77702ee8e14db3740e0456a1ce871f2f3fd19c32e`. A fresh independent post-repair verdict remains required before final `PASSED`.

Repair-cycle-2 workspace and native evidence on 2026-07-23: one `terminationStarted` latch made operation-timeout and failure termination entry idempotent, and timer installation is suppressed after that latch. Deferred-ready trailing stdout, stderr, child error, and stdin EPIPE each rejected without resolving a lease; a pending post-ready timeout whose child ignored TERM produced exactly one TERM and one KILL. Libc fixtures rejected mixed identifying `ldd` text, mixed loaded-library paths, and Node-glibc versus musl cross-source contradictions. Focused tests exited zero with 41/41 passed; formatting, ESLint, add-on ESLint/context, typecheck, syntax, exact mirror, and build checks passed. Authoritative `CI=true pnpm.cmd verify` exited zero with 39 files passed, 1090 tests passed, and 9 skipped. The network-disabled UID 0 Linux harness retained affirmative `musl loaded-library /lib/ld-musl-x86_64.so.1` consensus and passed the valid ordered N001–N025 registry 25/25. Root/add-on TypeScript SHA-256 was `24ee93d92bc8e73947cc4e6cec2e77c80becd3fa49c3f0b7689e88f7b84b63b6`; root/add-on C-source SHA-256 remained `8822a8c4c1c25a927433e70354136ccfc57c8ac9ab7d37f8dfb564565148467d`; compiled-helper SHA-256 remained `0fcaebf42af2f7a0532df1a77702ee8e14db3740e0456a1ce871f2f3fd19c32e`. A fresh independent post-repair verdict remains required before final `PASSED`.

Repair-cycle-3 evidence on 2026-07-23: release after termination no longer clears the active grace timer or sends control. Release requested before grace expiry rejected within that existing bound after exactly one TERM and one KILL; grace expiry before release retained `cleanup_unproved`, destroyed and unreferenced every child handle once, made concurrent/repeated release immediately share the same rejection without a new signal or timer, and retained provider capacity until late close. A close before release preserved its causal protocol result. Focused tests exited zero with 41/41 passed; add-on context, formatting, ESLint, typecheck, build, harness syntax, exact mirror, and diff checks passed. Workspace and external source-clean-room `CI=true pnpm verify` runs each exited zero with 39 files passed, 1090 tests passed, and 9 skipped; the clean room used an offline frozen install that reused 256 packages and downloaded zero. Workspace and clean-room network-disabled UID 0 Linux harnesses each passed the valid ordered N001–N025 registry 25/25 with affirmative `musl loaded-library /lib/ld-musl-x86_64.so.1` evidence. Root/add-on TypeScript SHA-256 was `bfc9de38ce3b2919488b47f3f342a9dcfdc867dcb71d21a37f976af206f154d7`; root/add-on C-source SHA-256 remained `8822a8c4c1c25a927433e70354136ccfc57c8ac9ab7d37f8dfb564565148467d`; compiled-helper SHA-256 remained `0fcaebf42af2f7a0532df1a77702ee8e14db3740e0456a1ce871f2f3fd19c32e`. The final exact snapshot must retain these gates, and a fresh independent post-repair verdict remains required before final `PASSED`.

Blocked boundary: Phase 3O proves only the exact conditional advisory behavior above. It does not prove production custody, mandatory exclusion, helper authenticity or race-safe launch, mount-namespace equivalence, protection from nonparticipants, continuous pathname binding across rename/replacement/rollback/deletion/transient restore, holder survival through remediation, descriptor-relative remediation, production packaging/composition, key/operator/audit workflow, process/power-loss remediation safety, activation, release, publication, or live mutation.

## Phase 3P inert create-once approval-key provisioning and existing-only loading

Risk: `HIGH`. This slice introduces persistent cryptographic key creation and loading primitives, but remains an unregistered source island. Exact allowed writes are the two mirrored source modules, one focused test, the existing isolation test, and these two docs. Runtime/index/config/package/container/mapping/CLI/MCP/audit/store modules, store creation, recovery mutation, composition, registration, activation, publication, release, deployment, and live mutation are prohibited.

| ID      | Observable requirement                                                                                                                                                                                                                                                                  | Implementation or planned evidence                                                                                                                              | Validation evidence                                                                                                                                                                                                                                             | Status      |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| P3P-001 | `provisionPhase3ApprovalKey` is explicit create-once and returns no key; `loadPhase3ApprovalKey` is existing-only; neither API mkdirs, creates on load, replaces, unlinks, repairs, rotates, deletes, or automatically recovers.                                                        | `src/phase3/approvalKey.ts` exact mirror with only crypto/fs/path imports and no mutation API beyond direct exclusive create/write/sync.                        | Focused API, missing, existing, retry, isolation import, mirror, and exact-scope tests.                                                                                                                                                                         | IMPLEMENTED |
| P3P-002 | Public errors are one `Phase3ApprovalKeyError`, the exact seven-code closed union, fixed sanitized messages, and no cause or injected text.                                                                                                                                             | Closed frozen code registry and fixed message record; every injected boundary is normalized.                                                                    | Error-shape/message/cause and filesystem/random/process canary tests.                                                                                                                                                                                           | VERIFIED    |
| P3P-003 | Paths are absolute/normalized; key is a direct child; Linux/UID equality and required no-follow/directory flags are proved before effects; parent is pinned no-follow, real-UID, `0700`, stable device/inode and revalidated through close.                                             | Configuration gate, retained directory handle, repeated descriptor/path checks.                                                                                 | Invalid path/platform/UID/flag, parent metadata, descriptor/path substitution, and every revalidation-boundary fault tests.                                                                                                                                     | VERIFIED    |
| P3P-004 | Provision uses one exact nonzero 32-byte random scratch, direct final `O_CREAT + O_EXCL + O_WRONLY + O_NOFOLLOW` mode `0600`, exact bounded positioned write, key and directory fsyncs, stable key metadata/identity, and complete close sequence; scratch is always wiped.             | Native crypto/fs boundary, intrinsic malformed-view wipe, and 64-attempt I/O ceiling.                                                                           | Exact flags/mode/order, scratch and malformed prototype-removed Buffer/DataView backing wipes, short write, bounded EINTR, no-progress, overrun, post-write descriptor metadata, final identity, and full filesystem-boundary matrix tests.                     | VERIFIED    |
| P3P-005 | Exclusive-open fulfillment separates precommit errors from commit-unknown. EEXIST is exists without inspection. Every later primary/cleanup failure is commit-unknown and leaves the artifact untouched; retry cannot upgrade it.                                                       | Explicit commit-state latch and no unlink/retry path.                                                                                                           | Every pre/post boundary fault, unsafe post-write descriptor metadata, final dev/inode substitution, symlink EEXIST, cleanup, artifact/retry/load-after-unknown tests.                                                                                           | VERIFIED    |
| P3P-006 | Load performs stable no-follow parent/key snapshots, exact 32-byte positioned read plus EOF, post-read descriptor/path checks, close-before-lease, candidate wipe on failure, and exact frozen lease with one stable mutable buffer and synchronous repeatable zeroing release.         | Existing-only load and exact lease construction after cleanup; release uses a fixed 32-index bound independent of exposed buffer metadata or prototype methods. | Missing/later disappearance, metadata/type/UID/mode/link/size/zero, short/EINTR/no-progress/overrun/EOF/trailing/drift, cleanup precedence, candidate wipe, and lease tests with shadowed length/byteLength/fill and removed prototype.                         | VERIFIED    |
| P3P-007 | Contention yields one success/one exists. A loader may observe complete bytes before provision completion, but receives readability only; callers serialize and initialize a store only after independently known successful provision or future reconciliation.                        | No coordinator/marker/state in lease.                                                                                                                           | Injected contention and deterministic post-write/pre-sync racing-loader test where provision later reports commit-unknown.                                                                                                                                      | VERIFIED    |
| P3P-008 | `/data/phase3` is key state only. Test-only bridge uses a distinct pre-existing nonnested store root after known successful provision, proves same-key convergence/wrong-key rejection, closes stores before lease release, and retains independent store constructor-copy zeroization. | No production bridge or store import in source; bridge exists only in focused test.                                                                             | Gated native Linux distinct-root durable bridge and UID mismatch test.                                                                                                                                                                                          | IMPLEMENTED |
| P3P-009 | Native default behavior and real filesystem evidence are Linux-only; native success/load, O_EXCL contention, nofollow/symlink/mode, and distinct-root bridge run in the focused test itself. Native evidence is syscall evidence, not process-death or power-cut evidence.              | Default Node Linux fs/crypto/process boundary.                                                                                                                  | Pinned native Linux/Docker focused run passed 23/23 with network disabled and the repository mounted read-only; each native test removed and proved absence of its unique tracked parent.                                                                       | VERIFIED    |
| P3P-010 | Exact mirrors and inert isolation remain; no approval-key symbol or module appears in runtime composition, and the sole positive importer is the focused test.                                                                                                                          | Isolation mirror list, runtime token prohibition, source import allowlist, recursive TypeScript-AST module-specifier allowlist.                                 | AST coverage includes static import, export-from, dynamic import, require, `module.require`, `require.resolve`, and import-equals; focused isolation, add-on context, full validation stages, exact hash/scope/diff review, clean room, and independent review. | IMPLEMENTED |

Behavioural matrix: provision/load; absent/existing/symlink key; native/injected platform; absolute/relative/unnormalized/NUL/nondirect paths; available/missing/mismatched UID; present/missing required flags; parent lstat/open/fstat/revalidation/fsync/close; parent type/owner/mode/device/inode; random throw/type/length/zero/nonzero and scratch wipe; exclusive-open EEXIST/other failure/fulfillment; positioned short/EINTR/no-progress/overrun/exhausted write/read; key fsync/fstat/close/lstat; key type/owner/mode/link/size/zero/device/inode; initial/later ENOENT and ELOOP; exact EOF/truncation/trailing data; pre/post key and parent drift; first-primary plus cleanup; candidate and lease zeroization; two provisioners; post-write/pre-sync loader; commit-unknown artifact/retry/readability; known-success distinct-root store bridge, same/wrong key, and close/release ordering; Windows injected logic and native Linux gating.

### Phase 3P exact boundary/error/artifact matrix

| Operation or boundary                                                       | State/checkpoint                                             | Result or injected condition                                                    | Public error                                                 | Artifact/lease rule                                                 |
| --------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------- |
| Option property/path validation                                             | before filesystem/random                                     | nonstring, empty, NUL, relative, unnormalized, or key not direct child          | `approval_key_invalid`                                       | No filesystem/random call; no artifact/lease.                       |
| Process platform/getuid/geteuid                                             | before filesystem/random                                     | non-Linux, unavailable/throwing/noninteger/negative, or real/effective mismatch | `approval_key_invalid`                                       | No filesystem/random call; no artifact/lease.                       |
| Filesystem flags                                                            | before filesystem/random                                     | missing/invalid `O_DIRECTORY`, `O_NOFOLLOW`, or another required open flag      | `approval_key_invalid`                                       | No filesystem/random call; no artifact/lease.                       |
| Parent initial `lstat`                                                      | pre-exclusive / pre-key-load                                 | boundary failure, including missing parent                                      | `approval_key_io_failure`                                    | No key effect and no lease.                                         |
| Parent initial `lstat` metadata                                             | pre-exclusive / pre-key-load                                 | symlink/non-directory, wrong real UID, or mode other than `0700`                | `approval_key_unsafe`                                        | No key effect and no lease.                                         |
| Parent `open(O_RDONLY\|O_DIRECTORY\|O_NOFOLLOW)`                            | pre-exclusive / pre-key-load                                 | any failure or malformed handle                                                 | `approval_key_io_failure`                                    | No key effect and no lease; trusted opened handles are closed once. |
| Parent initial descriptor `stat`                                            | pre-exclusive / pre-key-load                                 | boundary/malformed result                                                       | `approval_key_io_failure`                                    | No key effect and no lease; directory cleanup follows.              |
| Parent initial descriptor metadata/identity                                 | pre-exclusive / pre-key-load                                 | unsafe metadata or device/inode differs from initial path snapshot              | `approval_key_unsafe`                                        | No key effect and no lease.                                         |
| Parent checkpoint descriptor `stat` or path `lstat`                         | before random, before exclusive open, before load key access | boundary failure                                                                | `approval_key_io_failure`                                    | No key effect yet; first-primary survives directory cleanup.        |
| Parent checkpoint metadata/identity                                         | before random, before exclusive open, before load key access | unsafe metadata or persistent substitution                                      | `approval_key_unsafe`                                        | No key effect yet; first-primary survives directory cleanup.        |
| Random invocation                                                           | pre-exclusive                                                | throws                                                                          | `approval_key_io_failure`                                    | No key artifact; any obtained scratch is wiped.                     |
| Random return contract                                                      | pre-exclusive                                                | non-Buffer, length other than 32, or all zero                                   | `approval_key_invalid`                                       | No key artifact; mutable returned bytes are wiped where available.  |
| Key exclusive absolute-path open                                            | pre-fulfillment                                              | `EEXIST`, including symlink                                                     | `approval_key_exists`                                        | Existing artifact is not inspected, replaced, removed, or repaired. |
| Key exclusive absolute-path open                                            | pre-fulfillment                                              | other failure                                                                   | `approval_key_io_failure`                                    | No fulfilled create claim; no retry.                                |
| Key exclusive absolute-path open                                            | fulfillment boundary                                         | handle fulfills, including malformed returned handle                            | later failure is `approval_key_commit_unknown`               | Artifact may exist; it is left untouched.                           |
| Positioned key `write`                                                      | post-fulfillment                                             | positive short write                                                            | continue within exact remaining range and 64-attempt ceiling | Same final artifact only.                                           |
| Positioned key `write`                                                      | post-fulfillment                                             | `EINTR`                                                                         | continue only within 64-attempt ceiling                      | Bounded retry claim only.                                           |
| Positioned key `write`                                                      | post-fulfillment                                             | other failure, zero progress, overrun/malformed count, or attempt exhaustion    | `approval_key_commit_unknown`                                | Partial/complete final artifact remains untouched; scratch wiped.   |
| Key descriptor `fsync`                                                      | post-fulfillment                                             | any failure/malformed result                                                    | `approval_key_commit_unknown`                                | Final artifact remains untouched.                                   |
| Key descriptor `stat`                                                       | post-fulfillment                                             | boundary/malformed result or unsafe/corrupt metadata                            | `approval_key_commit_unknown`                                | Final artifact remains untouched.                                   |
| Provision key `close`                                                       | post-fulfillment                                             | failure/non-undefined result                                                    | `approval_key_commit_unknown`                                | Close is not retried; artifact remains untouched.                   |
| Provision final key `lstat`                                                 | post-fulfillment                                             | boundary failure, disappearance, unsafe/corrupt metadata, or identity drift     | `approval_key_commit_unknown`                                | Artifact/path state remains untouched.                              |
| Provision post-key parent descriptor/path revalidation                      | post-fulfillment                                             | boundary, metadata, or identity failure                                         | `approval_key_commit_unknown`                                | Artifact remains untouched.                                         |
| Retained parent descriptor `fsync`                                          | post-fulfillment                                             | any failure/malformed result                                                    | `approval_key_commit_unknown`                                | Artifact remains untouched.                                         |
| Provision final parent descriptor/path revalidation                         | post-fulfillment                                             | boundary, metadata, or identity failure                                         | `approval_key_commit_unknown`                                | Artifact remains untouched.                                         |
| Provision directory `close`                                                 | post-fulfillment                                             | failure/non-undefined result                                                    | `approval_key_commit_unknown`                                | Success is forbidden; close is not retried.                         |
| Load initial key `lstat`                                                    | first key observation                                        | `ENOENT` only                                                                   | `approval_key_missing`                                       | No key creation and no lease.                                       |
| Load initial key `lstat`                                                    | first key observation                                        | other boundary failure                                                          | `approval_key_io_failure`                                    | No lease.                                                           |
| Load initial key metadata                                                   | first key observation                                        | nonregular/wrong UID/mode/link                                                  | `approval_key_unsafe`                                        | No lease.                                                           |
| Load initial key metadata                                                   | first key observation                                        | static size other than 32                                                       | `approval_key_corrupt`                                       | No lease.                                                           |
| Load absolute-path `open(O_RDONLY\|O_NOFOLLOW)`                             | after initial key snapshot                                   | `ENOENT` or `ELOOP`                                                             | `approval_key_unsafe`                                        | Later disappearance/drift; no lease.                                |
| Load absolute-path open                                                     | after initial key snapshot                                   | other failure/malformed handle                                                  | `approval_key_io_failure`                                    | No lease.                                                           |
| Load initial key descriptor `stat`                                          | after open                                                   | boundary/malformed result                                                       | `approval_key_io_failure`                                    | No lease.                                                           |
| Load initial descriptor metadata/identity                                   | after open                                                   | snapshot drift or unsafe metadata                                               | `approval_key_unsafe`                                        | No lease.                                                           |
| Load initial descriptor size                                                | after open                                                   | static wrong size without earlier drift                                         | `approval_key_corrupt`                                       | No lease.                                                           |
| Positioned key `read`                                                       | candidate incomplete                                         | positive short read                                                             | continue within exact remaining range and 64-attempt ceiling | Candidate retained only within call.                                |
| Positioned key `read`                                                       | candidate or EOF proof                                       | `EINTR`                                                                         | continue only within shared 64-attempt ceiling               | Bounded retry claim only.                                           |
| Positioned key `read`                                                       | candidate incomplete                                         | zero/unexpected EOF                                                             | `approval_key_corrupt`                                       | Candidate wiped; no lease.                                          |
| Positioned key `read`                                                       | any read                                                     | overrun/malformed count, other failure, or attempt exhaustion                   | `approval_key_io_failure`                                    | Candidate wiped; no lease.                                          |
| One-byte EOF proof                                                          | after exactly 32 bytes                                       | one trailing byte                                                               | `approval_key_corrupt`                                       | Candidate and EOF scratch wiped; no lease.                          |
| Candidate content                                                           | after exact read                                             | all zero                                                                        | `approval_key_corrupt`                                       | Candidate wiped; no lease.                                          |
| Load post-read key descriptor `stat`                                        | after exact data/EOF                                         | boundary/malformed result                                                       | `approval_key_io_failure`                                    | Candidate wiped; no lease.                                          |
| Load post-read descriptor metadata/identity                                 | after exact data/EOF                                         | any drift/unsafe state                                                          | `approval_key_unsafe`                                        | Candidate wiped; no lease.                                          |
| Load final key `lstat`                                                      | after post-read stat                                         | `ENOENT`, `ELOOP`, metadata or identity drift                                   | `approval_key_unsafe`                                        | Candidate wiped; no lease.                                          |
| Load final key `lstat`                                                      | after post-read stat                                         | other boundary failure                                                          | `approval_key_io_failure`                                    | Candidate wiped; no lease.                                          |
| Load final parent descriptor/path revalidation                              | before close                                                 | boundary failure                                                                | `approval_key_io_failure`                                    | Candidate wiped; no lease.                                          |
| Load final parent metadata/identity                                         | before close                                                 | unsafe state or substitution                                                    | `approval_key_unsafe`                                        | Candidate wiped; no lease.                                          |
| Load key `close`                                                            | cleanup                                                      | cleanup-only failure/non-undefined result                                       | `approval_key_io_failure`                                    | Candidate wiped; no lease; no close retry.                          |
| Load directory `close`                                                      | cleanup                                                      | cleanup-only failure/non-undefined result                                       | `approval_key_io_failure`                                    | Candidate wiped; no lease; no close retry.                          |
| Any load cleanup after an earlier primary                                   | cleanup                                                      | key/directory close or parent preclose revalidation also fails                  | first primary unchanged                                      | Candidate wiped; no lease; injected cleanup text is not retained.   |
| Any provision cleanup before exclusive fulfillment after an earlier primary | cleanup                                                      | parent revalidation/directory close also fails                                  | first primary unchanged                                      | No new key effect; existing state untouched.                        |
| Any provision primary or cleanup after exclusive fulfillment                | post-fulfillment                                             | one or multiple failures                                                        | `approval_key_commit_unknown`                                | Final artifact remains untouched; no success and no retry.          |
| Successful load cleanup                                                     | after all validation                                         | key close then directory close both fulfill with `undefined`                    | none                                                         | Only then create exact frozen lease over the one candidate buffer.  |

Fixed public messages: `approval_key_invalid` → `Phase 3 approval key input is invalid`; `approval_key_missing` → `Phase 3 approval key is missing`; `approval_key_exists` → `Phase 3 approval key already exists`; `approval_key_unsafe` → `Phase 3 approval key state is unsafe`; `approval_key_corrupt` → `Phase 3 approval key is corrupt`; `approval_key_commit_unknown` → `Phase 3 approval key commit is unknown`; `approval_key_io_failure` → `Phase 3 approval key I/O failed`.

Validation contract: focused `pnpm exec vitest run tests/phase3ApprovalKey.test.ts tests/phase3Isolation.test.ts --maxWorkers=1 --testTimeout=30000`; focused native Linux/Docker execution of `tests/phase3ApprovalKey.test.ts` from the same snapshot when available; changed-file formatting/lint, typecheck, build, add-on context, exact mirror/scope/diff checks; unchanged `validate:linux:approval` and `validate:linux:persistence`; authoritative `CI=true pnpm verify`; same-snapshot external/offline clean-room locked install and authoritative verify plus native focused test when feasible; complexity prune after correctness; independent high-risk review.

Initial implementation evidence: Windows focused tests passed 28/28 with three native-only tests skipped; changed-source lint, typecheck, build, add-on context, diff checks, and authoritative `CI=true pnpm verify` passed with 40 files, 1,110 tests passed, and 12 skipped. In pinned image `sha256:1c489404380cadf3a66d3440da070ce35ca3669cb8c8c9b56ee960834e236c04`, network-disabled native focused tests passed 22/22, `validate:linux:approval` passed 56/56, and `validate:linux:persistence --cc cc --tmpfs-root /durability` passed 45/45 against an isolated 128 MiB tmpfs. The final same-image offline clean-room authoritative verify failed on seven failures reproduced in the earlier base `4a4d969` run: four unavailable `openssl` spawns, one pre-existing Linux hardlink message assertion, and two nondeterministic process-group cleanup failures; the branch introduced no clean-room regression, but the authoritative clean-room result is classified `FAILED (INHERITED_BASELINE_FAILURES)`. Phase 3P itself passed 22/22 in that clean room. The initial independent-review route was blocked by unavailable agent/job capability and denied local Codex execution; a subsequent independent review produced the four repair-cycle-1 findings below.

Repair-cycle-1 evidence: release and internal fixed-size wipes no longer consult shadowable buffer length/prototype state; native test artifacts use exclusively created paths beneath unique tracked parents with per-test removal proof; successful load asserts exact no-follow flags and a deterministic regular-file-to-symlink substitution returns unsafe; and recursive TypeScript AST inspection covers static import, export-from, dynamic import, require, `module.require`, `require.resolve`, and import-equals module specifiers. Windows focused tests passed 29 with three native-only tests skipped, and the pinned network-disabled Linux focused run passed 23/23. Changed-file formatting/lint, full repository formatting/lint, typecheck, build, add-on context, mirror, scope, and diff checks passed. The monolithic `CI=true pnpm.cmd verify` invocation timed out after 90 seconds without buffered output; its exact stages run separately passed, including 40/40 test files with 1,111 tests passed and 12 skipped. The refreshed same-image offline clean room passed Phase 3P 23/23 and failed only on the same seven structurally matched inherited base failures, with 1,111 tests passed, seven failed, and five skipped. Root/add-on source SHA-256 is `4E28DA893BE2E6F2707BACFA9BF1059E528F401FDDFC0CF6E35A300D2D1EA074`. A fresh independent post-repair verdict remains required.

Repair-cycle-2 evidence: a prototype-removed nonzero Buffer and a DataView are recognized through intrinsic ArrayBuffer-view state, their retained backing bytes are zeroed, and both retain the fixed `approval_key_invalid` result while only genuine Buffer values remain accepted. Injected provision tests make each representative post-write descriptor field unsafe and substitute final key device/inode; every case returns `approval_key_commit_unknown` and preserves the artifact. The AST fixture recognizes seven forms including literal `require.resolve("../src/phase3/approvalKey.js")`. Windows focused tests passed 30 with three native-only tests skipped. Changed-file and repository formatting/lint, typecheck, build, add-on context, mirror, scope, and diff checks passed; authoritative `CI=true pnpm.cmd verify` exited zero with 40 files, 1,112 tests passed, and 12 skipped. Root/add-on source SHA-256 is `AC5BF411F1EAEE9E6F7A1A12CD5CE081021B0F64F96385343DED07B08ECD02D8`. After the local Docker daemon recovered, the same-snapshot pinned Linux focused run passed Phase 3P 24/24. A network-disabled same-image clean-room frozen install reused all 255 packages and downloaded zero; its authoritative verify passed Phase 3P 24/24 and reproduced only the same seven inherited base failures: four unavailable `openssl` spawns, one case-sensitive hardlink-message assertion, and two process-group cleanup failures. The final independent post-repair review found no actionable issues and returned `APPROVED`.

Explicit residuals: descriptor-relative `openat`/`openat2`; transient same-UID/root swap-and-restore; mount namespaces; process death; close/fsync ambiguity across death; real power loss; restart/persistence reconciliation; corrupt interrupted-key removal; cross-process commit marker/lock; rotation/deletion; simultaneous key+store loss; rollback; backup; operator authorization/audit/status/recovery; production custody; composition/registration/activation; release/publication; and live mutation remain `BLOCKED` or `UNVERIFIED`.
