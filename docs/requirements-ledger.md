# Requirements ledger

Status: Phase 1 validation contract, updated 2026-07-15

## Objective

Build a standalone, production-quality TypeScript MCP server that complements Home Assistant's official MCP server by providing bounded, auditable configuration inspection and a staged, validated, reversible mutation workflow without exposing generic shell, arbitrary file writes, secrets, or unrestricted service calls.

## Risk

`HIGH`. The project crosses authentication, authorization, filesystem, Git, process execution, smart-home availability, secret handling, public MCP contracts, and cross-platform deployment boundaries. Production mutation is explicitly outside the initial development phase.

## Acceptance criteria

| ID     | Observable requirement                                                                                                                                                                                                                                                                                                                                                                           | Planned implementation evidence                                                                                                                               | Planned validation evidence                                                                                                                                                                                                                                                                                                                     |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC-001 | Codex connects using distinct `ha_*` engineering tools through local stdio and the HA add-on's authenticated Streamable HTTP endpoint; an optional local stdio bridge preserves stdio-only clients.                                                                                                                                                                                              | Shared tool registry with stdio, HTTP, and bridge transport adapters plus Codex examples.                                                                     | MCP client integration tests list/invoke identical tools over both transports and through the bridge.                                                                                                                                                                                                                                           |
| AC-002 | REST and WebSocket clients provide authenticated, typed, timed-out read access without leaking credentials through URLs, redirects, proxies, or cross-origin forwarding.                                                                                                                                                                                                                         | Separate clients with validated responses, strict endpoint policy, bounded retries for idempotent reads, and WebSocket reconnect policy.                      | Mock HTTP/WebSocket integration tests for success, 401, timeout, disconnect/reconnect, rejected URL userinfo/scheme, redirects, and cross-origin credential non-forwarding.                                                                                                                                                                     |
| AC-003 | Read-only tools inspect system information, entities, automations, scripts, helpers, dashboards, scenes, blueprints, config, errors, Git, and pending diffs with filtering/pagination.                                                                                                                                                                                                           | Read services and bounded result contracts.                                                                                                                   | Unit and integration tests for empty/single/multiple and truncation paths.                                                                                                                                                                                                                                                                      |
| AC-004 | Configuration access is confined to configured roots and safe text formats.                                                                                                                                                                                                                                                                                                                      | Canonical-path policy rejects traversal, out-of-root paths, symlink escapes, disallowed symlink writes, oversized/binary/unsupported files.                   | Temporary-filesystem adversarial tests on Ubuntu and Windows.                                                                                                                                                                                                                                                                                   |
| AC-005 | Home Assistant YAML tags and split includes are handled without resolving `!secret`; structured edits preserve comments/order and avoid unrelated reformatting where feasible.                                                                                                                                                                                                                   | Round-trip YAML document model and include graph scanner.                                                                                                     | Fixtures for all requested include tags, duplicate keys, comments, anchors, and secrets.                                                                                                                                                                                                                                                        |
| AC-006 | Every write begins as an immutable, expiring proposal containing all requested metadata, content hashes, exact diff, validation/risk/reload information, and redacted output.                                                                                                                                                                                                                    | Proposal repository and state machine.                                                                                                                        | Hash, expiry, stale proposal, tampering, redaction, and serialization tests.                                                                                                                                                                                                                                                                    |
| AC-007 | Applying a proposal rechecks hashes and approval, creates a recoverable checkpoint, validates, applies atomically, revalidates, reloads the narrowest domain, verifies, and automatically restores on failure.                                                                                                                                                                                   | Transaction coordinator with injected filesystem, validator, reload, verification, backup, and Git ports.                                                     | Full success and failure workflow tests, including crash/failure injection at every state transition.                                                                                                                                                                                                                                           |
| AC-008 | Writes, restarts, and deletes default disabled; high-risk/destructive actions require explicit approval metadata; deletion is unavailable by default.                                                                                                                                                                                                                                            | Central policy engine evaluated before side effects.                                                                                                          | Authorization matrix and configuration-default tests.                                                                                                                                                                                                                                                                                           |
| AC-009 | Service calls are explicit allowlisted operations; no generic service-call or shell/write tool exists.                                                                                                                                                                                                                                                                                           | Capability registry and deployment command allowlist.                                                                                                         | MCP tool inventory snapshot and policy rejection tests.                                                                                                                                                                                                                                                                                         |
| AC-010 | Validation has local, deployment-aware Home Assistant, and post-apply levels with machine and human summaries.                                                                                                                                                                                                                                                                                   | Validator composition and deployment adapters.                                                                                                                | Parser, check-config result, timeout, unavailable-adapter, and post-apply tests.                                                                                                                                                                                                                                                                |
| AC-011 | Reloads prefer affected domains; restart is separate, disabled by default, and never used merely to obtain validation.                                                                                                                                                                                                                                                                           | Change-impact mapper and reload/restart policy.                                                                                                               | Mapping tests and restart-denial tests.                                                                                                                                                                                                                                                                                                         |
| AC-012 | Git operations are scoped to proposal files, detect unrelated changes/conflicts, avoid history rewriting and remote pushes, and commit only through separately authorized `ha_commit_change`; apply checkpoints are non-commit backups/journals.                                                                                                                                                 | Git adapter using argument arrays/scoped pathspecs plus separate backup store.                                                                                | Temporary-repository tests for dirty trees, conflicts, scoped commits, rollback, no apply-time commits, and no remote operations.                                                                                                                                                                                                               |
| AC-013 | Every tool call is durably represented by a redacted JSONL audit record; if the audit sink is unavailable, the call fails before protected data or side effects are returned.                                                                                                                                                                                                                    | Fail-closed audit middleware, startup health check, serialized append sink, and safe rotation contract.                                                       | Schema, redaction, partial-write recovery, concurrency, injection, permissions, disk/write failure, correlation, and rotation tests.                                                                                                                                                                                                            |
| AC-014 | Structured automation/script/helper/dashboard/scene tools generate proposals and cannot bypass workflow.                                                                                                                                                                                                                                                                                         | Domain builders call the same proposal service.                                                                                                               | Contract tests proving no direct write path.                                                                                                                                                                                                                                                                                                    |
| AC-015 | Deployment-specific validation, reload, restart, and config access are isolated for OS, Supervised, Container, and Core; first supported target is selected only after environment discovery.                                                                                                                                                                                                    | Deployment adapter interfaces and capability negotiation.                                                                                                     | Adapter contract tests plus supported-target integration/E2E tests.                                                                                                                                                                                                                                                                             |
| AC-016 | CLI provides `doctor`, `check-auth`, `check-config-path`, `check-git`, `validate`, `list-capabilities`, and `show-policy` without disclosing secrets.                                                                                                                                                                                                                                            | CLI composition over the same application services.                                                                                                           | CLI exit-code, output, redaction, and unavailable-capability tests.                                                                                                                                                                                                                                                                             |
| AC-017 | Configured secret-source files are metadata-only and excluded from reads/search/index/diff/Git-content/diagnostic paths; known exact secret values and credential patterns are redacted from all outputs, while raw HA logs are never returned by default.                                                                                                                                       | Secret-source registry, canonical identity checks, exact-value redactor held only in memory, heuristic credential redactor, and safe error summarizer.        | Direct/alias/symlink read and search denials plus canary tests across diffs, logs, responses, audit, diagnostics, and Git output.                                                                                                                                                                                                               |
| AC-022 | High-risk approval cannot be satisfied by MCP-client assertions; it uses a separate operator-controlled out-of-band channel and binds one-time authorization to proposal ID and diff digest.                                                                                                                                                                                                     | Phase 3 approval port, initially local CLI confirmation producing a short-lived single-use server-side grant.                                                 | Forgery, replay, expiry, restart, wrong digest/proposal, and cancellation tests.                                                                                                                                                                                                                                                                |
| AC-023 | The project installs as a custom Home Assistant add-on on HA OS/aarch64 without `/config` in Phase 1, host access, privileged mode, Docker socket, general Supervisor API, or public exposure; all mutation options default false.                                                                                                                                                               | Add-on repository metadata, container, minimal permissions, schema/options, health/start scripts, and ingress diagnostics/setup.                              | Metadata/schema validation, aarch64 container build, startup/health tests, manifest capability inspection, and absence-of-mount tests.                                                                                                                                                                                                          |
| AC-024 | The disabled-by-default HTTPS transport uses generated/pinned TLS plus distinct paired client credentials and enforces secure ingress bootstrap, hashed secrets, per-client rotation/revocation, Host/Origin/proxy, body/output, session ownership, timeout, rate/concurrency, topology, and exposure policies; neither credential leaves its trust boundary or crosses plaintext LAN transport. | Separate ingress/HTTPS listeners, certificate/client credential stores under `/data`, HTTP security middleware, and bridge OS credential/pinning integration. | Startup-default, TLS generation/storage/fingerprint/rotation, entropy/hash/constant-time, unauthorized/query credential, non-loopback plaintext refusal, Host/Origin/DNS-rebinding/forwarded-header, oversized, replay/cross-client/session expiry, rotation, concurrency/overload, logging-canary, topology, MITM, and identity-pinning tests. |
| AC-025 | In add-on mode the HA client uses only the Supervisor-injected runtime token against the fixed Home Assistant Core API proxy and never accepts arbitrary origins/paths or exposes/persists the token.                                                                                                                                                                                            | `homeassistant_api: true` manifest plus fixed-origin/path HA client credential provider.                                                                      | Manifest denial assertions, fixed-origin/path tests, 401/403 redaction, and token canaries across options, diagnostics, audit, logs, MCP and bridge traffic.                                                                                                                                                                                    |
| AC-018 | Documentation covers architecture, security, deployment types, setup, tools, least privilege, backup, mutation kill switch, audit rotation, and all requested recovery cases.                                                                                                                                                                                                                    | Required documentation set.                                                                                                                                   | Documentation link/command/example review.                                                                                                                                                                                                                                                                                                      |
| AC-019 | Tests are layered: unit, mocked API integration, temporary filesystem/Git, complete workflow, rollback, and practical Docker E2E without production access.                                                                                                                                                                                                                                      | Test suites and fixtures.                                                                                                                                     | Phase gates and clean-room validation.                                                                                                                                                                                                                                                                                                          |
| AC-020 | Each phase runs formatting, linting, type checking, relevant tests, and security tests; critical safety failure stops progress.                                                                                                                                                                                                                                                                  | Package scripts and CI phase gates.                                                                                                                           | Exact command evidence and clean-room runs.                                                                                                                                                                                                                                                                                                     |
| AC-021 | No live Home Assistant change occurs without explicit approval of the exact change; no commit/push/deploy occurs unless separately requested.                                                                                                                                                                                                                                                    | Operational policy and human gates.                                                                                                                           | Review of audit trail and prohibited-action checks.                                                                                                                                                                                                                                                                                             |

## Scope

In scope: the standalone engineering MCP server, read-only HA API integration, bounded config repository access, proposal lifecycle, validation/reload/verification/rollback, scoped Git, audit, CLI, adapters, tests, and documentation.

Out of scope for initial delivery: replacing the official Home Assistant MCP server, arbitrary shell/file/service access, remote Git push, cloud deployment, production configuration mutation, delete tools enabled by default, token management UI, and supporting undocumented Home Assistant internals without a versioned adapter and fallback.

Public contracts: MCP tool names/input/output schemas; environment variable names; proposal/audit schemas; CLI commands/exit codes; deployment adapter capability model.

## Constraints and assumptions

- TypeScript/Node.js with one registry exposed through local stdio and the Phase 1 add-on's TLS-only Streamable HTTP transport; the optional bridge preserves stdio-only Codex clients.
- The HA add-on/aarch64 image is the authoritative runtime. Platform-neutral core/bridge logic remains portable to Linux/Windows/macOS where supported.
- Credentials come only from environment/external secret storage. Token values are never accepted as MCP tool arguments.
- Direct filesystem access is a negotiated capability, not an assumption.
- API-managed resources and YAML-managed resources require separate repository adapters.
- `secrets.yaml` and configured/renamed secret-source files are categorically content-inaccessible through MCP. Their canonical identities, aliases, includes, and symlinks are tracked so alternate paths cannot bypass the rule.
- The actual target is Home Assistant OS 18.1 on Raspberry Pi 5 (`aarch64`), Core 2026.7.2, Supervisor 2026.06.2, with add-on-mapped `/config` and storage-mode dashboards.
- Home Assistant OS is not treated as a general-purpose Node host. The server is packaged as a least-privilege managed add-on; no direct OS access is required.
- The repository's authoritative deterministic validation command is `pnpm verify`.

## Behavioural matrix

| Dimension           | Required paths                                                                                                                                                             |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Policy              | reads enabled; writes disabled/enabled; restart disabled/enabled; deletes disabled and explicit approval if ever enabled                                                   |
| Identity            | authorized; missing/expired/invalid token; insufficient HA privileges; forged/replayed/expired out-of-band approval                                                        |
| Data size           | empty; single; multiple; paginated/truncated; oversized rejection                                                                                                          |
| Filesystem          | valid relative path; traversal; absolute outside root; symlink escape; changed inode/hash; unsupported/binary encoding                                                     |
| Proposal            | valid; expired; stale source; already applied/discarded; approval absent/mismatched; concurrent proposals touching same resource                                           |
| Apply               | success; local validation failure; HA validation failure; atomic write failure; reload failure; verification failure; rollback success/failure; crash recovery             |
| API                 | success; 4xx; 5xx; timeout; safe-read retry; mutation no-retry; WebSocket disconnect/reconnect/correlation                                                                 |
| Git                 | clean; scoped dirty; unrelated dirty; conflict; no repository; detached branch; checkpoint/final commit failure                                                            |
| Deployment          | HA OS 18.1 on aarch64 as first live target; OS/Supervised/Container/Core adapter contracts; capability unavailable; clean install vs existing config                       |
| Dashboard storage   | storage-mode discovery/read via supported API; mutation capability unavailable unless a supported API is verified; YAML-mode fixtures/adapters remain separate             |
| Platform            | HA add-on/aarch64 authoritative; amd64/Linux container for development and clean-room validation; Windows/macOS coverage for the optional bridge and platform-neutral core |
| Resource management | YAML storage; UI/API storage; include graph; resource missing/renamed/disabled/active                                                                                      |

Cancellation is relevant for CLI/MCP requests before side effects; once an apply transaction crosses its commit point it must finish verification or rollback rather than abandon state. Backward compatibility begins once Phase 1 tool schemas ship.

## Validation plan

Targeted: schema, policy, redaction, path, YAML, proposal, API, Git, and transaction tests. Full validation: format check, lint, `tsc --noEmit`, all non-E2E tests, package/build check, MCP smoke test, and security-focused suite. Environment: Ubuntu supported-adapter diagnostics and Docker HA E2E where practical. Clean room: required from Phase 1 onward because this is high-risk and cross-platform. The user authorized and completed the named Phase 1 read-only diagnostic recorded in [deployment.md](deployment.md). Deployed 0.1.4 recent-error and malformed-cursor checks failed; repository candidate 0.1.5 repairs remain `UNVERIFIED` live pending separately authorized deployment. Mutations remain prohibited.

## Required human approvals

1. Approve package/base-image resolution required for the add-on and bridge implementation (the user's “let's start” authorizes Phase 1 dependency resolution, not deployment).
2. Pair each local bridge/client through ingress and verify the TLS fingerprint during installation; public exposure remains prohibited.
3. Approve enabling writes after read-only phases and backups are verified.
4. Approve each high-risk/destructive proposal through the operator-controlled channel with matching proposal ID/diff digest; MCP-supplied identity fields are never sufficient.
5. Separately request any Git commit; pushing remains outside default scope.

## Prohibited actions

No production mutation, unrestricted shell, arbitrary file write, unrestricted HA service call, token exposure, commit, push, merge, rebase, history rewrite, publish, deploy, deletion, or restart during planning and initial development.
